{
  "version": "0.0.1",
  "configurations": [
    {
      // Attach-only: a `vite preview --port 5173` is already running and the
      // Cognito redirect URI is registered as http://localhost:5173/auth/callback,
      // so the port is not negotiable — logging in anywhere else fails the
      // callback. No command here on purpose; starting a second server on a
      // taken port would just fail.
      "name": "console-preview",
      "url": "http://localhost:5173",
      "port": 5173
    }
  ]
}
