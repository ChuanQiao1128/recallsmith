# B09 — DrawCeremonyScreen.tsx wiring: phase machine driven ONLY by timers, delete Lottie/orbit setInterval/slider, wire B02–B08, Reduce Motion parallel path, fallback renderer, tapFlow param, feature-flag gate; integration test edits per test_changes (`ceremony-screen-wiring`)

Rewrite `DrawCeremonyScreen.tsx` as the thin state machine of the Seam of Light ceremony: seven phases advanced only by `setTimeout`, the DEVICE/TEST_BASE tables from B02, the timeline hook from B07 feeding one `StageCanvas` + one `PackTear` (skia renderer) or the new `FallbackStage` (no Skia / kill switch), B08 `TapCard`s that are reachable on every device path and enabled only on the table, a Reduce-Motion parallel path that keeps the table, a fast-forward that compresses but never leaves, an always-present accessible `Leave ceremony`, and the integration test rewritten by the exact transform B00 §4.2 prescribes plus eight new cases.

> **Scheduling (binding, B00 §5 / §9 #2):** this issue depends on **B05, B06, B07, B08, B10 and B12** and must not be started before all six have merged into the integration branch — the wave spec's original `B09 deps: B05,B06,B07,B08` / `B10 deps: B09` is the cycle §9 #2 removed. Step 0 of `B09.verify.sh` fails fast with "deps not merged" when `CEREMONY_COPY_V10` (B10) or `GLOW_9SLICE` (B12) is absent. **Time budget: 150 minutes**, not the 90 the spec lists for the ~1,200-LOC issues (this one is ≈ 1,230 LOC across six files with a byte-exact test prefix to keep).

## Context

Today the table is unreachable and would crash: `CeremonyLottie`'s `onAnimationFinish` calls `goResult()` during settle (`mobile/src/screens/DrawCeremonyScreen.tsx:1034-1041`) while the `cards-on-table` timer is scheduled `autoAdvanceTail = ceremonyLottieAvailable ? 4500 : 500` ms later (`:758-785`), so on a device with Lottie the Lottie callback always wins (design §1 row `docs/release-1.6.0-plan-2026-09-19.md:41`; §3.3 `:125`: "the table is dead code on every device with Lottie installed (and would crash at mount with any RAR/LEG card via HolographicLayer)"). The rest of the audit lives in this file too: `hasAnimated`/`TIMING_SCALE` pick the timing table from whether RN `Animated` exists (`:52`, `:95`), the inline `phaseDurations` (`:97-125`) and constants (`:67-69`) that B02 now owns, the 33 ms orbit `setInterval` that re-renders the whole tree ~70 times (`:798-819`, `:1085-1177`) and pads a 2-card pull to six clones (`:826-836`), the slider control (`:1071-1082`), the always-visible × that throws away the reveal (`:882-894`), the rarity leaked from frame 0 through `haloColor` (`:465`, `:919-958`) and the approach title (`:452-459`), the footer that keeps the rarity word in the tree at every phase (`:1358-1368`), the RM path that fires the 0.85 flash for 180 ms and never reaches the table (`:714-728`, `:1393-1403`), and the RN `Animated` refs (`:416-440`) whose choreography B07 replaced. Everything animated the screen still touches (`SparkleField`, `ParticleBurst`, `MultiPackFlyIn`, `buildParticles`, `RevealCard`, `HolographicLayer`) is imported from `CeremonyLottie.tsx` / `HolographicLayer.tsx` (`:24-38`), which B11 deletes.

The rule that fixes it (design §3 `:87`, §3.3 `:125`, B00 §3.2 `:650-652`): **no phase transition or navigation depends on a native callback**; one `useEffect` schedules every transition from `startSequence()` with `setTimeout`, and the table is entered from settle by timer (`settleMs + tableTailMs`) whenever `enableTapFlow = route.params.tapFlow ?? (motionAvailable && cards.length > 0)`. Under vitest `motionAvailable` is false (B00 §2.1), so the TEST_BASE table (`tableTailMs` 500) and the tapFlow-off auto-replace keep the retained integration cases byte-identical in cadence (`tests/integration/draw-ceremony.screen.test.tsx:73-92` `MULTI_TIMING`/`SINGLE_TIMING`/`REDUCED_TIMING`; the multi case's `advanceTimersByTime(820)` after settle at `:215-217`, the RM 180/420 cadence at `:381-402`). The renderer choice is separate from the timing choice (B00 §3.5 `:668-680`): `'skia'` only when `motionAvailable && skiaAvailable && flags.ceremony.seamOfLight && !flags.ceremony.forceFallback && !dev.forceFallback`; under vitest always `'fallback'`, so every new test asserts against `FallbackStage`. The exhaustive list of what this issue may change in the integration test — three Lottie cases deleted, the fourth de-Lottied with four mechanical edits, ten literals (already applied by B10, idempotent here), eight new cases — is B00 §4.2 (`:711-738`) with the resolution in §9 #1 (`:814`); B09.verify.sh derives the expected 596-line prefix from the base file and compares it byte-for-byte.

The pieces this screen assembles all exist on the branch you start from (deps B05, B06, B07, B08, B10, B12; B00 §5 `:769`): `reanimatedGuard.ts` + `ceremonyTimings.ts` (B02), `spillSchedule.ts` / `skipPolicy.ts` / `ceremonyPrefs.ts` (B03), the three-layer `ceremonyAudio.ts` and rate-limited `ceremonyHaptics.ts` (B04), `StageCanvas.tsx` (B05), `PackTear.tsx` + `SEAM_BAND_RATIO` (B06), `useCeremonyTimeline.ts` with `tableSlotLayout`/`spillSlotOffset` (B07), `TapCard.tsx` / `FoilLayer.tsx` / `ceremonyStyles.ts` (B08), `CEREMONY_COPY_V10` + the `tapFlow`/`revealedUids`/`tableReached` params (B10), and the frame/particle/glow assets in `packArt.ts` (B12). Three leaf components are created here because B11 (which was to create them) runs after this issue and this screen may not import a `Lottie`-named file (B00 §9 #4 `:817`): `FeaturedCard.tsx` (the static `RevealCard`), `FallbackStage.tsx` (the no-Skia stage) and `SpillSampler.tsx` (the orbit sampling contract kept on a leaf — design decision #9, `release-1.6.0-plan:299`, B00 §2.12 `:552`).

## Read first

1. `docs/delivery/r16-issues/B00-contracts.md` §2.1 (`:107-186`), §2.2 (`:187-273` — `resolveCeremonyTimings`, `phaseDurations`, `compressTimings`, `REDUCED_MOTION_*`, `SWIPE_TRIGGER_DISTANCE`, `FAST_FORWARD_FROM_HOLD_FRACTION`, `FAST_FORWARD_TEAR_FACTOR`), §2.3–2.5 (`:274-350`), §2.6–2.7 (`:351-423`, the audio/haptic controllers you cue), §2.8 (`:424-448`, `StageCanvas` props), §2.9 (`:449-476`, `PackTear` props, the pack testID rule, `PACK_A11Y_*`, `seamProgressFromDelta`), §2.10 (`:477-508`, `TimelineInput` and the binding "Units" paragraph), §2.11 (`:509-545`, `TapCard` props), §2.12 (`:546-552`, the three files you create + the styles you append — verbatim contract), §2.13–2.14 (`:553-584`, copy + params), §3 in full (`:644-691` — phases, timers, RM path, flag, renderer/tapFlow, controls and CTA copy), §4.2 (`:711-738`, the test transform + the eight new cases), §4.3 (`:739-742`, testIDs that must survive), §7 (`:796-805`), §8 (`:806-811`), §9 #1, #2, #4, #11, #14, #15, #17 (`:814-830` — the last three: seam ownership, the `PanHost` naming rule, the camera wrapper).
2. `mobile/src/screens/DrawCeremonyScreen.tsx` in full (1418 lines; you replace it). **Keep verbatim** (byte-identical bodies): `resolvePeakRarity` (`:71-88`), `flashColor` (`:127-131`) and the reduce-motion listener effect (`:497-514`). **Keep the shape of, with these mandatory edits** (the verify's step 5/6 greps reject the originals): the stage responder (`:987-1021` — extended per change 5l: `stageOwnsSwipe`, `seamProgressFromDelta`, `timeline.seam.value`, the compress tap); the phase-copy `Text`s (`:968-985` — **drop** `accessibilityElementsHidden` and `importantForAccessibility` (both banned tokens), **add** `accessibilityLiveRegion="polite"` on the title `Text`, keep the testIDs, `style={styles.phaseCopyHidden}` and `numberOfLines={1}`); the CTA (`:1370-1386` — same `Pressable` props and `style={({ pressed }) => [styles.skipButton, pressed && styles.pressed]}`, but the text comes from `CEREMONY_COPY_V10.showResult / continueCta / skipProgress(...)` (no `'Continue'` / `'Show result'` / `` `Skip · ` `` literals) and `onPress={ctaPress}` (not `goResult` — `'Continue'` routes through `onContinue`, change 5k)); the flash `View` (`:1393-1403` — `opacity: phase === 'flash-reveal' && !reduceMotion ? 0.85 : 0` and a third style entry `renderer === 'skia' ? styles.flashHiddenBehindCanvas : null`). `readRN` (`:40-47`) is **not** kept in the screen at all (no `Image` here; the token `readRN(` is banned in this file — it lives in `FeaturedCard.tsx`/`FallbackStage.tsx` only). Delete: `hasAnimated`/`TIMING_SCALE`/`phaseDurations` (`:49-52`, `:90-125`), `pickFeaturedCard` (`:133-140`, → `featuredCardIndex` from B03), the inline `TapCard` (`:142-388`), every `useRef(new A.Value…)` (`:416-440`), the halo/bob/phase `Animated` effects (`:516-700`), the orbit interval (`:798-819`) and derived values (`:821-875`), the halo stack (`:896-961`), the × (`:882-894`), the slider (`:1071-1082`), the orbit stage (`:1084-1177`), the Lottie mount (`:1032-1042`), `flashCore` (`:1404-1412`).
3. `mobile/tests/integration/draw-ceremony.screen.test.tsx` in full (787 lines, base blob `7dd36b4d…`; after B10 the ten literals differ and the count is still 787): the mocks `:9-47`, helpers `:94-121` (`collectText`, `phaseTitle`, `phaseBody`, `armCeremonySwipe` — the raw responder on `draw-ceremony-stage`), the nine cases that survive (`:135-516`, `:636-733`) and the three you delete (`:518-634`, `:735-786`).
4. `mobile/src/components/CeremonyLottie.tsx:271-327` (the static `RevealCard` branch → `FeaturedCard`), `:548-554`, `:589-594`, `:615-621`, `:643-663`, `:697-729`, `:754-808`, `:897-960`, `:975-997` (the screen-level style entries you append to `ceremonyStyles.ts`), `:24-27` (theme imports).
5. `mobile/src/components/ceremony/{reanimatedGuard,useCeremonyTimeline,StageCanvas,PackTear,TapCard,FoilLayer,ceremonyStyles}.ts(x)`, `mobile/src/features/gacha/draw/{ceremonyTimings,spillSchedule,skipPolicy,ceremonyPrefs,ceremonyCopy}.ts`, `mobile/src/components/{ceremonyAudio,ceremonyHaptics}.ts`, `mobile/src/theme/packArt.ts`, `mobile/src/config/featureFlags.ts`, `mobile/src/navigation/types.ts` — as they are on your branch; confirm every name you import exists with the signature B00 gives before writing a line.
6. `docs/release-1.6.0-plan-2026-09-19.md:96-104` (S0–S6/FF/RM), `:110-117` (M0–M6), `:121` (§3.2), `:125` (§3.3), `:129` (§3.4), `:163` (§3.7 row for this file), `:180-185` (DoD bullets 2–7).
7. `mobile/tests/unit/libraryCardTile.test.tsx:1-25` (mock shape) and `mobile/tests/integration/draw-result.screen.test.tsx` (B10's `revealedUids` consumer — you pass what it reads).

## Constraints

- **Scope (the ONLY files that may change):**
  - `mobile/src/screens/DrawCeremonyScreen.tsx` (rewrite)
  - `mobile/tests/integration/draw-ceremony.screen.test.tsx` (the §4.2 transform + new cases only)
  - `mobile/src/components/ceremony/FallbackStage.tsx` (new — B00 §1/§2.12)
  - `mobile/src/components/ceremony/SpillSampler.tsx` (new — B00 §1/§2.12)
  - `mobile/src/components/ceremony/FeaturedCard.tsx` (new — B00 §1/§2.12)
  - `mobile/src/components/ceremony/ceremonyStyles.ts` (append the screen entries after B08's `tapCardShadow`; nothing above it changes)
- **Frozen files — zero diff:** `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`. Also untouched here: `CeremonyLottie.tsx` and `HolographicLayer.tsx` (B11 deletes them — you stop importing them, you do not edit them), every B02–B08/B10/B12 module, `ceremonyCopy.ts`, `types.ts`, `DrawResultScreen.tsx`, `DrawScreen.tsx`, `vitest.config.ts`, `tests/setup/*`, every other test.
- **No dependency changes.** `"vite": "7.2.4"` stays; nothing under `@sentry`.
- **Timers only.** Every phase transition and the tapFlow-off auto-replace is a `setTimeout` pushed to `timers.current` and cleared by the effect's cleanup. The screen contains no `setInterval`, no `onAnimationFinish`, no `Date.now()` except the two reads that measure elapsed-in-phase for `compress()` (fake timers fake `Date`, as today's `:801`). `goResult` is called from exactly four places: the settle CTA, the table CTA, `draw-ceremony-leave`, and the tapFlow-off / RM tail timer (B00 §3.2). `compress()` never calls it.
- **`react-native` imports.** The integration test's mock (`:9-36`) exports only `View`, `Text`, `Pressable`, `StyleSheet.{create,absoluteFillObject}`, `AccessibilityInfo`, `useWindowDimensions`. The screen and the three new leaves may name-import only those (plus `import type { ImageSourcePropType }`); `Image` is reached through `readRN('Image', null)` (`:40-50`, copied into each file that needs it) and rendered only when non-null. `AccessibilityInfo.announceForAccessibility?.(…)` is optional-called (the mock has no such member; a missing key on the *object* is `undefined`, not a throw).
- **Every Reanimated / Skia / gesture symbol comes from the guard** (`GestureHandler`, `Reanimated`, `motionAvailable`, `skiaAvailable`); the screen itself renders no Skia element and calls no `Reanimated.*` hook except through `useCeremonyTimeline` **and the single camera wrapper of change 5d/5l** (`Reanimated.useAnimatedStyle` once, `Reanimated.View` once — the only consumer of `timeline.cameraScale`/`cameraRot`, B00 §9 #17; under the guard fallback both are a plain `View` and a plain object, so no test sees a difference). No `import * as RN` in the screen at all (no `readRN` here — the leaves that need `Image` carry their own copy); no `readRN('Animated', …)`; no `Animated.Value`.
- **Test-literal rules (exhaustive, B00 §4.2):** the file must begin byte-for-byte with the 596-line prefix produced by the transform in change 7 (the verify script regenerates it from `git show "$BASE_REF":mobile/tests/integration/draw-ceremony.screen.test.tsx`); after the prefix only new `it(` cases, and the file ends with `});`. `MULTI_TIMING`/`SINGLE_TIMING`/`REDUCED_TIMING` (`:73-92`) are inside the prefix and therefore unchanged. No `vi.mock` of `CeremonyLottie`, no `mock-ceremony-lottie`, no `'Legendary inbound'`/`'Rare inbound'`/`'Card revealed'` anywhere in the file.
- **testIDs that must survive (both renderers; B00 §4.3):** `screen-draw-ceremony-root`, `draw-ceremony-backdrop`, `draw-ceremony-stage` (with `onStartShouldSetResponder`, `onMoveShouldSetResponder`, `onResponderGrant`, `onResponderMove`, `onResponderRelease`, `onResponderTerminate`), `draw-ceremony-phase-copy`, `draw-ceremony-phase-body-copy`, `draw-ceremony-hold-marker`, `draw-ceremony-swipe-pack`, `draw-ceremony-single-pack-flyin`, `draw-ceremony-multi-flyin`, `draw-ceremony-orbit-stage`, `draw-ceremony-orbit-progress`, `draw-ceremony-orbit-samples`, `draw-ceremony-orbit-focus`, `draw-ceremony-orbit-mode`, `draw-ceremony-reveal-flash` (plain `View`, contract object at `style[1]`), `draw-ceremony-reveal-rarity`, `draw-ceremony-reveal-question`, `draw-ceremony-cards-on-table`, `tap-card-${i}`, `draw-ceremony-footer-rarity`, `screen-draw-ceremony-primary-cta` + `nativeID="draw-ceremony-skip-hint"`. New: `draw-ceremony-stage-canvas` (`STAGE_TESTID`, skia only), `draw-ceremony-fallback-stage`, `draw-ceremony-spill-card-${i}`, `draw-ceremony-leave`, `draw-ceremony-fast-forward`. Dropped: `draw-ceremony-orbit-center`, `mock-ceremony-lottie`. Every `findByProps` target in the tests must be unique in the tree (one `'Reward pack'`, one `'Leave ceremony'`).
- **Copy:** every user-visible string comes from `CEREMONY_COPY_V9` / `getCeremonyPhaseCopy` / `CEREMONY_COPY_V10` (B10); the only literals typed in the screen are the flash colours (`flashColor`, `:127-131`) and the three rarity words of the footer (`'Legendary' | 'Rare' | 'Common'`, as today `:1367`).
- **Banned literals** in identifiers/comments/strings (driver gate): `humanizer`, `bypass`, `undetect`, `detector`, `evade`, `Gemini said`. No `@ts-ignore`, `@ts-expect-error`, `eslint-disable`, `.skip(`, `.only(`. No `shadowRadius` ≥ 16 in any file you write or append (clamp copied values to 8 — B00 §7.4's guard covers `mobile/src/screens/Draw*.tsx` and `mobile/src/components/ceremony`). Exactly one `<Canvas`/`<Skia.Canvas` across `DrawCeremonyScreen.tsx` + the three new leaves: zero — the stage canvas lives in `StageCanvas.tsx` and the foil in `FoilLayer.tsx` (B00 §7.3).

## Changes required

### 1. `mobile/src/components/ceremony/SpillSampler.tsx` (new)

```ts
export const SPILL_SAMPLE_MS = 100;
export type SpillSamplerProps = { durationMs: number; renderer: 'skia' | 'fallback' };
export const SpillSampler: React.NamedExoticComponent<SpillSamplerProps>;   // React.memo leaf
```
`import React, { useEffect, useState } from 'react'; import { Text, View } from 'react-native'; import { ceremonyStyles } from './ceremonyStyles';`. State `samples` (0). `useEffect` on `[durationMs]`: `const id = setInterval(() => setSamples((n) => n + 1), SPILL_SAMPLE_MS); return () => clearInterval(id);`. Derived: `progress = Math.min(1, (samples * SPILL_SAMPLE_MS) / Math.max(1, durationMs))` (tick-count based, no clock: strictly increasing under fake timers, saturating at 1), `focus = Math.min(1, progress * 1.35)` (today's `:823`). Render, testIDs verbatim (today's `:1085-1097`): root `View` `testID="draw-ceremony-orbit-stage"` `style={ceremonyStyles.spillSampler}` (a **1×1, absolute, opacity 0** box — change 4 — not today's 248×260 `orbitStage`, which would sit over the pack/spill area and, under the skia renderer, over the Canvas) `pointerEvents="none"` `accessibilityLiveRegion="polite"` `accessibilityLabel={`Dealing cards, ${Math.round(progress * 100)} percent`}` (B00 §2.12; equals `CEREMONY_COPY_V10.dealing(…)` — import it from `'../../features/gacha/draw/ceremonyCopy'` and use it), children `Text`s with `style={ceremonyStyles.holdMarker}`: `draw-ceremony-orbit-progress` → `progress.toFixed(2)`, `draw-ceremony-orbit-samples` → `samples` (a number child, as today `:1090`), `draw-ceremony-orbit-focus` → `focus.toFixed(2)`, `draw-ceremony-orbit-mode` → `renderer`. Export `SpillSampler = React.memo(function SpillSamplerImpl(...) {...})`. This is the **only** `setInterval` in the ceremony tree (B00 §3.2); it re-renders one invisible 1×1 leaf, not the screen.

### 2. `mobile/src/components/ceremony/FeaturedCard.tsx` (new)

```ts
export type FeaturedCardProps = { accent: string; rarityText: string; questionText: string; packPaletteCover: PackPalette['cover']; coverImage?: ImageSourcePropType; faceUp: boolean };
export function FeaturedCard(props: FeaturedCardProps): JSX.Element;
```
The static `RevealCard` branch (`CeremonyLottie.tsx:290-327`) without `Animated`: `import { StyleSheet, Text, View } from 'react-native'; import * as RN from 'react-native'; import { LinearGradient } from 'expo-linear-gradient'; import type { PackPalette } from '../../theme/packArt'; import { ceremonyStyles } from './ceremonyStyles';` + the `readRN` copy. Root `View style={[ceremonyStyles.flipCard, faceUp && [ceremonyStyles.flipCardRevealed, { borderColor: accent }]]}`. Always render **both** faces: the back (`LinearGradient colors={packPaletteCover} start={{ x: 0.1, y: 0 }} end={{ x: 0.9, y: 1 }} style={ceremonyStyles.flipBackGradient}` with the cover `RNImage` when available and the `Text style={ceremonyStyles.cardBackText}` reading `Reward card` — that literal is hard-coded at `CeremonyLottie.tsx:321` today and is not in the copy module; keep it byte-identical with a one-line comment saying so) and the front `View style={ceremonyStyles.flipFront}` with the chip `Text testID="draw-ceremony-reveal-rarity" numberOfLines={1}` → `rarityText` and `Text testID="draw-ceremony-reveal-question" numberOfLines={3}` → `questionText`. When `!faceUp` the front is wrapped in a `View` with `style={{ opacity: 0, transform: [{ rotateY: '180deg' }] }}` and `pointerEvents="none"` (rendered rotated away so both testIDs stay in the tree — B00 §2.12); when `faceUp` the back gets that treatment instead. Under `reduceMotion` nothing differs (it is static already).

### 3. `mobile/src/components/ceremony/FallbackStage.tsx` (new)

```ts
export const FALLBACK_STAGE_TESTID = 'draw-ceremony-fallback-stage';
export type FallbackStageProps = {
  phase: CeremonyPhase; isMulti: boolean; reduceMotion: boolean; peakRarity: PeakRarity;
  palette: PackPalette; coverImage?: ImageSourcePropType; pitySeal: boolean;
  /** true under reduceMotion or when phase !== 'swipe' (same rule as PackTear.disabled). */
  disabled: boolean;
  onTear: () => void;
  swipeProgress: number;                       // 0..1 from the stage responder
  cards: ReadonlyArray<{ stableUid: string; rarity: PeakRarity }>;
  spill: SpillSchedule | null;                 // slots for the static spill layout (multi)
  glowNineSlice?: ImageSourcePropType;         // GLOW_9SLICE (B12); undefined → plain border rims
  /** hold onward: rims/pack border take the rarity colour (the tell); before: neutral. */
  tellVisible: boolean;
  featured: FeaturedCardProps | null;          // mounted when non-null (tapFlow off, flash-reveal/settle)
};
export function FallbackStage(props: FallbackStageProps): JSX.Element;
```
Imports: `react`; `{ StyleSheet, Text, View } from 'react-native'` + `import * as RN` for `readRN`; `LinearGradient`; `{ PACK_A11Y_HINT, PACK_A11Y_LABEL, PACK_ACTIVATE_ACTION } from './PackTear'` (B06 constants — no Skia is touched by importing them; `PackTear` itself is not rendered here); `{ spillSlotOffset, tableSlotLayout, TELL_COLORS } from './useCeremonyTimeline'`; `{ rarityAccentColor } from '../../theme/packArt'`; `{ FeaturedCard, type FeaturedCardProps } from './FeaturedCard'`; `{ ceremonyStyles } from './ceremonyStyles'`; `{ CEREMONY_COPY_V10 } from '../../features/gacha/draw/ceremonyCopy'`; types from `ceremonyTimings`/`spillSchedule`/`packArt`. **No** `./reanimatedGuard`, no `StageCanvas`, no `TapCard` (the screen mounts the cards).
Render (root `View testID={FALLBACK_STAGE_TESTID} style={ceremonyStyles.fallbackStage}`):
- `phase === 'hold'` → `<View testID="draw-ceremony-hold-marker" style={ceremonyStyles.holdMarker} />`.
- Pack node while `phase` ∈ {`swipe`, `approach`, `hold`, `tear-flip`}: a `View` with `accessible accessibilityRole="button" accessibilityLabel={PACK_A11Y_LABEL} accessibilityHint={PACK_A11Y_HINT} accessibilityActions={[{ name: PACK_ACTIVATE_ACTION, label: CEREMONY_COPY_V10.activateAction }]} onAccessibilityAction={(e) => { if (!disabled && e.nativeEvent.actionName === PACK_ACTIVATE_ACTION) onTear(); }} accessibilityState={{ disabled }}` and **`testID={phase === 'swipe' ? 'draw-ceremony-swipe-pack' : isMulti ? 'draw-ceremony-multi-flyin' : 'draw-ceremony-single-pack-flyin'}`** (B00 §2.9's rule, shared with `PackTear`). Style: `ceremonyStyles.swipePack` in swipe, `ceremonyStyles.stageCard` otherwise, plus `phase === 'tear-flip' && !isMulti && ceremonyStyles.stageCardTear` (`:1329`), plus `{ transform: [{ translateX: swipeProgress * 24 }] }` in swipe (the pack follows the finger — the slider is gone), `{ borderColor: tellVisible ? rarityAccentColor(peakRarity) : TELL_COLORS.NEUTRAL }`, and `{ opacity: 1 }` (fallback transitions are plain state changes; no `Animated`). Inside: `LinearGradient colors={palette.cover} start={{ x: 0.1, y: 0 }} end={{ x: 0.9, y: 1 }} style={[ceremonyStyles.swipePackInner, { borderColor: palette.ring }]}` with the cover `RNImage` (`:1060-1067` verbatim) and, when `pitySeal`, a small `View style={ceremonyStyles.pitySeal}` (a 28×28 gold disc, top-right). `isMulti` adds the two offset `View`s of `ceremonyStyles.deckEdge` behind the pack (the "deck thickness" tell, M0 `:110`).
- `phase === 'tear-flip' && isMulti && spill` → for each `entry` of `spill.entries` (index `< cards.length`): `const o = spillSlotOffset(entry.slot, cards.length); const size = tableSlotLayout(cards.length);` `<View key testID={`draw-ceremony-spill-card-${entry.index}`} style={[ceremonyStyles.spillCard, { width: size.width, height: size.height, borderColor: tellVisible ? rarityAccentColor(cards[entry.index].rarity) : TELL_COLORS.NEUTRAL, transform: [{ translateX: o.x }, { translateY: o.y }, { rotate: `${o.rot}deg` }] }]}>` with the 9-slice glow behind it when `glowNineSlice && RNImage` (`<RNImage source={glowNineSlice} style={ceremonyStyles.fallbackRim} tintColor={…accent…} resizeMode="stretch" />`). Exactly `cards.length` views, never padded (fixes `:826-836`).
- `featured` non-null → `<FeaturedCard {...featured} />`.

### 4. `mobile/src/components/ceremony/ceremonyStyles.ts` — append after `tapCardShadow`

Copy from `CeremonyLottie.tsx` (comments trimmed to one line each; **any `shadowRadius` ≥ 16 becomes 8**): `safeArea` (`:549`), `gradient` (`:550`), `backdrop` (`:551-554`), `content` (`:589-594`), `stage` (`:615-621`), `swipePack` (`:643-648`), `swipePackInner` (`:649-663`, shadowRadius 22 → 8), `holdMarker` (`:697-702`), `stageCard` (`:704-720`, 18 → 8), `stageCardBack` (`:721-726`), `stageCardTear` (`:727-729`), `flipWrap` (`:754-757`), `flipFace` (`:758-765`), `flipBackGradient` (`:766-771`), `flipFront` (`:772-777`), `flipCard` (`:778-793`, 18 → 8), `flipCardRevealed` (`:794-802`, 22 → 8), `orbitStage` (`:803-808`), `cardBackText` (`:897-901`), `cardRarityChip` (`:902-907`), `cardRarity` (`:908-913`), `cardQuestion` (`:914-921`), `footerRarity` (`:922-929`), `footerRarityHidden` (`:930`), `phaseCopyHidden` (`:932`), `skipButton` (`:933-949`), `skipText` (`:950-954`), `flash` (`:955-960`), `ceremonySkipX` (`:975-991`), `ceremonySkipXText` (`:992-997`). Add the theme imports those entries need (`a11y`, `spacing`, `typography` from `../../theme/*`, next to B08's `colors`). New entries:
```ts
flashHiddenBehindCanvas: { opacity: 0 },                                  // skia renderer: the Canvas bloom is the flash
leaveButton: { position: 'absolute', top: 8, left: 8, width: 1, height: 1, opacity: 0 },   // VoiceOver-only; never accessibilityElementsHidden
fallbackStage: { ...StyleSheet.absoluteFillObject, alignItems: 'center', justifyContent: 'center' },
fallbackRim: { position: 'absolute', left: -14, top: -14, right: -14, bottom: -14, opacity: 0.55 },
spillCard: { position: 'absolute', borderRadius: 10, borderWidth: 2, backgroundColor: '#10143A' },
deckEdge: { position: 'absolute', width: 240, minHeight: 336, borderRadius: 22, borderWidth: 1, borderColor: 'rgba(255,255,255,0.35)', backgroundColor: 'rgba(16,20,58,0.35)' },
pitySeal: { position: 'absolute', top: 10, right: 10, width: 28, height: 28, borderRadius: 14, backgroundColor: '#F5C95E', borderWidth: 2, borderColor: '#FFF7EC' },
tapTableFrom: { width: '100%', minHeight: 240, alignItems: 'center', justifyContent: 'center', paddingHorizontal: 4 },   // the table container before 'cards-on-table' (no testID yet)
spillSampler: { position: 'absolute', width: 1, height: 1, opacity: 0 },   // SpillSampler root: an invisible, non-interactive sampling leaf (VoiceOver still reads its live region)
```

### 5. `mobile/src/screens/DrawCeremonyScreen.tsx` — the rewrite

**a. Imports** (every module by these exact names):
```ts
import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { AccessibilityInfo, Pressable, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { LinearGradient } from 'expo-linear-gradient';
import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import type { RootStackParamList } from '../navigation/types';
import { CEREMONY_COPY_V10, CEREMONY_COPY_V9, getCeremonyPhaseCopy } from '../features/gacha/draw/ceremonyCopy';
import { GLOW_9SLICE, PAGE_GRADIENT_CEREMONY, PARTICLE_SHEET, cardBackImageForSlug, cardFrameForRarity, packImageForSlug, packPaletteFromSlug, rarityAccentColor } from '../theme/packArt';
import { useFeatureFlags } from '../config/featureFlags';
import { CEREMONY_GAIN, useCeremonyAudio } from '../components/ceremonyAudio';
import { useCeremonyHaptics } from '../components/ceremonyHaptics';
import { GestureHandler, Reanimated, motionAvailable, skiaAvailable } from '../components/ceremony/reanimatedGuard';
import { FAST_FORWARD_FROM_HOLD_FRACTION, FAST_FORWARD_TEAR_FACTOR, REDUCED_MOTION_FLASH_MS, REDUCED_MOTION_SETTLE_MS, SWIPE_TRIGGER_DISTANCE, compressTimings, phaseDurations, resolveCeremonyTimings, type CeremonyPhase, type PeakRarity, type ResolvedCeremonyTimings } from '../features/gacha/draw/ceremonyTimings';
import { buildSpillSchedule, featuredCardIndex } from '../features/gacha/draw/spillSchedule';
import { skipPolicy } from '../features/gacha/draw/skipPolicy';
import { effectiveCeremoniesCompleted, getCeremonyDevOverrides, markCeremonyCompleted, readCeremoniesCompleted } from '../features/gacha/draw/ceremonyPrefs';
import { tableSlotLayout, useCeremonyTimeline } from '../components/ceremony/useCeremonyTimeline';
import { STAGE_TESTID, StageCanvas } from '../components/ceremony/StageCanvas';
import { PackTear, seamProgressFromDelta } from '../components/ceremony/PackTear';
import { TapCard, type TapCardData } from '../components/ceremony/TapCard';
import { FallbackStage } from '../components/ceremony/FallbackStage';
import { SpillSampler } from '../components/ceremony/SpillSampler';
import { ceremonyStyles as styles } from '../components/ceremony/ceremonyStyles';
```
`readRN` is no longer needed in the screen (no `Image` here); `CeremonyPhase`/`PeakRarity` come from B02 — delete the local copies (`:57-65`) and the constants (`:67-69`). `StyleSheet` is not needed (delete from the import).

**b. Derived facts** (top of the component, in this order): `drawResult` (`:391-398` kept); `cards = drawResult.cards`; `isMulti = cards.length > 1`; `peakRarity = resolvePeakRarity(drawResult)`; `featuredIdx = featuredCardIndex(cards)`; `featured = featuredIdx >= 0 ? cards[featuredIdx] : null`; `palette`/`coverImage`/`cardBackImage` (`:461-464` kept); `accent = rarityAccentColor(peakRarity)`; `pitySeal = route.params.pityCardIndex != null` (B10's param; the seal mark on the pack); `flags = useFeatureFlags()`; `dev = getCeremonyDevOverrides()`; `renderer` and `enableTapFlow` **exactly** as B00 §3.5:
```ts
const renderer: 'skia' | 'fallback' =
  motionAvailable && skiaAvailable && flags.ceremony.seamOfLight && !flags.ceremony.forceFallback && !dev.forceFallback
    ? 'skia' : 'fallback';
const timings = useMemo(() => resolveCeremonyTimings({ isMulti, peakRarity, motionAvailable }), [isMulti, peakRarity]);
const enableTapFlow = route.params.tapFlow ?? (motionAvailable && drawResult.cards.length > 0);
```
`stageWidth = 280; stageHeight = 360` (`styles.stage`, `:617-618`); `slot = tableSlotLayout(cards.length)` (card size + rows for the table and the spill landing — the same numbers B07 uses, so a dealt card lands where its `TapCard` sits).

**c. State / refs**: `phase` (`'swipe'`), `canSkip`, `reduceMotion`, `swipeProgress`, `sequenceToken`, `flippedSet: Set<string>`, `focusedUid: string | null`, `ceremoniesCompleted` (0), `tellLanded` (false), `compressed` (false), `activeTimings: ResolvedCeremonyTimings` (initialised to `timings`; reset whenever `timings` changes). Refs: `timers: number[]`, `swipeStartXRef`, `phaseRef` (mirrors `phase` via a tiny effect), `flippedRef` (mirrors `flippedSet`), `phaseStartedAtRef` (`Date.now()` written by `setPhaseAt`, the single helper every timer callback uses: `const setPhaseAt = (p) => { phaseStartedAtRef.current = Date.now(); setPhase(p); }`).

**d. Timeline + schedule**: `spill = useMemo(() => (isMulti && cards.length > 0 ? buildSpillSchedule(cards, activeTimings.tearFlip) : null), [cards, isMulti, activeTimings.tearFlip])`; `timeline = useCeremonyTimeline({ phase, peakRarity, isMulti, cardCount: cards.length, timings: activeTimings, spill, reduceMotion, compressed })`; `durations = useMemo(() => phaseDurations(activeTimings), [activeTimings])`. **Camera** (the only consumer of `cameraScale`/`cameraRot`, B00 §9 #17 — the S4 1.04 punch and the LEG decaying rock of §2 `:68` reach the screen through this and nothing else): `const cameraStyle = Reanimated.useAnimatedStyle(() => ({ transform: [{ scale: timeline.cameraScale.value }, { rotate: `${timeline.cameraRot.value}deg` }] }), [timeline]);` — `cameraRot` is in degrees (B00 §2.10 "Units"); under the fallback this is a plain object with `scale: 1` / `rotate: '0deg'`; under RM the hook keeps both at rest, so nothing else is needed. This is the one `Reanimated.*` hook the screen calls directly.

**e. `goResult`** (`useCallback`, deps: `navigation`, `drawResult`, `peakRarity`, the four route params — **not** `flippedSet`/`phase`, read through the refs so the effect below never restarts on a flip):
```ts
navigation.replace('DrawResult', {
  slug: route.params.slug, drawResult, deckTitle: route.params.deckTitle, ownedAfter: route.params.ownedAfter, totalCards: route.params.totalCards,
  revealedUids: [...flippedRef.current],
  ceremonyEcho: { rarity: peakRarity, phaseCue: CEREMONY_COPY_V9.settle.body, tableReached: phaseRef.current === 'cards-on-table' },
});
```
`goResultRef.current = goResult` (mirror), and the sequence effect calls `goResultRef.current()` so `goResult` is not a dependency.

**f. `startSequence`** (`:492-495` kept) + `haptics.tick()` (selection at threshold, S0). **`onTear`** = `startSequence` (from `PackTear`, `FallbackStage`'s activate action and the stage responder alike — B00 §9 #11).

**g. The sequence effect** — `useEffect(() => {...}, [sequenceToken, reduceMotion, timings, enableTapFlow])`. Body, in order:
1. Clear `timers.current`; `setCanSkip(false); setTellLanded(false); setCompressed(false); setActiveTimings(timings); setFocusedUid(null);`.
2. **Reduce Motion** (`reduceMotion === true`): `setPhaseAt('flash-reveal')`; `settle` at `REDUCED_MOTION_FLASH_MS` (`setPhaseAt('settle'); setCanSkip(true)`); at `REDUCED_MOTION_FLASH_MS + REDUCED_MOTION_SETTLE_MS`: `enableTapFlow ? (setFlippedSet(new Set()), setPhaseAt('cards-on-table'), setCanSkip(true)) : goResultRef.current()`. Return the cleanup. (Cadence byte-identical to `:714-728` and test `:364-403`, `:484-516`.)
3. `sequenceToken === 0` → `setPhaseAt('swipe')`; return cleanup.
4. Otherwise `setPhaseAt('approach')` and `scheduleFrom('approach', timings, 0)` where `scheduleFrom(startPhase, t, elapsedInStartMs)` is a module-level pure helper returning `{ at: number; phase: CeremonyPhase | 'tail' }[]` that the effect turns into timers:
   - `d = { approach: t.approach, hold: t.hold, 'tear-flip': t.tearFlip, 'flash-reveal': t.flashReveal, settle: t.settleMs }`; `order = ['approach', 'hold', 'tear-flip', 'flash-reveal', 'settle']`; `at = Math.max(0, d[startPhase] − elapsedInStartMs)`; for each phase `p` after `startPhase` in `order`: emit `{ at, phase: p }`, `at += d[p]`; finally emit `{ at, phase: 'tail' }` where the tail is at `settle + settleMs + tableTailMs`.
   - Timer bodies: `'settle'` → `setPhaseAt('settle'); setCanSkip(true)`; `'tail'` → `enableTapFlow ? (setFlippedSet(new Set()), setPhaseAt('cards-on-table'), setCanSkip(true)) : goResultRef.current()`; others → `setPhaseAt(p)`.
   - Only when `startPhase === 'approach'`: `tellTimer` at `t.approach + Math.round(t.hold × FAST_FORWARD_FROM_HOLD_FRACTION)` → `setTellLanded(true)`; when `t.beatMs > 0`, `beatTimer` at `t.approach + t.hold − t.beatMs` → `audio.duck(CEREMONY_GAIN.duck, 80)` (silence beat, §2 `:66`; `CEREMONY_GAIN` is B04's export).
   TEST_BASE multi check: hold 620, tear 920, flash 1860, settle 2140, tail 2940 (test `:162-225` incl. `advanceTimersByTime(820)` after settle); single: 300 / 480 / 840 / 1060 / 1760.
5. Cleanup: clear every timer.

**h. `compress()`** (`useCallback`): only when `skipDecision === 'compress'` (below). `elapsed = Date.now() − phaseStartedAtRef.current`; `c = compressTimings(timings, phase === 'hold' ? elapsed : timings.hold)`; clear the pending phase timers (not the whole `timers` array's cleanup — clear and rebuild): from `phase`: `'hold'` → `scheduleFrom('hold', c, 0)` (`c.hold` is the remaining beat); `'tear-flip'` → `scheduleFrom('tear-flip', c, 0)` but with the first entry's `at` replaced by `Math.max(0, Math.round((timings.tearFlip − elapsed) / FAST_FORWARD_TEAR_FACTOR))`; `'flash-reveal'` → `scheduleFrom('flash-reveal', c, elapsed)`. Then `setCompressed(true); setActiveTimings(c); audio.duck(0, 60); haptics.tick();`. **Never** `goResult`. TEST_BASE multi LEG, compress at hold+180: tear-flip at +0, flash at +588, settle at +868 → CTA at 1668 ms (< 2440).

**i. Skip decision** (render-time, pure): `skipDecision = skipPolicy({ ceremoniesCompleted, phase, phaseElapsedMs: phase === 'hold' ? (tellLanded ? durations.hold : 0) : 0, phaseDurationMs: durations[phase], reduceMotion, alreadyCompressed: compressed })`. `ceremoniesCompleted` is loaded once on mount: `readCeremoniesCompleted().then((n) => { if (mounted) setCeremoniesCompleted(effectiveCeremoniesCompleted(n)); }).catch(() => {})` (fail-closed 0 = first ceremony).

**j. Cues** (`useEffect` on `[phase]`, plus a mount effect; every call is fire-and-forget through the B04 controllers — `const audio = useCeremonyAudio(); const haptics = useCeremonyHaptics();`):

| when | audio | haptics |
|---|---|---|
| mount / `reduceMotion` change | RM: `tail('soft-chime')`; else nothing | `reset({ reduceMotion })`; RM: `impact('light')` once |
| `startSequence` (tear commit) | `bed('crinkle')` was started on responder grant (S0); on commit `hit('whoosh')`, `bed('air')` | `tick()` |
| `hold` | `bed(peakRarity === 'LEG' ? 'choir-swell' : peakRarity === 'RAR' ? 'shimmer-pad' : 'air', { gain: CEREMONY_GAIN.bed[peakRarity] })`; beat duck by timer (g.4) | `tellTimer` also fires `impact(peakRarity === 'LEG' ? 'heavy' : 'medium')` for RAR/LEG only |
| `tear-flip` | `hit('rip')`; multi: `hit('stack-thud')` at `+Math.round(tearFlip × 0.5)` (a timer pushed to `timers`) | `impact('light')` |
| `flash-reveal` | `hit('seam-burst')`; `peakRarity === 'LEG' ? hit('stinger') : peakRarity === 'RAR' ? hit('chime') : null` | `impact(peakRarity === 'LEG' ? 'heavy' : peakRarity === 'RAR' ? 'medium' : 'light')`; LEG: `success()` |
| `settle` | RAR/LEG: `tail('sparkle-tail')`; `bed(bedName, { gain: CEREMONY_GAIN.bedTable })` | none |
| `cards-on-table` tap (`onTapStart`) | `hit('card-flip')`; RAR `hit('chime')`; LEG `hit('legendary')` | `impact('soft')` |
| unmount | `stopAll()` | — |

**k. Table handlers**: `onTapStart(card)` as the row above; `onFlipped(uid)` → `setFlippedSet((s) => new Set(s).add(uid))`; `onFocusToggle(uid)` → `setFocusedUid((cur) => (cur === uid ? null : uid))`; `onContinue` → `markCeremonyCompleted().catch(() => {}); goResult();`.

**l. JSX** (stage 280×360; the tree below is the contract — keep the order):
```
SafeAreaView testID="screen-draw-ceremony-root" style={styles.safeArea}
  LinearGradient colors={PAGE_GRADIENT_CEREMONY} style={styles.gradient}
    View testID="draw-ceremony-backdrop" style={styles.backdrop}
    Pressable testID="draw-ceremony-leave" accessibilityRole="button" accessibilityLabel={CEREMONY_COPY_V10.leaveCeremony} onPress={goResult} style={styles.leaveButton}   ← always mounted, opacity 0, NO accessibilityElementsHidden / importantForAccessibility
    {skipDecision === 'compress' ? Pressable testID="draw-ceremony-fast-forward" accessibilityRole="button" accessibilityLabel={CEREMONY_COPY_V10.speedUp} onPress={compress} hitSlop={12} style={styles.ceremonySkipX} > Text style={styles.ceremonySkipXText} "×" : null}
    Reanimated.View style={[styles.content, cameraStyle]}   ← the camera wrapper (5d); a plain View under the fallback
      Text testID="draw-ceremony-phase-copy" style={styles.phaseCopyHidden} numberOfLines={1} accessibilityLiveRegion="polite"   → phase === 'approach' ? CEREMONY_COPY_V9.approach.rareTitles[peakRarity] : phaseCopy.title
      Text testID="draw-ceremony-phase-body-copy" style={styles.phaseCopyHidden} numberOfLines={1}   → phaseCopy.body
      View testID="draw-ceremony-stage" style={styles.stage} + the six responder props (below)
        {renderer === 'skia' ? <StageCanvas testID={STAGE_TESTID} width={280} height={360} peakRarity={peakRarity} timeline={timeline} reduceMotion={reduceMotion} particleSheet={PARTICLE_SHEET} glowNineSlice={GLOW_9SLICE} cardCount={cards.length} /> : null}
        {renderer === 'skia' && packPhase ? <PackTear width={240} height={336} coverImage={coverImage} palette={palette} phase={phase} isMulti={isMulti} pitySeal={pitySeal} timeline={timeline} disabled={reduceMotion || phase !== 'swipe'} onTear={startSequence} onSeamProgress={setSwipeProgress} /> : null}
        {renderer === 'skia' && phase === 'hold' ? <View testID="draw-ceremony-hold-marker" style={styles.holdMarker} /> : null}
        {renderer === 'fallback' ? <FallbackStage phase={phase} isMulti={isMulti} reduceMotion={reduceMotion} peakRarity={peakRarity} palette={palette} coverImage={coverImage} pitySeal={pitySeal} disabled={reduceMotion || phase !== 'swipe'} onTear={startSequence} swipeProgress={swipeProgress} cards={cards} spill={spill} glowNineSlice={GLOW_9SLICE} tellVisible={phase !== 'swipe' && phase !== 'approach'} featured={showFeatured ? featuredProps : null} /> : null}
        {renderer === 'skia' && showFeatured ? <FeaturedCard {...featuredProps} /> : null}
        {phase === 'tear-flip' && isMulti ? <SpillSampler durationMs={activeTimings.tearFlip} renderer={renderer} /> : null}   ← an invisible 1×1 absolute leaf (change 1), so it never covers the stage
        {enableTapFlow && tablePhase ? <View testID={phase === 'cards-on-table' ? 'draw-ceremony-cards-on-table' : undefined} style={phase === 'cards-on-table' ? styles.tapTable : styles.tapTableFrom}> rows of <TapCard …/> </View> : null}
      Text testID="draw-ceremony-footer-rarity" style={[styles.footerRarity, !rarityVisible && styles.footerRarityHidden]} numberOfLines={1}   → rarityVisible ? rarityWord : ''
      {canSkip ? Pressable testID="screen-draw-ceremony-primary-cta" nativeID="draw-ceremony-skip-hint" accessibilityRole="button" style={…skipButton…} onPress={ctaPress} > Text style={styles.skipText} numberOfLines={1} {ctaText} : null}
    View testID="draw-ceremony-reveal-flash" pointerEvents="none" style={[styles.flash, { backgroundColor: flashColor(peakRarity), opacity: phase === 'flash-reveal' && !reduceMotion ? 0.85 : 0 }, renderer === 'skia' ? styles.flashHiddenBehindCanvas : null]}
```
where `packPhase = phase === 'swipe' || phase === 'approach' || phase === 'hold' || phase === 'tear-flip'`; `tablePhase = phase === 'flash-reveal' || phase === 'settle' || phase === 'cards-on-table'`; `showFeatured = !enableTapFlow && (phase === 'flash-reveal' || phase === 'settle')`; `featuredProps = { accent, rarityText: featured?.rarity ?? peakRarity, questionText: featured?.question ?? 'Cards revealed', packPaletteCover: palette.cover, coverImage, faceUp: true }` (today's `:821-822`, `:1260-1269`); `rarityVisible = phase === 'cards-on-table' ? flippedSet.size > 0 : phase === 'settle'` and `rarityWord = peakRarity === 'LEG' ? 'Legendary' : peakRarity === 'RAR' ? 'Rare' : 'Common'` (B00 §3.6 — the word is **not** in the tree before then; the hidden footer renders `''`); `ctaText = phase === 'cards-on-table' ? (allFlipped ? CEREMONY_COPY_V10.continueCta : CEREMONY_COPY_V10.skipProgress(flippedSet.size, cards.length)) : CEREMONY_COPY_V10.showResult` with `allFlipped = cards.length > 0 && flippedSet.size >= cards.length`; `ctaPress = phase === 'cards-on-table' && allFlipped ? onContinue : goResult`. The table rows reuse today's split (`:1202-1256`: `≤ 5` one `styles.tapRow`, else `styles.tapTwoRows` of two rows split at `Math.ceil(n / 2)`), each `<TapCard key={card.stableUid} card={card} index={i} total={cards.length} width={slot.width} height={slot.height} disabled={phase !== 'cards-on-table'} flipped={flippedSet.has(card.stableUid)} onTapStart={onTapStart} onFlipped={onFlipped} cardBackImage={cardBackImage} frameImage={cardFrameForRarity(card.rarity)} reduceMotion={reduceMotion} focused={focusedUid === card.stableUid} onFocusToggle={onFocusToggle} timings={activeTimings} />` — `index`/`total` are the global index and total (not per-row as today `:1232-1233`, so `tap-card-${i}` is unique and the a11y label counts the whole pull).
The stage responder (kept from `:990-1020`, extended): `stageOwnsSwipe = phase === 'swipe' && !reduceMotion && !(renderer === 'skia' && GestureHandler.available)` (the GH Pan in `PackTear` owns the device swipe; the raw responder is the test/fallback path — B00 §9 #11); `onStartShouldSetResponder={() => stageOwnsSwipe || skipDecision === 'compress'}`, same for `onMoveShouldSetResponder`; `onResponderGrant`: if `stageOwnsSwipe` → record `pageX`, `audio.bed('crinkle')`, `haptics.impact('light')`; `onResponderMove`: `p = seamProgressFromDelta(delta); setSwipeProgress(p); timeline.seam.value = p;`; `onResponderRelease`: if `stageOwnsSwipe` and `delta >= SWIPE_TRIGGER_DISTANCE` → `startSequence()` (do **not** force `timeline.seam.value = 1` here: the finger already drove it, and from `approach` the hook owns the cut — it relaxes `seam` to `SEAM_PRECUT` and sweeps it to 1 in tear-flip, B00 §9 #14); else if `stageOwnsSwipe` → `setSwipeProgress(0); timeline.seam.value = 0; audio.bed(null)`; else if `skipDecision === 'compress'` → `compress()`; `onResponderTerminate` as today. `armCeremonySwipe` (test `:114-121`: grant 16 → move 104 → release 104, delta 88 ≥ 72) must still start the sequence.
`AccessibilityInfo.announceForAccessibility?.(title)` in a `useEffect` on the title (VoiceOver phase announcements, DoD `:185`).

### 6. What is gone from the screen (verify greps): `lottie`/`Lottie`/`CeremonyLottie`, `HolographicLayer`, `SparkleField`, `ParticleBurst`, `MultiPackFlyIn`, `buildParticles`, `RevealCard`, `onAnimationFinish`, `orbitTimerRef`, `setInterval(`, `hasAnimated`, `TIMING_SCALE`, `readRN(`, `Animated`, `A.Value`, `rarityHaloColor`, `'Skip ceremony'`, `Swipe right to rip open`, `swipeTrack`, `flashCore`, `draw-ceremony-orbit-center`, `function phaseDurations(`, `const SWIPE_TRIGGER_DISTANCE`, `const REDUCED_MOTION_`.

### 7. `mobile/tests/integration/draw-ceremony.screen.test.tsx`

**Prefix (byte-exact).** Starting from the base file (787 lines), apply exactly, with the original line numbers: delete `518-635` (the two RM/early-finish Lottie cases), delete `637-652` (the `vi.resetModules()`/`vi.doMock`/dynamic-import scaffold of the retained multi case), delete `730-732` (its `vi.doUnmock`/`vi.resetModules` tail), delete `734-787` (blank + the single-draw Lottie case + the final `});`); line 636 title `keeps normal-motion lottie multi choreography and cadence parity` → `keeps normal-motion multi choreography and cadence parity`; line 657 `<DrawCeremonyScreenWithLottie` → `<DrawCeremonyScreen`; line 680 `.toBe('lottie')` → `.toBe('fallback')`; lines 152/166/667 `'Legendary inbound'` → `'Pack inbound'`, 243/258 `'Rare inbound'` → `'Pack inbound'`, 197/204/288/378/384 `'Card revealed'` → `'Pack open'` (no-ops if B10 already applied them). Result: 596 lines, nine `it(` cases, ending with the retained case's `  });`. The verify script runs this exact `sed` and `cmp`s it against `head -n 596` of your file.

**Then append**, after a blank line, the eight cases (titles verbatim; each begins with a one-line comment naming the grammar rule it encodes; use the file's own helpers; every `renderer.create` inside `await act(async () => { …; await Promise.resolve(); })` as the retained cases do) and finish the file with `});`:
1. `'reaches the tap-to-flip table by timer when tapFlow is on and flips only on the table'` — multi, `params: { slug: 'csharp', drawResult: MULTI_DRAW_RESULT, tapFlow: true }`; `armCeremonySwipe`; advance `620 + 300 + 940 + 1` (flash-reveal): `findByProps({ testID: 'tap-card-0' }).props.onPress()` inside `act` → CTA absent and `tap-card-0`'s `accessibilityLabel` is `'Card 1 of 2, face down'`; advance `280` → CTA text `'Show result'`; advance `300 + 500` → `draw-ceremony-cards-on-table` present, CTA `'Skip · 0/2'`; press `tap-card-0` → `'Skip · 1/2'`; press `tap-card-1` → `'Continue'`; press the CTA → `replace` called with `('DrawResult', expect.objectContaining({ revealedUids: ['1', '2'], ceremonyEcho: expect.objectContaining({ tableReached: true }) }))`.
2. `'keeps the rarity word out of every text before a card is face up'` — single RAR (`SINGLE_DRAW_RESULT`) with `tapFlow: true`; at swipe, after `armCeremonySwipe` (approach), at `300` (hold), `+180` (tear-flip), `+360` (flash-reveal), `+220` (settle): `collectText(tree)` contains neither `'Rare'` nor `'Legendary'`; advance `200 + 500` to the table, press `tap-card-0` → `collectText(tree)` contains `'Rare'`.
3. `'reduced motion with tapFlow keeps the reveal on the table with no flash'` — `__setReduceMotionEnabled(true)`, multi with `tapFlow: true`: t=0 `phaseTitle` contains `'Pack open'` and `draw-ceremony-reveal-flash`'s `props.style[1].opacity === 0`; advance `180` → CTA `'Show result'`; advance `240` → `draw-ceremony-cards-on-table`, CTA `'Skip · 0/2'`, flash opacity still `0`, `replace` not called; press `tap-card-0` → `'Skip · 1/2'`.
4. `'exposes the pack as an accessible button whose activate action starts the ceremony'` — multi, no swipe: `const pack = tree.root.findByProps({ accessibilityLabel: 'Reward pack' })`; `pack.props.accessibilityRole === 'button'`; `pack.props.accessibilityActions` contains `expect.objectContaining({ name: 'activate' })`; `act(() => pack.props.onAccessibilityAction({ nativeEvent: { actionName: 'activate' } }))` → `phaseTitle` contains `'Pack inbound'`.
5. `'renders the fallback stage when motion is unavailable and still reaches settle'` — single: `draw-ceremony-fallback-stage` present, `findAllByProps({ testID: 'draw-ceremony-stage-canvas' })` length 0; `armCeremonySwipe`; advance `300 + 180 + 360 + 220` → `phaseTitle` contains `'Cards in place'`.
6. `'spills exactly as many cards as were drawn'` — multi (2 cards); `armCeremonySwipe`; advance `980` → `findAllByProps({ testID: 'draw-ceremony-spill-card-0' })` and `-1` each length ≥ 1, `-2` length 0.
7. `'lets a repeat user compress from hold without leaving the ceremony'` — `vi.resetModules(); vi.doMock('../../src/features/gacha/draw/ceremonyPrefs', async () => ({ ...(await vi.importActual<any>('../../src/features/gacha/draw/ceremonyPrefs')), readCeremoniesCompleted: async () => 1 }));` then `const { DrawCeremonyScreen: Screen } = await import('../../src/screens/DrawCeremonyScreen');` (the scaffold shape of the deleted `:519-533`); multi; `armCeremonySwipe`; advance `620 + 179` → `findAllByProps({ testID: 'draw-ceremony-fast-forward' })` length 0; advance `1` → length 1; press it inside `act`; advance `0 + 588 + 280` → CTA present and `replace` not called; `vi.doUnmock(...)`; `vi.resetModules()` at the end (as the deleted cases did at `:577-578`).
8. `'never shows a fast-forward control on a first-ever ceremony before settle'` — multi, default prefs; at swipe, approach, `620` (hold), `+300` (tear-flip), `+940` (flash-reveal): `draw-ceremony-fast-forward` absent and `findByProps({ accessibilityLabel: 'Leave ceremony' })` exists (unique) at each step.

Estimated size: screen ≈ 520 LOC (from 1418), FallbackStage ≈ 150, SpillSampler ≈ 50, FeaturedCard ≈ 70, styles +≈ 140, test +≈ 300.

## Acceptance

Run from `mobile/`. `docs/delivery/r16-issues/B09.verify.sh` runs exactly these from the worktree root.

0. Deps merged: `grep -q 'export const CEREMONY_COPY_V10' src/features/gacha/draw/ceremonyCopy.ts` (B10) and `grep -q 'GLOW_9SLICE' src/theme/packArt.ts` (B12) and `[ -f src/components/ceremony/useCeremonyTimeline.ts ]` (B07) and `[ -f src/components/ceremony/TapCard.tsx ]` (B08) and `[ -f src/components/ceremony/StageCanvas.tsx ]` (B05) and `[ -f src/components/ceremony/PackTear.tsx ]` (B06) — otherwise the verify stops with "deps not merged" before anything else.
1. Files exist: `src/components/ceremony/FallbackStage.tsx`, `src/components/ceremony/SpillSampler.tsx`, `src/components/ceremony/FeaturedCard.tsx`.
2. Test prefix: `git show "$BASE_REF":mobile/tests/integration/draw-ceremony.screen.test.tsx` has 787 lines; piped through the change-7 `sed` it yields 596 lines, and `head -n 596 tests/integration/draw-ceremony.screen.test.tsx | cmp - <that>` is silent. The file's last line is `});`; it has ≥ 17 `it(` blocks; the eight new titles are present verbatim; it contains none of `CeremonyLottie`, `mock-ceremony-lottie`, `'Legendary inbound'`, `'Rare inbound'`, `'Card revealed'`, `DrawCeremonyScreenWithLottie`.
3. `npx vitest run tests/integration/draw-ceremony.screen.test.tsx tests/integration/draw-result.screen.test.tsx tests/unit/ceremonyTimings.test.ts tests/unit/useCeremonyTimeline.test.ts tests/unit/tapCard.test.tsx tests/unit/stageCanvas.test.tsx tests/unit/packTear.test.tsx tests/unit/skipPolicy.test.ts --reporter=dot` exits 0.
4. `npm run test:typecheck` exits 0.
5. Screen positive guards (fixed-string): the import lines of change 5a for `'../components/ceremony/StageCanvas'`, `'../components/ceremony/PackTear'`, `'../components/ceremony/TapCard'`, `'../components/ceremony/FallbackStage'`, `'../components/ceremony/SpillSampler'`, `'../components/ceremony/useCeremonyTimeline'`, `'../components/ceremony/reanimatedGuard'`, `'../components/ceremony/ceremonyStyles'`, `'../features/gacha/draw/ceremonyTimings'`, `'../features/gacha/draw/spillSchedule'`, `'../features/gacha/draw/skipPolicy'`, `'../features/gacha/draw/ceremonyPrefs'`, `'../config/featureFlags'`; `resolveCeremonyTimings({ isMulti, peakRarity, motionAvailable })` · `route.params.tapFlow ??` · `flags.ceremony.seamOfLight` · `flags.ceremony.forceFallback` · `dev.forceFallback` · `compressTimings(` · `skipPolicy(` · `readCeremoniesCompleted(` · `markCeremonyCompleted(` · `effectiveCeremoniesCompleted(` · `buildSpillSchedule(` · `featuredCardIndex(` · `useCeremonyTimeline(` · `tableSlotLayout(` · `seamProgressFromDelta(` · `REDUCED_MOTION_FLASH_MS` · `REDUCED_MOTION_SETTLE_MS` · `SWIPE_TRIGGER_DISTANCE` · `FAST_FORWARD_FROM_HOLD_FRACTION` · `FAST_FORWARD_TEAR_FACTOR` · `revealedUids:` · `tableReached:` · `CEREMONY_COPY_V10.leaveCeremony` · `CEREMONY_COPY_V10.speedUp` · `CEREMONY_COPY_V10.showResult` · `CEREMONY_COPY_V10.continueCta` · `CEREMONY_COPY_V10.skipProgress(` · `rareTitles[peakRarity]` · `STAGE_TESTID` · `<StageCanvas` · `<PackTear` · `<FallbackStage` · `<SpillSampler` · `<TapCard` · `<FeaturedCard` · `flashHiddenBehindCanvas` · `Reanimated.useAnimatedStyle(` · `<Reanimated.View` · `timeline.cameraScale.value` · `timeline.cameraRot.value` · `accessibilityLiveRegion="polite"` · `onPress={ctaPress}` · testIDs `draw-ceremony-leave`, `draw-ceremony-fast-forward`, `draw-ceremony-reveal-flash`, `draw-ceremony-cards-on-table`, `draw-ceremony-footer-rarity`, `screen-draw-ceremony-primary-cta`, `draw-ceremony-skip-hint`, `draw-ceremony-stage`, `draw-ceremony-phase-copy`, `draw-ceremony-phase-body-copy`, `draw-ceremony-backdrop`, `screen-draw-ceremony-root`, `draw-ceremony-hold-marker`; the six responder prop names; `flashColor(peakRarity)`; `phase === 'flash-reveal' && !reduceMotion ? 0.85 : 0`.
6. Screen negative guards (ERE, none may match): `[Ll]ottie|HolographicLayer|SparkleField|ParticleBurst|MultiPackFlyIn|buildParticles|RevealCard|onAnimationFinish|orbitTimerRef|setInterval\(|hasAnimated|TIMING_SCALE|readRN\(|\bAnimated\b|A\.Value|rarityHaloColor|Skip ceremony|Swipe right to rip open|swipeTrack|flashCore|draw-ceremony-orbit-center|function phaseDurations\(|const SWIPE_TRIGGER_DISTANCE|const REDUCED_MOTION_|accessibilityElementsHidden|from '@shopify/react-native-skia'|from 'react-native-reanimated'|<Canvas|Skia\.Canvas`; plus `grep -c "Date.now()"` ≤ 2.
7. Leaves: `SpillSampler.tsx` contains `export const SPILL_SAMPLE_MS = 100`, `setInterval(`, `React.memo(`, the five `draw-ceremony-orbit-*` testIDs, `accessibilityLiveRegion="polite"`, `pointerEvents="none"`, `ceremonyStyles.spillSampler`, `CEREMONY_COPY_V10.dealing(`, and not `orbitStage`; `FallbackStage.tsx` contains `export const FALLBACK_STAGE_TESTID = 'draw-ceremony-fallback-stage'`, `draw-ceremony-swipe-pack`, `draw-ceremony-multi-flyin`, `draw-ceremony-single-pack-flyin`, `draw-ceremony-spill-card-`, `draw-ceremony-hold-marker`, `PACK_A11Y_LABEL`, `PACK_A11Y_HINT`, `PACK_ACTIVATE_ACTION`, `onAccessibilityAction`, `spillSlotOffset(`, `tableSlotLayout(`, `<FeaturedCard`, and none of `reanimatedGuard|StageCanvas|TapCard|@shopify|react-native-reanimated|setInterval\(|setTimeout\(`; `FeaturedCard.tsx` contains `export function FeaturedCard(`, `draw-ceremony-reveal-rarity`, `draw-ceremony-reveal-question`, `rotateY: '180deg'`, `readRN(`, and no `Animated`. Across `DrawCeremonyScreen.tsx` + `mobile/src/components/ceremony/`, `grep -l "setInterval("` lists only `SpillSampler.tsx`.
8. Styles: `ceremonyStyles.ts` contains keys `safeArea`, `gradient`, `backdrop`, `content`, `stage`, `holdMarker`, `phaseCopyHidden`, `footerRarity`, `footerRarityHidden`, `skipButton`, `skipText`, `flash`, `flashHiddenBehindCanvas`, `ceremonySkipX`, `ceremonySkipXText`, `orbitStage`, `flipCard`, `flipCardRevealed`, `flipFront`, `flipBackGradient`, `cardRarityChip`, `cardRarity`, `cardQuestion`, `cardBackText`, `swipePack`, `swipePackInner`, `stageCard`, `stageCardBack`, `stageCardTear`, `leaveButton`, `spillCard`, `fallbackStage`, `fallbackRim`, `tapTableFrom`, `spillSampler` and still `tapTable`, `tapCardSlot`, `tapCardShadow`; `grep -E "shadowRadius: *(1[6-9]|[2-9][0-9])"` over `DrawCeremonyScreen.tsx` and `mobile/src/components/ceremony/` is empty.
9. Suppression grep over the six scope files empty; case-insensitive driver-gate mirror (`humanizer|bypass|undetect|detector|evade|Gemini said`) over the six scope files empty; `"vite": "7.2.4"` present; no `@sentry`.
10. Scope guard: only the six scope files (plus `docs/delivery/r16-issues/*`) differ from the merge-base; frozen files zero diff; `CeremonyLottie.tsx`, `HolographicLayer.tsx`, `ceremonyCopy.ts`, `types.ts` unchanged.

## Do NOT

- Do not schedule any transition from an animation callback, a gesture callback or a native event: `startSequence` bumps a token, timers do the rest. Do not call `goResult` from `compress()`, from a `TapCard` callback, from the fast-forward ×, or from any `SpillSampler` state.
- Do not add or rename phases; the beat, the hit-pause and the spill are `useCeremonyTimeline` sub-beats (B00 §3.1). Do not pass `phaseElapsedMs` from a clock read at render time — it comes from the `tellTimer` state.
- Do not edit any line of the 596-line test prefix beyond the listed transform; do not touch `MULTI_TIMING`/`SINGLE_TIMING`/`REDUCED_TIMING`; do not delete the retained multi-choreography case; do not add `vi.mock` of `CeremonyLottie` or of the guard; do not add `.skip`/`.only`.
- Do not import `CeremonyLottie.tsx`, `HolographicLayer.tsx`, `@shopify/react-native-skia`, `react-native-reanimated`, `react-native-gesture-handler` or `Image`/`Animated` from `react-native` in the screen or the three leaves; do not render a `Canvas` in any of them.
- Do not let the rarity word (`Legendary`/`Rare`/`Common`) or a rarity-tinted approach title into the tree before settle (tapFlow off) / the first flip (tapFlow on); do not set `accessibilityElementsHidden` on `draw-ceremony-leave`.
- Do not edit `ceremonyCopy.ts`, `types.ts`, `DrawResultScreen.tsx`, `DrawScreen.tsx`, `packArt.ts`, any B02–B08 module or any test other than `draw-ceremony.screen.test.tsx`; do not change anything above B08's `tapCardShadow` in `ceremonyStyles.ts`.
- **Tokens that must not appear in these files even inside a comment or a string** — the verify's negative greps are bare-token, not import-scoped, so "the static RevealCard" or "no TapCard here" in a header comment fails a correct implementation. Describe things as "the old Lottie tree", "the featured card", "the tap table", "the static reveal branch":
  - `DrawCeremonyScreen.tsx`: `Lottie`/`lottie`, `HolographicLayer`, `SparkleField`, `ParticleBurst`, `MultiPackFlyIn`, `buildParticles`, `RevealCard`, `onAnimationFinish`, `orbitTimerRef`, `setInterval(`, `hasAnimated`, `TIMING_SCALE`, `readRN(`, `Animated` as a bare word (`Reanimated.*`/`useAnimatedStyle` are fine — the grep is `\bAnimated\b`), `A.Value`, `rarityHaloColor`, `Skip ceremony`, `Swipe right to rip open`, `swipeTrack`, `flashCore`, `draw-ceremony-orbit-center`, `function phaseDurations(`, `const SWIPE_TRIGGER_DISTANCE`, `const REDUCED_MOTION_`, `accessibilityElementsHidden`, `<Canvas`, `Skia.Canvas`; at most two `Date.now()`.
  - `FallbackStage.tsx`: `reanimatedGuard`, `StageCanvas`, `TapCard`, `@shopify`, `react-native-reanimated`, `setInterval(`, `setTimeout(`.
  - `FeaturedCard.tsx`: `Animated` as a bare word, `reanimatedGuard`, `@shopify`.
  - `SpillSampler.tsx`: `orbitStage` (it uses `spillSampler`).
  - all six files: `humanizer`, `bypass`, `undetect`, `detector`, `evade`, `Gemini said` (case-insensitive — the driver's Gate 1). The gesture-handler library's host-component name contains one of these; if you ever need it, it is `GestureHandler.PanHost` (B00 §2.1) — the screen does not need it.
- Do not put `styles.orbitStage` (248×260) on the `SpillSampler` root and do not let it intercept touches: `spillSampler` + `pointerEvents="none"`.
- Do not force `timeline.seam.value = 1` in the responder's commit branch, and do not touch `timeline.seam` outside the `swipe` phase (B00 §9 #14).
- Do not add a second `Reanimated.*` hook or a second `Reanimated.View` to the screen: the camera wrapper is the single exception (B00 §9 #17); `TapCard`, `StageCanvas` and `PackTear` own their own motion.
- Standing rules: no `git push`, no PR, never target or touch `main`, no `npm install`/`npm ci`/`eas`/`expo` commands, no disabling/skipping/gutting tests, no `@ts-ignore`/`@ts-expect-error`/`eslint-disable`, no loosening of `tsconfig`.
