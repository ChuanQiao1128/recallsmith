# D04 — MCQ session components (`mcq-components`)

Three presentational components for the multiple-choice card and one storage helper: `McqReviewBody` (the three screens — stem, options, verdict — with letters assigned by displayed position, radio / checkbox rows, the verdict row states with their WHY expanders and ReviewBody-style sections), `McqActionDock` (Show options → Sure / Not sure / I don't know → Next / Finish run, rendered where `RatingBar` sits today), `McqCoachLine` (the one-time dismissible hint) and `mcqCoachPrefs` (the device-global "seen" flag, `ceremonyPrefs` pattern), plus four unit suites. Pure TS shipped as an OTA on runtime 1.6.0 — no dependency, manifest or native change. Nothing in this issue is mounted anywhere yet: D05 wires the components into `SessionCardScreen`; D04 only has to make them exist, render and behave under the contracts below, so that D05's integration suite can pass without touching them.

## Context

What the tree looks like today (`delivery/r16-d-mcq` == `main@107e2a2`, plus D01–D03 merged ahead of you in the queue; every line read on 2026-09-22):

- **What you consume (D01/D02, already on the branch when you start):** `mobile/src/types/deckExport.ts` — `McqOption { key; text; why; correct }` and `McqExport { v: 1; qualifier; shuffle; options }` (D00 §2.1). `mobile/src/features/gacha/mcq/normalizeMcq.ts` — `mcqRequiredCount(mcq)` (1..3). `mobile/src/features/gacha/mcq/mcqConstants.ts` — `MCQ_COPY` (every user-facing string below), `MCQ_TEST_IDS` (every pinned testID below), `mcqLetter(index)`, `mcqKindChip(n)`, `mcqSelectedCount(k, n)`, `mcqBannerPartial(k, n)`, `mcqQualifierBody(q)`, `mcqOptionA11yLabel(index, total, text)`, `MCQ_COACH_SEEN_KEY = 'recallsmith:mcq:coach-seen:v1'`, `MCQ_COACH_READ_TIMEOUT_MS = 250` (D00 §2.2). `mobile/src/features/gacha/mcq/mcqVerdict.ts` — `type McqVerdict = 'correct' | 'partial' | 'wrong'`, `type McqConfidence = 'sure' | 'unsure'`. You never re-declare a copy string, a testID or a letter table: if a string is not in `MCQ_COPY`, it is not user-facing copy of this issue.
- **The sibling you mirror, not edit:** `mobile/src/features/gacha/components/ReviewBody.tsx` (423 lines, do-not-touch). Header badges `:138-150` (`orderBadge` `:93` from `formatRank`, `difficultyLabel` `:95-96`); the full, never-clamped question `:156-169`; sections built at `:105-134` and rendered `:202-218` with `sectionBlock` `:371-373`, `sectionDivider` `:376-380`, `sectionHeader` `:381-388`, `sectionBody` `:389-394`; the markdown styles `mdContainer`/`mdBulletRow`/`mdBullet`/`mdText` `:400-421` that `renderSimpleMarkdown` (`mobile/src/features/gacha/session/reviewContentHelpers.tsx:59-89`) reads off the styles object; `friendlyCodeLanguage` `:13-57` is module-private (nothing is exported from ReviewBody but the component), so you copy what you need. `faceUp`/`onFlip` (`:89-90`) and the Hide button (`:187-195`) are exactly what MCQ must not have (D00 §6 #6): no re-hide, no re-submit, no RatingBar.
- **The dock you replace:** `mobile/src/features/gacha/components/RatingBar.tsx` (98 lines, do-not-touch): root `View testID={testID}` with default `'review-rating-bar'` (`:40`, `:43`), hint `:44`, four buttons `:48-67` with `minHeight: 56` (`:80`) and — a pattern gap, not a pattern — no `accessibilityRole`. The untouched `tests/integration/session-card.screen.test.tsx:555-557` finds exactly one `View` with `testID 'review-rating-bar'`; D05 keeps that true by giving your dock the same testID.
- **Import rule that keeps the untouched screen suite green** (D00 §2.4): `session-card.screen.test.tsx:5-40` factory-mocks `react-native` with exactly `View, Text, ScrollView, ActivityIndicator, Pressable, Animated, useWindowDimensions, Alert, StyleSheet`. D05 imports your components into that screen, so a name outside that set (`Platform`, `AccessibilityInfo`, `Vibration`, `Image`, …) rendered by your component would throw there. Your three components need only `Pressable, StyleSheet, Text, View`. Haptics and VoiceOver announcements are the parent's job (D05 `handleSubmit`, `onOverLimit` → `loadExpoHaptics()` from `mobile/src/components/ceremonyHaptics.ts:45`; `AccessibilityInfo` through the `import * as RN` + `readRN` pattern of `mobile/src/screens/HomeScreen.tsx:11`, `:48-57`, announced as at `mobile/src/screens/DrawCeremonyScreen.tsx:523`) — your components import neither `expo-haptics` nor `AccessibilityInfo`.
- **`CodeBlock`** (`mobile/src/components/CodeBlock.tsx`, props `:222-231` `{ code, language?, label? }`, caption `testID="code-block-language"` `:241`, horizontal scroll `testID="code-block-scroll"` `:249`) is reused as-is for the CODING SAMPLE section, exactly as ReviewBody does at `:118-124`.
- **The storage pattern you copy:** `mobile/src/features/gacha/draw/ceremonyPrefs.ts` — device-global key (`:1-8`, `:12-13`), a `setTimeout` race with `clearTimeout` on settle (`:21-42`), a write that never throws (`:44-57`), no module-level memo. Its test `mobile/tests/unit/ceremonyPrefs.test.ts:6-24` (Map-backed `AsyncStorage` mock with a switchable `mode`, `loadPrefs()` with `vi.resetModules()`) and the hang case `:73-83` (`vi.useFakeTimers()` + `advanceTimersByTimeAsync`) are the harness for `mcqCoachPrefs.test.ts`.
- **The component-test harness you copy:** `mobile/tests/unit/reviewBody.test.tsx:16-31` (`react-native` factory mock with `View, Text, ScrollView, Pressable, Platform, StyleSheet`) and its helpers `:86-106` (`render` → `renderer.create` inside `act`, `texts`, `textByTestId`, `hasText`); `react-test-renderer` is a devDependency (`mobile/package.json:67`). Doc drift: D00 §3.4 says the CodeBlock is mocked in that file — it is not (only `session-card.screen.test.tsx:63-69` mocks it); the `Platform` entry in the mock is what lets the real `CodeBlock` render, and your body suite keeps it so the CODING SAMPLE section can be asserted through `code-block-language`.
- **Theme tokens** (`mobile/src/theme/colors.ts`, `spacing.ts`, `typography.ts`): `colors.softCream`, `hairline`, `shadowSoft`, `ink`, `inkSoft`, `inkMuted`, `inkSecondary`, `gold`, `mint`, `danger`, `pokeBlue`, `pokeBlueFaint`, `parchmentBg`, `parchmentBgDeep`; `spacing.xs/sm/md`; `typography.title3/body/bodySmall/caption/button`. No new token.
- **Fixtures** (the only MCQ text a test may quote): plan §4.3 card 1 `aws-sqs-order-buffer-mcq-01` (`docs/mcq-card-type-plan-2026-09-18.md:177-217`: 4 options, correct `b`, `QUALIFIER: LEAST operational overhead`, A: and USAGE: present) and card 2 `aws-s3-compliance-copy-mcq-02` (`:218-253`: 5 options, correct `a` + `c`, no qualifier, "(Choose two.)" in the stem); the 481-character option `a` of `aws-kms-admin-vs-user-separation-mcq-43` at `content/decks/aws-saa-c03.md:4516` (card header `:4510`) for the Dynamic Type case. Join a fixture's wrapped lines with one space. Never copy ExamTopics / SAA-C03 dump content.
- **Feature flags are not read here.** `mobile/src/config/featureFlags.ts:5-11`, `:21-27` exist; D05 reads them once when a card becomes current (D00 §2.5). Your components take an already-normalised `mcq` and never consult `card.Mcq` (D00 §0 reader rule; after D03 the allowed readers are `deckExport.ts`, `deckRepository.ts`, `normalizeMcq.ts` and the planner's pinned `normalizeMcq(card.Mcq)` line in `sessionPlanner.ts` — none of them yours).

What D00 decided (binding; `docs/delivery/r16-issues/D00-contracts.md`): §0 non-negotiables; §1.1 (`:47-50`, `:62`) the four source files and four test files of this issue; §2.4 (the signatures repeated verbatim below, the react-native import rule, the stage-by-stage behaviour); §3.4 the required cases and the eleven pinned `it` titles; §4 deps = **D02** (queue: after D03); §5 the verify skeleton; §6 #6 (sibling), #16 (the count is a dock element, the over-limit hint a body element), #17 ("Show full question" is the one deliberate clamp).

Gaps D00 leaves open — resolved here and binding for this issue:

1. **Stem emphasis.** `stemSegments(stem, qualifier)` (exported, pure) splits the stem into `{ text, emphasis: 'qualifier' | 'caps' | null }` segments: when `qualifier` is non-null and its trimmed text occurs in the stem (first occurrence, case-insensitive, the stem's own casing kept), exactly one `'qualifier'` segment; otherwise every match of `/\b[A-Z]{4,}\b/g` becomes a `'caps'` segment (fallback, not a demotion — D00 §6 #2). The stem `Text` (testID `mcq-stem`) renders the segments as children: plain strings and nested `<Text>` nodes with `testID="mcq-qualifier"` (`MCQ_TEST_IDS.qualifier`) or `testID="mcq-stem-caps"` (D04-local) and the emphasis style `{ fontWeight: '900', color: colors.ink }` on a base of `{ fontSize: typography.title3, lineHeight: 24, color: colors.inkSoft, fontWeight: '700' }`.
2. **D04-local testIDs** (not in `MCQ_TEST_IDS`, exported from `McqReviewBody.tsx` as `MCQ_BODY_TEST_IDS` so D05's suite can import them): `orderBadge: 'mcq-order-badge'`, `stemCaps: 'mcq-stem-caps'`, `optionText: (key) => 'mcq-option-text-<key>'`, `rowGlyph: (key) => 'mcq-row-glyph-<key>'`, `rowLabel: (key) => 'mcq-row-label-<key>'`.
3. **Over-limit hint lifetime.** No timer. The hint appears on an ignored tap and disappears on the next change of `picks` or `stage` (a `useEffect` keyed on `picks.join('|')` and `stage`). Tapping an already-picked option in multi-select always calls `onToggleOption` (deselect is never over the limit).
4. **Verdict rows.** Every row stays the same `Pressable testID="mcq-option-<key>"`, now `disabled` with `accessibilityState={{ checked, disabled: true }}` and no `onPress`; its WHY block (and the `mcq-why-toggle-<key>` Pressable) is rendered as a SIBLING directly after the row inside the option list, never nested inside the disabled Pressable. Glyphs: `'correct-picked' → '✔'`, `'correct-missed' → '✓'`, `'wrong-picked' → '✗'`, `'wrong-unpicked' → ''`; labels: `MCQ_COPY.rowCorrectPicked` / `rowCorrectMissed` / `rowWrongPicked` / none. The wrong-picked WHY is always open (no toggle); a correct option's `why` never renders even when non-null. In the verdict stage the row's `accessibilityLabel` is `${mcqOptionA11yLabel(index, total, text)}. ${label}` for the three labelled states and unchanged for `wrong-unpicked`.
5. **Local state resets** (`stemExpanded`, `overLimit`, `expandedWhy`) on `[card.StableUid, attemptIndex]` via `useEffect` — D05 keeps one body mounted across cards.
6. **`rowStateFor` outside the verdict stage** answers `'selected'` when picked, else `'idle'`.
7. **The only clamps** in `McqReviewBody`: the options-stage collapsed stem (`numberOfLines={3}`, until expanded), the letter disc (1), the three header badges/chip (1), the section headers (1). The verdict banner, the redeal banner, the schedule line, every option text and every WHY are unclamped.
8. **`readMcqCoachSeen`**: `null` (nothing stored) → `false`; any stored string (`'1'`, garbage, `''`), a rejected read, a thrown read or a read slower than `MCQ_COACH_READ_TIMEOUT_MS` → `true`.
9. **`friendlyCodeLanguage`** is copied from `ReviewBody.tsx:13-57` into `McqReviewBody.tsx` under the same name (module-private there; ReviewBody is do-not-touch).
10. **`k` for the partial banner** is computed in the body: `shownOrder.filter((o) => o.correct && picks.includes(o.key)).length`; `N = mcqRequiredCount(mcq)`.

## Read first

1. `docs/delivery/r16-issues/D00-contracts.md` §0, §1.1 (the D04 column and the test list at `:62`), §2.2 (the exact `MCQ_COPY` / `MCQ_TEST_IDS` you consume), §2.4 (this issue), §2.5.3 (how D05 mounts you — read-only for you), §3.4, §5, §6 #6, #16, #17.
2. `mobile/src/features/gacha/mcq/mcqConstants.ts`, `mcqVerdict.ts`, `normalizeMcq.ts` and `mobile/src/types/deckExport.ts` as they are on the branch (D01/D02 output) — the names you import.
3. `mobile/src/features/gacha/components/ReviewBody.tsx` (whole, 423 lines) and `RatingBar.tsx` (whole, 98 lines). You edit neither.
4. `mobile/src/features/gacha/session/reviewContentHelpers.tsx:41-89`; `mobile/src/components/CodeBlock.tsx:222-252`; `mobile/src/features/gacha/library/cardRank.ts:26-28`.
5. `mobile/src/features/gacha/draw/ceremonyPrefs.ts` (whole, 79 lines) and `mobile/tests/unit/ceremonyPrefs.test.ts:1-83`.
6. `mobile/tests/unit/reviewBody.test.tsx` (whole, 243 lines) and `mobile/tests/unit/ratingBar.test.tsx:1-33`.
7. `mobile/tests/integration/session-card.screen.test.tsx:5-40` (the export set your imports must fit) and `:536-573` (the dock pin).
8. `mobile/src/screens/HomeScreen.tsx:11`, `:48-57` (why `AccessibilityInfo` is not yours to import); `mobile/src/components/ceremonyHaptics.ts:27-33`, `:45-52`.
9. `docs/mcq-card-type-plan-2026-09-18.md:177-253` (fixtures) and `content/decks/aws-saa-c03.md:4510-4516`.

## Constraints

- **Scope (the ONLY files that may change):** `mobile/src/features/gacha/mcq/mcqCoachPrefs.ts` (new), `mobile/src/features/gacha/components/McqReviewBody.tsx` (new), `mobile/src/features/gacha/components/McqActionDock.tsx` (new), `mobile/src/features/gacha/components/McqCoachLine.tsx` (new), `mobile/tests/unit/mcqReviewBody.test.tsx` (new), `mobile/tests/unit/mcqActionDock.test.tsx` (new), `mobile/tests/unit/mcqCoachLine.test.tsx` (new), `mobile/tests/unit/mcqCoachPrefs.test.ts` (new). Eight creates, zero edits. Nothing else.
- **Frozen files (gacha-v7 §2.1, narrowed by C00 §0; D00 §0):** `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts` are zero-diff (D01 holds the wave's only exception; not you). Also untouched: `mobile/src/features/gacha/components/ReviewBody.tsx`, `RatingBar.tsx`, `mobile/src/screens/SessionCardScreen.tsx`, `SessionSummaryScreen.tsx`, `mobile/src/navigation/types.ts`, `mobile/src/features/gacha/mcq/normalizeMcq.ts`, `mcqConstants.ts`, `mcqVerdict.ts`, `mcqShuffle.ts`, `mcqRotation.ts`, `mobile/src/types/deckExport.ts`, `mobile/src/config/featureFlags.ts`, `remoteConfig.ts`, `forceUpdateGate.ts`, `mobile/src/review/storage.ts`, `mobile/src/content/chunkedInstall.ts`, `mobile/src/features/gacha/contracts.ts`, `mobile/src/features/gacha/session/reviewContentHelpers.tsx`, `mobile/src/components/CodeBlock.tsx`, `mobile/src/components/ceremonyHaptics.ts`, `mobile/src/features/gacha/draw/ceremonyPrefs.ts`, `mobile/src/theme/*`, `mobile/tests/setup/*`, `mobile/vitest.config.ts`, `mobile/tsconfig.json`.
- **OTA rule (D00 §0):** no change to `mobile/package.json`, `mobile/package-lock.json`, `mobile/app.json`, `mobile/eas.json`; no new dependency, no native module, no `npm install`. `"expo-updates": "~29.0.15"`, `"vite": "7.2.4"` and `"version": "1.6.0"` stay.
- **React-native imports (D00 §2.4):** each of the three components has exactly one `react-native` import line, byte for byte `import { Pressable, StyleSheet, Text, View } from 'react-native';`. No `Platform`, `AccessibilityInfo`, `Vibration`, `Image`, `Animated`; no static `expo-haptics` / `expo-updates` import; no `react-native-safe-area-context`, no `@sentry/*`. `mcqCoachPrefs.ts` imports only `AsyncStorage` and `./mcqConstants`. The verify greps the four source files for these tokens as plain text, so `Platform`, `AccessibilityInfo`, `Vibration`, `expo-haptics`, `expo-updates`, `faceUp`, `onFlip`, `RatingBar`, `allowFontScaling`, `maxFontSizeMultiplier`, `Date.now`, `Math.random`, `mcqShuffle`, `mcqRotation` must not appear **even inside a comment** (do not paste D00's "no faceUp/onFlip, no Hide, no RatingBar" header line). `mcqVerdict.ts` is imported with `import type` only.
- **Readers of `card.Mcq`:** none of your files references `.Mcq` (D00 §0). The body receives `mcq: McqExport` already normalised.
- **No scheduler / feature-flag / store import:** nothing under `mobile/src/review`, `mobile/src/config`, `mobile/src/sync`, `mobile/src/features/gacha/session/sessionStore.ts` or `rewards/` is imported by your files (`reviewContentHelpers.tsx` is allowed — ReviewBody already depends on it). `mcqVerdict.ts` is imported for types only (`import type { McqConfidence, McqVerdict }`); `describeScheduledRating` is D05's call, you receive its `.line` as `scheduleLine`.
- **Banned literals in any new line:** the six terms of B00 §0 (driver gate). Use "work around", "sidestep", "guard", "fallback", "probe". No `@ts-ignore`, `@ts-expect-error`, `eslint-disable`, `.skip(`, `.only(`. Any explicit component return type is `React.JSX.Element` (or omitted), never `JSX.Element` (C00 §6 #23).
- **Existing tests:** no existing test file changes. `tests/unit/reviewBody.test.tsx`, `tests/unit/ratingBar.test.tsx`, `tests/unit/ceremonyPrefs.test.ts`, `tests/integration/session-card.screen.test.tsx`, `tests/unit/mcqConstants.test.ts`, `tests/unit/mcqVerdict.spec.ts`, `tests/unit/mcqShuffle.test.ts`, `tests/unit/normalizeMcq.test.ts` stay green untouched.
- **testIDs (pinned, all from `MCQ_TEST_IDS` unless marked local):** body `mcq-review-body`, `mcq-kind-chip`, `mcq-stem`, `mcq-qualifier`, `mcq-stem-hint`, `mcq-show-full-stem`, `mcq-option-<key>`, `mcq-option-letter-<key>`, `mcq-over-limit-hint`, `mcq-verdict-banner`, `mcq-schedule-line`, `mcq-why-<key>`, `mcq-why-toggle-<key>`, `mcq-section-explanation`, `mcq-section-qualifier`, `mcq-section-usage`, `mcq-section-code`, `mcq-redeal-banner`; local `mcq-order-badge`, `mcq-stem-caps`, `mcq-option-text-<key>`, `mcq-row-glyph-<key>`, `mcq-row-label-<key>`; dock `review-rating-bar` (default root), `mcq-dock-hint`, `mcq-selected-count`, `mcq-show-options`, `mcq-submit-sure`, `mcq-submit-unsure`, `mcq-dont-know`, `mcq-next`; coach `mcq-coach-line` (default root), `mcq-coach-dismiss`. **Copy (pinned):** only `MCQ_COPY` strings and the `mcq*` formatters of D00 §2.2 reach the screen — `Multiple choice` / `Choose 2` / `Choose 3`, `Decide on your answer before you look at the options.`, `Show options`, `Show full question`, `Sure`, `Not sure`, `I don't know`, `How confident are you?`, `Deselect one first`, `Correct`, `Not this time`, `You knew ${k} of ${n}`, `You missed this one`, `Your pick`, `Why not?`, `EXPLANATION`, `WHY THE QUALIFIER MATTERS`, `REAL USAGE`, `CODING SAMPLE`, `Next`, `Finish run`, `Back again — let's see if it stuck`, the coach sentence, `Got it`, `${k} of ${n} selected`, `Option ${letter} of ${total}: ${text}`.
- Standing rules: no `git push`, no PR, never touch `main`, no EAS / expo / deploy command, no npm/git activity outside your worktree.

## Changes required

1. **`mobile/src/features/gacha/mcq/mcqCoachPrefs.ts` (new)** — `ceremonyPrefs.ts` pattern (`:1-13`, `:21-42`, `:44-57`): `import AsyncStorage from '@react-native-async-storage/async-storage';`, `import { MCQ_COACH_READ_TIMEOUT_MS, MCQ_COACH_SEEN_KEY } from './mcqConstants';`, and, verbatim:
   ```ts
   export { MCQ_COACH_SEEN_KEY, MCQ_COACH_READ_TIMEOUT_MS } from './mcqConstants';
   /** true when the key holds '1'; any error, garbage or a read slower than MCQ_COACH_READ_TIMEOUT_MS → true ("seen": nobody is ever stuck behind the line). */
   export async function readMcqCoachSeen(): Promise<boolean>;
   /** Writes '1'. Never throws. */
   export async function markMcqCoachSeen(): Promise<void>;
   ```
   `readMcqCoachSeen` races `AsyncStorage.getItem(MCQ_COACH_SEEN_KEY)` against a `setTimeout(…, MCQ_COACH_READ_TIMEOUT_MS)` exactly as `readCeremoniesCompleted` does (settle-once guard, `clearTimeout` on settle, a synchronous throw resolves too); the resolved value is `raw !== null` (gap #8). `markMcqCoachSeen` awaits `AsyncStorage.setItem(MCQ_COACH_SEEN_KEY, '1')` inside `try { … } catch { /* storage stays the source of truth */ }`. No module-level `let`, no memo, no user scoping, no `__DEV__` override, no react import. ≈ 40 lines.

2. **`mobile/src/features/gacha/components/McqReviewBody.tsx` (new)** — imports: `React, { useEffect, useState } from 'react'`; the pinned `react-native` line; `CodeBlock from '../../../components/CodeBlock'`; `type { CardExport, McqExport, McqOption } from '../../../types/deckExport'`; `{ formatRank } from '../library/cardRank'`; `{ normalizeCodeLanguage, renderSimpleMarkdown } from '../session/reviewContentHelpers'`; `{ MCQ_COPY, MCQ_TEST_IDS, mcqBannerPartial, mcqKindChip, mcqLetter, mcqOptionA11yLabel, mcqQualifierBody } from '../mcq/mcqConstants'`; `{ mcqRequiredCount } from '../mcq/normalizeMcq'`; `type { McqVerdict } from '../mcq/mcqVerdict'`; `colors`, `spacing`, `typography`. Exports, verbatim (D00 §2.4):
   ```tsx
   export type McqStage = 'stem' | 'options' | 'verdict';
   export type McqOptionRowState = 'idle' | 'selected' | 'correct-picked' | 'correct-missed' | 'wrong-picked' | 'wrong-unpicked';
   export function rowStateFor(option: McqOption, picked: boolean, stage: McqStage): McqOptionRowState;   // exported for the tests
   export type McqReviewBodyProps = {
     card: CardExport;
     mcq: McqExport;                      // already normalised (resolveMcq)
     rank?: number | null;                // same "#011" badge as ReviewBody (formatRank)
     stage: McqStage;
     shownOrder: readonly McqOption[];    // display order; letter = mcqLetter(index)
     picks: readonly string[];            // option keys in pick order
     verdict: McqVerdict | null;          // non-null only in 'verdict'
     scheduleLine: string | null;         // describeScheduledRating(...).line, non-null only in 'verdict'
     attemptIndex: number;                // ≥ 1 → redeal banner (MCQ_COPY.redeal, testID redealBanner)
     onToggleOption: (key: string) => void;
     onOverLimit?: () => void;            // multi-select tap beyond requiredCount (parent does the haptic)
   };
   export function McqReviewBody(props: McqReviewBodyProps)
   export default McqReviewBody;
   ```
   plus the D04-local exports (gaps #1 and #2), verbatim:
   ```ts
   export type StemSegment = { text: string; emphasis: 'qualifier' | 'caps' | null };
   export function stemSegments(stem: string, qualifier: string | null): StemSegment[]
   export const MCQ_BODY_TEST_IDS = Object.freeze({
     orderBadge: 'mcq-order-badge',
     stemCaps: 'mcq-stem-caps',
     optionText: (key: string) => `mcq-option-text-${key}`,
     rowGlyph: (key: string) => `mcq-row-glyph-${key}`,
     rowLabel: (key: string) => `mcq-row-label-${key}`,
   });
   ```
   `rowStateFor`: `stage !== 'verdict'` → `picked ? 'selected' : 'idle'`; else `option.correct ? (picked ? 'correct-picked' : 'correct-missed') : (picked ? 'wrong-picked' : 'wrong-unpicked')`.

   Render, root `<View style={styles.card} testID={MCQ_TEST_IDS.body}>` with ReviewBody's card chrome (`:230-243`), top-down:
   a. **Redeal banner** when `attemptIndex >= 1`: `<Text testID={MCQ_TEST_IDS.redealBanner} style={styles.redeal}>{MCQ_COPY.redeal}</Text>` (all stages, unclamped).
   b. **Header row** (`:138-150` layout): `<Text testID="mcq-order-badge" numberOfLines={1}>` with `orderBadge` computed exactly as `ReviewBody.tsx:93`; difficulty badge as `:95-96`, `numberOfLines={1}`; kind chip `<Text testID={MCQ_TEST_IDS.kindChip} numberOfLines={1}>{mcqKindChip(requiredCount)}</Text>` where `const requiredCount = mcqRequiredCount(mcq)`.
   c. **Stem**: `<Text testID={MCQ_TEST_IDS.stem} style={styles.stem} numberOfLines={collapsed ? 3 : undefined}>` (that exact expression, once — the verify greps it and rejects any other numeric `numberOfLines` except `{1}`) whose children are `stemSegments(card.Question, mcq.qualifier).map(...)` — a plain string for `emphasis === null`, otherwise `<Text key={i} testID={seg.emphasis === 'qualifier' ? MCQ_TEST_IDS.qualifier : MCQ_BODY_TEST_IDS.stemCaps} style={styles.stemEmphasis}>{seg.text}</Text>`. `collapsed = stage === 'options' && !stemExpanded`. Directly under it, only while `collapsed`: `<Pressable testID={MCQ_TEST_IDS.showFullStem} accessibilityRole="button" onPress={() => setStemExpanded(true)}><Text numberOfLines={1}>{MCQ_COPY.showFullStem}</Text></Pressable>`; once expanded the Pressable is gone and the stem is unclamped. In the stem stage, below the stem: `<Text testID={MCQ_TEST_IDS.stemHint} style={styles.stemHint}>{MCQ_COPY.stemHint}</Text>`.
   d. **Option list** (options and verdict stages only; nothing with a `mcq-option-` testID exists in the stem stage): for `shownOrder.map((option, index) => …)`, `picked = picks.includes(option.key)`, `state = rowStateFor(option, picked, stage)`, `letter = mcqLetter(index)`, `label = mcqOptionA11yLabel(index, shownOrder.length, option.text)`:
      ```tsx
      <Pressable
        key={option.key}
        testID={MCQ_TEST_IDS.option(option.key)}
        accessibilityRole={requiredCount === 1 ? 'radio' : 'checkbox'}
        accessibilityState={stage === 'verdict' ? { checked: picked, disabled: true } : { checked: picked }}
        accessibilityLabel={stage === 'verdict' && rowLabel ? `${label}. ${rowLabel}` : label}
        disabled={stage === 'verdict'}
        onPress={stage === 'verdict' ? undefined : () => handleTap(option.key, picked)}
        style={[styles.optionRow, styles[rowStyleKey(state)]]}   // minHeight: 48
      >
        <View style={styles.letterDisc}><Text testID={MCQ_TEST_IDS.optionLetter(option.key)} numberOfLines={1} style={styles.letter}>{letter}</Text></View>
        <View style={styles.optionBody}>
          {glyph || rowLabel ? (<View style={styles.rowStatus}>
            {glyph ? <Text testID={MCQ_BODY_TEST_IDS.rowGlyph(option.key)} style={styles.rowGlyph}>{glyph}</Text> : null}
            {rowLabel ? <Text testID={MCQ_BODY_TEST_IDS.rowLabel(option.key)} style={styles.rowLabel}>{rowLabel}</Text> : null}
          </View>) : null}
          <Text testID={MCQ_BODY_TEST_IDS.optionText(option.key)} style={styles.optionText}>{option.text}</Text>
        </View>
      </Pressable>
      ```
      followed, as a sibling (gap #4) and only in the verdict stage, by the WHY block: for `'wrong-picked'` `<Text testID={MCQ_TEST_IDS.why(option.key)} style={styles.why}>{option.why}</Text>`; for `'wrong-unpicked'` a `<Pressable testID={MCQ_TEST_IDS.whyToggle(option.key)} accessibilityRole="button" accessibilityState={{ expanded }} onPress={toggleWhy(option.key)}><Text numberOfLines={1}>{MCQ_COPY.rowWhyNot}</Text></Pressable>` and, when expanded, the same `mcq-why-<key>` Text; for the two correct states nothing. `handleTap(key, picked)`: single (`requiredCount === 1`) → `onToggleOption(key)`; multi → `!picked && picks.length >= requiredCount` ? (`setOverLimit(true)`, `onOverLimit?.()`, no toggle) : `onToggleOption(key)`. Directly under the list, while `overLimit`: `<Text testID={MCQ_TEST_IDS.overLimitHint} style={styles.overLimit}>{MCQ_COPY.overLimit}</Text>` (gap #3). Row styles (`styles.optionRow` carries `minHeight: 48`, row layout, `spacing.sm` padding, 12 radius): idle hairline border; selected `pokeBlue` border on `pokeBlueFaint`; correct-picked `mint` border/tint; correct-missed `mint` border, no tint; wrong-picked `danger` border/tint; wrong-unpicked `opacity: 0.55`. Colour is never the only signal — the glyph and label carry the state.
   e. **Verdict block** (verdict stage only, after the list): `<Text testID={MCQ_TEST_IDS.verdictBanner} style={styles.banner}>` = `verdict === 'correct' ? MCQ_COPY.bannerCorrect : verdict === 'partial' ? mcqBannerPartial(k, requiredCount) : MCQ_COPY.bannerWrong` (gap #10); then `scheduleLine !== null ? <Text testID={MCQ_TEST_IDS.scheduleLine} style={styles.scheduleLine}>{scheduleLine}</Text> : null`.
   f. **Sections** (verdict stage only), each `<View testID={…}>` with `sectionDivider` between blocks and `sectionHeader` (`numberOfLines={1}`) + body, in this order and only when present: EXPLANATION (`card.Explanation` truthy; `MCQ_TEST_IDS.sectionExplanation`; `<Text style={styles.sectionBody}>`), WHY THE QUALIFIER MATTERS (`mcq.qualifier !== null`; `MCQ_TEST_IDS.sectionQualifier`; body `mcqQualifierBody(mcq.qualifier)`), REAL USAGE (`card.RealWorldUsage` truthy; `MCQ_TEST_IDS.sectionUsage`; `<View style={styles.mdContainer}>{renderSimpleMarkdown(card.RealWorldUsage, styles)}</View>`), CODING SAMPLE (`card.CodeSnippet` truthy; `MCQ_TEST_IDS.sectionCode`; `<CodeBlock code={card.CodeSnippet} language={normalizeCodeLanguage(card.CodeLanguage || 'javascript')} label={card.CodeLanguage ? friendlyCodeLanguage(card.CodeLanguage) : undefined} />` in `codeContainer`). Section labels are `MCQ_COPY.sectionExplanation` / `sectionQualifier` / `sectionUsage` / `sectionCode`. Styles `sectionBlock`, `sectionDivider`, `sectionHeader`, `sectionBody`, `codeContainer`, `mdContainer`, `mdBulletRow`, `mdBullet`, `mdText` are copied from `ReviewBody.tsx:371-421` (the markdown keys are read by name off the styles object).
   g. **State**: `const [stemExpanded, setStemExpanded] = useState(false); const [overLimit, setOverLimit] = useState(false); const [expandedWhy, setExpandedWhy] = useState<readonly string[]>([]);` reset together in `useEffect(..., [card.StableUid, attemptIndex])` (gap #5); `useEffect(() => setOverLimit(false), [picks.join('|'), stage])` (gap #3). Nothing sets `allowFontScaling` or `maxFontSizeMultiplier`; `numberOfLines` appears only where gap #7 allows. ≈ 330 lines.

3. **`mobile/src/features/gacha/components/McqActionDock.tsx` (new)** — imports: `React`; the pinned `react-native` line; `{ MCQ_COPY, MCQ_TEST_IDS, mcqSelectedCount } from '../mcq/mcqConstants'`; `type { McqConfidence } from '../mcq/mcqVerdict'`; `type { McqStage } from './McqReviewBody'`; `colors`. Exports, verbatim (D00 §2.4):
   ```tsx
   export type McqActionDockProps = {
     testID?: string;                     // default 'review-rating-bar' (the untouched dock test finds a View with this testID)
     stage: McqStage;
     requiredCount: number;
     selectedCount: number;
     disabled?: boolean;                  // reviewing
     isLastNode: boolean;                 // 'Finish run' instead of 'Next'
     onShowOptions: () => void;
     onSubmit: (confidence: McqConfidence) => void;
     onDontKnow: () => void;
     onNext: () => void;
   };
   export function McqActionDock(props: McqActionDockProps)
   export default McqActionDock;
   ```
   `const { testID = MCQ_TEST_IDS.dock, disabled = false, … } = props;` (`MCQ_TEST_IDS.dock === 'review-rating-bar'`). Root `<View style={styles.wrapper} testID={testID}>`. Every `Pressable` carries `accessibilityRole="button"`, `accessibilityState={{ disabled: <its disabled> }}`, `disabled={<its disabled>}` and a `minHeight` of 56 (buttons) or 48 (the link); the pressed style is `opacity: 0.9`, the disabled style `opacity: 0.55`. By stage:
   - `'stem'`: one primary `Pressable testID={MCQ_TEST_IDS.showOptions}` (`MCQ_COPY.showOptions`, `pokeBlue` pill like `ReviewBody.tsx:309-328`) → `onShowOptions`; disabled iff `disabled`.
   - `'options'`: `<Text testID={MCQ_TEST_IDS.dockHint} numberOfLines={2} style={styles.hint}>{MCQ_COPY.confidenceHint}</Text>`; when `requiredCount > 1` `<Text testID={MCQ_TEST_IDS.selectedCount} numberOfLines={1} style={styles.count}>{mcqSelectedCount(selectedCount, requiredCount)}</Text>`; a row with primary `mcq-submit-sure` (`MCQ_COPY.sure`) → `onSubmit('sure')` and secondary `mcq-submit-unsure` (`MCQ_COPY.unsure`, hairline outline) → `onSubmit('unsure')`, both disabled iff `disabled || selectedCount !== requiredCount`; then the text link `mcq-dont-know` (`MCQ_COPY.dontKnow`, `styles.link` — underlined caption, `inkSecondary`, centred, `minHeight: 48`) → `onDontKnow`, disabled iff `disabled`.
   - `'verdict'`: one primary `mcq-next` (`isLastNode ? MCQ_COPY.finishRun : MCQ_COPY.next`) → `onNext`; disabled iff `disabled`.
   Button labels are `numberOfLines={1}`. No hint text other than the two above; no `RatingBar` import; no `revealed` prop. ≈ 130 lines.

4. **`mobile/src/features/gacha/components/McqCoachLine.tsx` (new)** — imports `React`, the pinned `react-native` line, `{ MCQ_COPY, MCQ_TEST_IDS } from '../mcq/mcqConstants'`, `colors`, `spacing`, `typography`. Export, verbatim:
   ```tsx
   export type McqCoachLineProps = { visible: boolean; onDismiss: () => void; testID?: string /* 'mcq-coach-line' */ };
   export function McqCoachLine(props: McqCoachLineProps)
   export default McqCoachLine;
   ```
   `if (!visible) return null;` else `<View testID={testID ?? MCQ_TEST_IDS.coachLine} style={styles.band}>` (`parchmentBgDeep` band, hairline border, `spacing.sm` padding, row) containing `<Text style={styles.copy}>{MCQ_COPY.coach}</Text>` (flex 1, `bodySmall`, unclamped) and `<Pressable testID={MCQ_TEST_IDS.coachDismiss} accessibilityRole="button" onPress={onDismiss} style={styles.dismiss}>` (`minHeight: 44`, pill) with `<Text numberOfLines={1} style={styles.dismissText}>{MCQ_COPY.coachDismiss}</Text>`. Marking the flag is the parent's job (D05 `handleMcqNext` / the dismiss handler); this component only calls `onDismiss`. ≈ 50 lines.

5. **`mobile/tests/unit/mcqReviewBody.test.tsx` (new)** — the `reviewBody.test.tsx:16-31` mock verbatim (keep `Platform`: the real `CodeBlock` renders) and its helpers `:86-106` adapted; imports `{ MCQ_BODY_TEST_IDS, McqReviewBody, rowStateFor, stemSegments } from '../../src/features/gacha/components/McqReviewBody'`, `{ MCQ_COPY, MCQ_TEST_IDS, mcqQualifierBody } from '../../src/features/gacha/mcq/mcqConstants'`, `type { CardExport, McqExport } from '../../src/types/deckExport'`. Fixtures: `card1`/`mcq1` from plan §4.3 card 1 (`{ StableUid: 'aws-sqs-order-buffer-mcq-01', OrderInDeck: 1540, Difficulty: 2, Question, Explanation: <A:>, RealWorldUsage: <USAGE:> }`, `mcq1 = { v: 1, qualifier: 'LEAST operational overhead', shuffle: true, options: [a, b*, c, d] }` with the WHY texts, `why: null` on `b`); `card2`/`mcq2` from card 2 (5 options, `a` and `c` correct; give `c` a non-null `why: 'never shown'` to prove correct WHYs never render); `LONG_OPTION` = the 481-character option `a` of `content/decks/aws-saa-c03.md:4516`, as ONE string literal on one line (not a `+` chain — the verify greps its first words `Write a key policy with one statement that grants`). `SHOWN1 = [c, a, d, b]` (keys ≠ letters), `SHOWN2 = mcq2.options` in stored order. Pin testIDs and short copy as string literals in the assertions (`'mcq-option-b'`, `'radio'`, `'checkbox'`, `'Option B of 4: '`, `'Deselect one first'`, `'mcq-why-toggle-b'`, `'mcq-redeal-banner'`, `'code-block-language'`), not through `MCQ_TEST_IDS` / `MCQ_COPY` — a test that imports the constant it is meant to pin proves nothing; only the long sentences (`MCQ_COPY.stemHint`, `MCQ_COPY.redeal`, `MCQ_COPY.coach`) and `mcqQualifierBody(...)` may be imported. The verify greps the literals just listed plus `allowFontScaling`, `maxFontSizeMultiplier`, `numberOfLines`, `rowStateFor`, `stemSegments`, `MCQ_BODY_TEST_IDS`. A `renderBody(overrides: Partial<McqReviewBodyProps>)` helper with defaults `{ card: card1, mcq: mcq1, stage: 'stem', shownOrder: SHOWN1, picks: [], verdict: null, scheduleLine: null, attemptIndex: 0, onToggleOption: vi.fn() }`. Cases, each its own `it`, titles verbatim:
   1. `it('renders the full stem with the qualifier in bold', …)` — stem stage: `mcq-stem` has `numberOfLines` undefined; exactly one `mcq-qualifier` node, children `'LEAST operational overhead'`, `style` (flattened) `fontWeight === '900'`; concatenating the stem's children (strings and nested Text children) reproduces `card1.Question`; `stemSegments(card1.Question, 'least OPERATIONAL overhead')` finds the same segment (case-insensitive); `mcq-kind-chip` is `'Multiple choice'`, `'Choose 2'` for `mcq2`, `'Choose 3'` for `mcq2` with option `e` also `correct: true`; `mcq-stem-hint` is `MCQ_COPY.stemHint`; `mcq-order-badge` reads `'#011'` with `rank: 11` and `'#1540'` without; no node whose testID starts with `mcq-option-`, no `mcq-show-full-stem`, no section.
   2. `it('bolds all-caps words when the qualifier is absent or not found', …)` — `{ ...mcq1, qualifier: null }` → no `mcq-qualifier`, the `mcq-stem-caps` nodes read exactly `['LEAST']`; `{ ...mcq1, qualifier: 'MOST secure' }` (not in the stem) → the same fallback; `stemSegments('Pick the BEST and the FEWEST steps for S3', null)` yields caps segments `['BEST', 'FEWEST']` only (`S3` is not four letters).
   3. `it('collapses the stem to three lines on the options stage until Show full question', …)` — options stage: `mcq-stem` `numberOfLines === 3`, `mcq-show-full-stem` present with text `MCQ_COPY.showFullStem`; press it → `numberOfLines` undefined and the Pressable gone; the verdict stage never clamps the stem.
   4. `it('assigns letters by displayed position, never by key', …)` — options stage with `SHOWN1`: `mcq-option-letter-c` = `'A'`, `-a` = `'B'`, `-d` = `'C'`, `-b` = `'D'`; `mcq-option-a`'s `accessibilityLabel` is `'Option B of 4: ' + mcq1.options[0].text`; each `mcq-option-text-<key>` renders that option's full text; the list order in the tree is c, a, d, b.
   5. `it('uses radio for single and checkbox for multi with checked state', …)` — `mcq1`, `picks: ['b']`: every row `accessibilityRole === 'radio'`, `mcq-option-b` `accessibilityState` `toEqual({ checked: true })`, `mcq-option-a` `{ checked: false }`, `rowStateFor(mcq1.options[1], true, 'options') === 'selected'` and `'idle'` unpicked; pressing `mcq-option-a` calls `onToggleOption('a')` once; `mcq2`, `picks: ['a']`: every row `'checkbox'`, `mcq-option-a` checked, `mcq-option-c` unchecked; no `mcq-over-limit-hint`.
   6. `it('ignores a pick beyond the limit and says so', …)` — `mcq2`, `picks: ['a', 'c']`, `onOverLimit: vi.fn()`: press `mcq-option-d` → `onToggleOption` not called, `onOverLimit` called once, `mcq-over-limit-hint` reads `'Deselect one first'`; press `mcq-option-a` (picked) → `onToggleOption('a')` called (deselect is allowed); re-render with `picks: ['a']` → the hint is gone; single-select `mcq1` with `picks: ['b']`: pressing `mcq-option-a` toggles and never calls `onOverLimit`.
   7. `it('shows the four verdict row states with their WHYs', …)` — `mcq2`, `stage: 'verdict'`, `picks: ['a', 'd']`, `verdict: 'partial'`, `scheduleLine: 'Scheduled as Hard · the card stays where it is · back in 1 day'`: `rowStateFor` gives `correct-picked` (a), `correct-missed` (c), `wrong-picked` (d), `wrong-unpicked` (b, e); labels `mcq-row-label-a` `'Correct'`, `-c` `'You missed this one'`, `-d` `'Your pick'`, none for b/e; glyphs `'✔'`, `'✓'`, `'✗'`; `mcq-why-d` present with option d's WHY and no `mcq-why-toggle-d`; `mcq-why-b` absent until `mcq-why-toggle-b` (text `'Why not?'`, role button, `accessibilityState.expanded === false`) is pressed, then present with option b's WHY and `expanded === true`; no `mcq-why-a`, no `mcq-why-c` (c's `why` is non-null and still never rendered); every row `disabled === true` with `accessibilityState` `{ checked, disabled: true }` and no `onPress`; `mcq-option-a`'s label ends with `'. Correct'`; `mcq-verdict-banner` reads `'You knew 1 of 2'`; `mcq-schedule-line` reads the line; with `verdict: 'correct'` the banner is `'Correct'`, with `'wrong'` `'Not this time'`.
   8. `it('renders sections in order and the qualifier section only with a qualifier', …)` — `card1`/`mcq1`, verdict, plus `CodeSnippet: 'aws sqs get-queue-attributes --queue-url $Q'`, `CodeLanguage: 'bash'`: the section `View`s appear in tree order `['mcq-section-explanation', 'mcq-section-qualifier', 'mcq-section-usage', 'mcq-section-code']`, headers `'EXPLANATION'`, `'WHY THE QUALIFIER MATTERS'`, `'REAL USAGE'`, `'CODING SAMPLE'`, the qualifier body is `mcqQualifierBody('LEAST operational overhead')`, the explanation text is `card1.Explanation`, `code-block-language` reads `'Bash'` and `code-block-scroll` is horizontal; with `qualifier: null` no `mcq-section-qualifier`; without `CodeSnippet` no `mcq-section-code`; in the stem and options stages no section at all.
   9. `it('shows the redeal banner from the second attempt', …)` — `attemptIndex: 0` → no `mcq-redeal-banner` in any stage; `attemptIndex: 1` → present in all three stages with `MCQ_COPY.redeal` and no `numberOfLines`.
   10. `it('never disables font scaling and never clamps option text', …)` — `mcq1` with option `a`'s text replaced by `LONG_OPTION`, for each stage: no `Text` in the tree has `allowFontScaling === false` or a defined `maxFontSizeMultiplier`; in options and verdict `mcq-option-text-a` has children `=== LONG_OPTION` (481 chars) and `numberOfLines` undefined; every clamped `Text` is either the options-stage stem (3) or single-line chrome shorter than 40 characters; `mcq-why-<key>` texts are unclamped.

6. **`mobile/tests/unit/mcqActionDock.test.tsx` (new)** — the `ratingBar.test.tsx:5-17` mock verbatim; imports `{ McqActionDock } from '../../src/features/gacha/components/McqActionDock'` and `{ MCQ_COPY } from '../../src/features/gacha/mcq/mcqConstants'`; a `renderDock(overrides)` helper with defaults `{ stage: 'stem', requiredCount: 1, selectedCount: 0, isLastNode: false, onShowOptions: vi.fn(), onSubmit: vi.fn(), onDontKnow: vi.fn(), onNext: vi.fn() }`; a `pressable(tree, testID)` finder. TestIDs and copy as literals (the verify greps `'mcq-submit-sure'`, `'mcq-submit-unsure'`, `'mcq-dont-know'`, `'mcq-show-options'`, `'mcq-next'`, `'mcq-selected-count'`, `'1 of 2 selected'`, `'Finish run'`, `'review-rating-bar'`, `accessibilityRole`, `accessibilityState`). Titles verbatim:
   1. `it('shows one button per stage and gates submit on a full pick', …)` — stem: the only Pressable is `mcq-show-options` (text `'Show options'`), pressing it calls `onShowOptions`; no `mcq-submit-*`, `mcq-dont-know`, `mcq-next`. Options (`requiredCount 1`, `selectedCount 0`): `mcq-dock-hint` is `'How confident are you?'`, no `mcq-selected-count`, `mcq-submit-sure` (`'Sure'`) and `mcq-submit-unsure` (`'Not sure'`) both `disabled === true` with `accessibilityState.disabled === true`, `mcq-dont-know` (`"I don't know"`) enabled; `selectedCount 1` → both enabled, pressing them calls `onSubmit('sure')` / `onSubmit('unsure')`, `mcq-dont-know` calls `onDontKnow`; `requiredCount 2, selectedCount 1` → `mcq-selected-count` reads `'1 of 2 selected'` and submit stays disabled, `selectedCount 2` enables it; `disabled: true` disables every Pressable in every stage. Verdict: only `mcq-next` (`'Next'`) → `onNext`.
   2. `it('says Finish run on the last node', …)` — verdict with `isLastNode: true` → `mcq-next` reads `'Finish run'`; `false` → `'Next'`; `isLastNode` changes nothing in the other stages.
   3. `it('carries a button role and disabled state on every pressable', …)` — for each stage every Pressable has `accessibilityRole === 'button'` and `accessibilityState.disabled === props.disabled` (boolean); the root `View` has `testID 'review-rating-bar'` by default and the given `testID` otherwise; exactly one such root.

7. **`mobile/tests/unit/mcqCoachLine.test.tsx` (new)** — the same mock; imports `{ McqCoachLine } from '../../src/features/gacha/components/McqCoachLine'` and `MCQ_COPY`. Literals the verify greps: `'mcq-coach-line'`, `'mcq-coach-dismiss'`, `'Got it'`, `toJSON()`. Titles verbatim:
   1. `it('renders nothing when hidden', …)` — `visible: false` → `tree.toJSON()` is `null`, no `mcq-coach-line`, `onDismiss` never called.
   2. `it('shows the coach copy and dismisses through the button', …)` — `visible: true` → one `View` `mcq-coach-line` (and `testID="custom"` when given), a `Text` whose children are `MCQ_COPY.coach` with no `numberOfLines`, a Pressable `mcq-coach-dismiss` with `accessibilityRole 'button'` and text `'Got it'`; pressing it calls `onDismiss` exactly once.

8. **`mobile/tests/unit/mcqCoachPrefs.test.ts` (new)** — the harness of `ceremonyPrefs.test.ts:6-24` verbatim (Map-backed `AsyncStorage` mock with `mode: 'ok' | 'throw' | 'hang' | 'write-throw'`, `loadPrefs()` = `vi.resetModules()` + `import('../../src/features/gacha/mcq/mcqCoachPrefs')`, `beforeEach` clearing the store and mocks, `afterEach` `vi.useRealTimers()`); `const KEY = 'recallsmith:mcq:coach-seen:v1';` (the verify greps that literal, the `'hang'` / `'write-throw'` modes, `vi.resetModules()`, `useFakeTimers`, `advanceTimersByTimeAsync` and the `@react-native-async-storage/async-storage` mock). Titles verbatim:
   1. `it('reads not seen when nothing is stored', …)` — `await readMcqCoachSeen()` is `false`, `getItem` called once with `KEY`, and again `false` on a second call (no memo: `getItem` called twice).
   2. `it('reads seen on garbage, error or a slow read', …)` — stored `'abc'`, `''`, `'0'`, `'1'` each → `true` (fresh `loadPrefs()` per value); `mode = 'throw'` → `true` and `setItem` never called; `vi.useFakeTimers()`, `mode = 'hang'`, `const p = readMcqCoachSeen(); await vi.advanceTimersByTimeAsync(MCQ_COACH_READ_TIMEOUT_MS); expect(await p).toBe(true)`; back to `mode = 'ok'` with nothing stored → `false`.
   3. `it('marks and re-reads seen', …)` — `await markMcqCoachSeen()`; `setItem` called once with `(KEY, '1')`; `store.get(KEY) === '1'`; `await readMcqCoachSeen()` is `true`; `mode = 'write-throw'` → `await markMcqCoachSeen()` resolves without throwing.
   4. `it('exports the device-global key and the read budget', …)` — `MCQ_COACH_SEEN_KEY === KEY`, `MCQ_COACH_READ_TIMEOUT_MS === 250`, `KEY.startsWith('devcards:u:') === false`.

Estimated size: mcqCoachPrefs ~40 lines, McqReviewBody ~330, McqActionDock ~130, McqCoachLine ~50, tests ~330 + ~120 + ~50 + ~90 lines.

## Acceptance

Run from the worktree root. `docs/delivery/r16-issues/D04.verify.sh` re-runs exactly these.

1. Scope files exist: the four source files and the four test files above; the D01/D02 prerequisites are on the branch (`normalizeMcq.ts` with `mcqRequiredCount`, `mcqConstants.ts` with `MCQ_COPY`, `MCQ_TEST_IDS`, `mcqLetter`, `mcqKindChip`, `mcqSelectedCount`, `mcqBannerPartial`, `mcqQualifierBody`, `mcqOptionA11yLabel`, `MCQ_COACH_SEEN_KEY`, `MCQ_COACH_READ_TIMEOUT_MS`; `mcqVerdict.ts` with `McqVerdict` and `McqConfidence`; `deckExport.ts` with `McqOption` and `McqExport`).
2. Literal guards (all exit 0): each component has the pinned `react-native` import line and none of `Platform`, `AccessibilityInfo`, `Vibration`, `expo-haptics`, `expo-updates`, `react-native-safe-area-context`, `@sentry`, `allowFontScaling`, `maxFontSizeMultiplier`, `faceUp`, `onFlip`, `RatingBar`, `.Mcq`, bare `JSX.Element`, `featureFlags`, `review/model`; `McqReviewBody.tsx` has the `McqStage`, `McqOptionRowState`, `rowStateFor`, `McqReviewBodyProps` (each prop line), `stemSegments`, `MCQ_BODY_TEST_IDS` exports, the `radio`/`checkbox` role expression, `numberOfLines={collapsed ? 3 : undefined}` exactly once and no other numeric clamp but `{1}`, `import type` only from `mcqVerdict.ts`, and imports `../mcq/mcqConstants`, `../mcq/normalizeMcq`, `../library/cardRank`, `../../../components/CodeBlock`, `../session/reviewContentHelpers`; `McqActionDock.tsx` has the `McqActionDockProps` lines, `mcqSelectedCount(`, `MCQ_COPY.showOptions`/`sure`/`unsure`/`dontKnow`/`confidenceHint`/`next`/`finishRun`, `accessibilityRole="button"`, a `minHeight: 56`, no `revealed`; `McqCoachLine.tsx` has the `McqCoachLineProps` line, `MCQ_COPY.coach`, `MCQ_COPY.coachDismiss`, `MCQ_TEST_IDS.coachDismiss`, `return null`; `mcqCoachPrefs.ts` has the re-export line, both function signatures, the `AsyncStorage` import, `setTimeout(`/`clearTimeout(`, no module-level `let`, no react / user-scoping / `__DEV__`; the four test files carry every `it('…'` title above (≥ 10 / ≥ 3 / ≥ 2 / ≥ 4 `it(` blocks), the body suite quotes the 481-character option and asserts `'radio'`, `'checkbox'`, `allowFontScaling`, `maxFontSizeMultiplier`; the prefs suite calls `vi.resetModules()`, `useFakeTimers`, `advanceTimersByTimeAsync` and mocks `@react-native-async-storage/async-storage`; no `.skip(`/`.only(`/`@ts-ignore`/`@ts-expect-error`/`eslint-disable` in any scope file; the eight existing suites of Constraints are zero-diff against the base.
3. `cd mobile && npm run test:typecheck` — exit 0.
4. `cd mobile && npx vitest run tests/unit/mcqReviewBody.test.tsx tests/unit/mcqActionDock.test.tsx tests/unit/mcqCoachLine.test.tsx tests/unit/mcqCoachPrefs.test.ts tests/unit/reviewBody.test.tsx tests/unit/ratingBar.test.tsx tests/unit/ceremonyPrefs.test.ts tests/integration/session-card.screen.test.tsx --reporter=dot` — exit 0.
5. Scope + frozen + OTA guard: `git diff --quiet <merge-base> -- mobile/src/content/deckRepository.ts mobile/src/sync/progressSync.ts mobile/src/review/model.ts`; zero numstat on the do-not-touch list of Constraints and on `mobile/package.json mobile/package-lock.json mobile/app.json mobile/eas.json mobile/vitest.config.ts mobile/tsconfig.json mobile/tests/setup`; `"expo-updates": "~29.0.15"`, `"version": "1.6.0"`, `"vite": "7.2.4"` unchanged; no `@sentry` under `mobile/src`; `.Mcq` referenced only by `deckExport.ts`, `deckRepository.ts`, `normalizeMcq.ts` and `planner/sessionPlanner.ts` (the four-file list that holds after D03, D00 §0 / §6 #19); every changed or untracked path under `mobile/src` / `mobile/tests` is one of the eight scope files (or `docs/delivery/r16-issues/*`).

## Verify

```bash
BASE=delivery/r16-d-mcq bash docs/delivery/r16-issues/D04.verify.sh
```

Runs steps 1–5 above (≈ 1–2 min; `tsc` dominates; no network). The driver then runs the full mobile root gate — `cd mobile && npm run test:typecheck && npx vitest run` — plus the diff-scoped banned-term grep and the suppression scan; all must be green, so do not leave any other suite red.

## Do NOT

- Do NOT edit `ReviewBody.tsx`, `RatingBar.tsx`, `SessionCardScreen.tsx`, `mcqConstants.ts`, `mcqVerdict.ts`, `normalizeMcq.ts`, `deckExport.ts` or any frozen file — if a string, testID or formatter you want is missing from `MCQ_COPY` / `MCQ_TEST_IDS`, use the D04-local `MCQ_BODY_TEST_IDS` for ids and stop for copy: the copy set is closed.
- Do NOT import `Platform`, `AccessibilityInfo`, `Vibration`, `Image` or `Animated` from `react-native` in any of the four source files, nor `expo-haptics`, `expo-updates`, `react-native-safe-area-context`, feature flags, remote config, `review/model`, `sync/*`, the session store or rewards. Haptics and announcements belong to D05.
- Do NOT read `card.Mcq`, call `normalizeMcq` / `resolveMcq` / `describeScheduledRating` / `mapMcqVerdictToRating`, shuffle options, compute the verdict or the rating, or keep the pick state — the body is controlled by `picks`, `stage`, `verdict`, `scheduleLine` from the parent; the only local state is `stemExpanded`, `overLimit`, `expandedWhy`.
- Do NOT add a `faceUp`/`onFlip`, a Hide button, a RatingBar, an "override rating", a time estimate, a score, a percentage or exam vocabulary ("scored as wrong", "EXAM TIP") anywhere (plan §6.5, §6.6).
- Do NOT clamp the stem (except the options-stage 3-line collapse behind "Show full question"), an option text, a WHY, the banner or the coach copy; do NOT set `allowFontScaling={false}` or `maxFontSizeMultiplier`.
- Do NOT nest the WHY toggle inside the disabled verdict row; do NOT rely on colour alone for a row state.
- Do NOT memoise the coach flag in module scope, scope its key per user, or let `readMcqCoachSeen` block longer than 250 ms or ever reject.
- Do NOT edit any existing test; do NOT loosen `tsconfig.json`; do NOT run `npm install`, `npm ci`, `eas …`, `npx expo …`.
- Do NOT run npm or git inside `/Users/qc/src/recallsmith` or anywhere under `/Users/qc/Desktop` — only inside your worktree.
