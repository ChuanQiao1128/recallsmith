# E15 — runbooks and README (`docs-runbooks`)

Write the four operator runbooks the wave has been promising since E02 — `docs/runbooks/deploy-rollback.md` (backend alias, worker alias, console, landing page, OTA republish, deck live pointer, infra, hostnames), `docs/runbooks/incident.md` (the thirteen alarms of E04/E12, each mapped to the first three things to look at: a Logs Insights query over the JSON log fields, the dashboard widget, the runbook to jump to), `docs/runbooks/dr-restore-drill.md` (quarterly restore of the latest automated snapshot to `developercards-drill`, staging functions pointed at it by a `PGHOST` overlay, `scripts/smoke.sh staging`, delete, RTO recorded) and `docs/runbooks/staging-promotion.md` (the `cd.yml` order in prose plus the manual path) — and bring the root `README.md` up to the shipped system: the "Deployment" section (`:71-100`) rewritten around `src_C/deploy.sh` / CD / Terraform, a new "Architecture" section (the review's §1 table condensed, English) with a hostnames table (`api.` / `cdn.` / `console.` / `developercards.app` and the legacy AWS names that stay served), links to `infra/README.md`, `infra/RUNBOOK.md` and the five runbooks. Every state-changing command in a runbook sits in a fenced `bash` block whose first line names who runs it (`# who: supervisor|owner|ci`); nothing in this issue runs any of them. Root: `docs` (+ `README.md`). No code, no `.tf`, no plan, no allow-list file, no AWS call is needed at all. Worker timeout 45 min.

## Context

Base: `delivery/r16-e-prod` (== `main@4b07f19`). Every line below was read on that tree on 2026-09-22; the live AWS facts the same day with `AWS_PROFILE=dev` (account `622994489535`, `ap-southeast-2`, describe/get/list only, no secret value printed). `docs/delivery/r16-issues/E00-contracts.md` (E00) is binding: §0 (`:9-35`) non-negotiables, §1.2 (`:71-108`) file map — the `docs/runbooks/…` row (`:105-106`) is this issue's scope, §2.15 (`:407-409`) this issue's contract, §2.1.3 (`:205-207`) the names of every new resource, §2.4 (`:253-279`) the alarms, dashboard, access-log format and JSON log shape the incident runbook reads, §2.6 (`:290-296`) the SSM path scheme and the `deploy.sh` overlay, §2.9 (`:334-349`) the hostnames and the OTA switch, §2.10 (`:351-357`) staging, §2.11 (`:359-379`) the internal event contract and the schema gate, §2.12 (`:381-385`) the workflows, `scripts/smoke.sh` and `scripts/rollback.sh`, §3.3 (`:436-438`) the two frontend tests this issue runs, §4 (`:464-466`) E15 merges last, after E14, §5 (`:470-482`) verify conventions, §6 #3, #7, #17, #18, #23 (`:487`, `:491`, `:502`, `:503`, `:508`). Review: `docs/backend-architecture-review-2026-09-22.md` §1 (`:26-76`, the diagram + the component table the README condenses), §2.4.2 (`:132`, CD), §2.4.3 (`:133`, migrations), §2.4.5 (`:135`, rollback), §2.4.6 (`:136`, domain), §3 (`:177-241`).

What the tree looks like today:

- **No runbooks, no infra, no scripts.** `docs/runbooks/`, `infra/` and `scripts/` do not exist on base (`ls` → "No such file or directory"); `.github/workflows/` holds `ci.yml` only (four jobs at `:8` `mobile`, `:38` `frontend`, `:100` `e2e`, `:153` `backend`; `on: push` + `pull_request` `:3-5`). By the time E15 starts, E01–E14 have merged in E00 §4 order and the worktree contains: `infra/README.md` + `infra/RUNBOOK.md` + `infra/scripts/check-plan.py` (E01), `infra/scripts/rds-snapshot.sh` (E02), `src_C/Vpc/Authoring/DeckRollback.cs` + `PublishReaper.cs` (E03), `docs/runbooks/secrets-rotation.md` + `scripts/invoke-as-admin.sh` + the rewritten `src_C/deploy.sh` + `src_C/env/prod.env.json` (E06), `mobile/src/config/hosts.ts` + `site/deploy.sh` (E09), `src_C/env/staging.env.json` + `infra/envs/staging/` (E10), `src_C/Vpc/Internal/InternalEvents.cs` + `SchemaVersion.cs` (E12), `.github/workflows/{cd,ota,terraform}.yml` + `scripts/smoke.sh` + `scripts/rollback.sh` (E11), `snowflake/002_snapshot_export.sql` (E13), `README.md:1` = `# DeveloperCards` (E14). The runbooks cite those files; step 1 of the verify checks they are present (they are absent on base, which is also how the verify fails on base).
- **`README.md` (345 lines).** `:1` `# RecallSmith` (E14 turns it into `# DeveloperCards`; E15 never touches line 1). `:11` `## 1. What is in this repository`; `:37` `## 2. Running it`; `:71-100` `## Deployment` — the section this issue rewrites: `:73` "The console is deployed at https://d12pfy1rhi3ekm.cloudfront.net", three still-true bullets about SPA fallback / cache split / build-time config (`:78-87`), `:89` "Deploying is two commands from `frontend/`", the `aws s3 sync dist s3://recallsmith-console-622994489535 --delete` block (`:91-95`), `:98-99` "CI does not deploy: shipping on green is a decision, not a default"; `:101` `## 3. How one request flows`; `:125` `## 4. Tests and CI`; `:272` `## 5. Known limitations`; `:304` "the RecallSmith API and the Cognito hosted UI are stubbed" (the one prose brand string E14 left to E15 — E14's brief, Context "Brand strings"); `:317` `## 6. Repository map`; `:343` `## License`. `:49-53` still describes `frontend/.env.development` as pointing at "the deployed dev API" — true after E09 (only the client id changes there, E00 §2.9.5), left alone.
- **The two tests that read prose.** `frontend/tests/rootReadmePaths.test.ts`: every backticked path in `README.md` that starts with one of `frontend|mobile|src_C|pg-layer|snowflake|docs|.github` (`:19`, regex `:23`) must exist on disk (`:41-42`) and there must be at least 12 of them (`:39`); `infra/…`, `scripts/…`, `site/…` are not in that list, so the README may cite them without the test seeing them — this issue's verify checks those too. `frontend/tests/docsPaths.test.ts`: walks `docs/*.md` **non-recursively** (`readdirSync(DOCS)` `:110`), so `docs/runbooks/*.md` is never read by it (E00 §2.15) — the runbooks need no `paths-not-on-disk` block, and the verify runs its own citation check over them (same regex, wider top-level list). Both tests pass on base (`npx vitest run tests/docsPaths.test.ts tests/rootReadmePaths.test.ts` → 8 passed, 2026-09-22).
- **Deploy scripts as shipped.** `src_C/deploy.sh` (58 lines on base; E06 rewrites it, same CLI): `:10-11` migrations are not run here, `:27` `DRY_RUN` returns before any AWS call, `:44-51` `publish-version` → `update-alias prod` → alias `CodeSha256` check, `:38-43` the 2026-09-21 note (code moved, alias did not, the console migrated the old set). After E06 the script overlays the function environment as `$cur + $file + $sec` before `publish-version` (E00 §2.6.3): current env is the base, `src_C/env/<ENV>.env.json` overrides it, SSM `/developercards/<ENV>/*` overrides both; **keys are never removed** by a deploy, and the worker projection carries only `PGUSER`, `PGSSLMODE`, `PG_MAX`, `LOG_LEVEL` + `PGPASSWORD` (E06 brief, Context "§2.6.3"). Consequence for the DR drill: a `PGHOST` override cannot travel through `staging.env.json` to the worker — the runbook performs the same overlay by CLI on both staging functions (Changes 3). `frontend/deploy.sh`: `:9-10` bucket/distribution defaults, `:26` hard-codes `https://d12pfy1rhi3ekm.cloudfront.net` for the hash check (E09 makes it `CONSOLE_URL`, default `https://console.developercards.app`). `mobile/scripts/release/ios-build.sh`, `ios-submit.sh`, `asc-release.cjs` are the owner's binary scripts (E00 §6 #18: binaries stay manual).
- **Mobile facts the OTA runbook states.** `mobile/app.json:7` `"version": "1.6.1"`, `:51-53` `runtimeVersion.policy = appVersion` (an OTA can only reach binaries of the same version; a republish must come from the same runtime's update list); `mobile/eas.json:66-79` profile `production` → channel `production`; `:52-65` `staging-internal-release` → channel `production` on base (E10 moves it to `staging`, E00 §2.10.4); `:84` `ascAppId` `6756044885`. `mobile/src/content/deckRepository.ts:26-33` (frozen) pins `https://d1ditdi9jqpy6n.cloudfront.net` and `https://ktbq1sie2c.execute-api.ap-southeast-2.amazonaws.com` as defaults; E09's `mobile/src/config/hosts.ts` prefers `https://api.developercards.app` and falls back to the execute-api host on a network error; the owner's `eas update` with `EXPO_PUBLIC_API_BASE=https://api.developercards.app` (+ `EXPO_PUBLIC_CONTENT_BASE_URL=https://cdn.developercards.app`) in the EAS `production` environment is what moves shipped clients (E00 §2.9.4) — listed in the deploy runbook, never run by anyone in this wave.
- **Routes the runbooks call through `scripts/invoke-as-admin.sh`** (`src_C/Vpc/VpcFunction.cs`, base line numbers; E03/E06/E07/E12 add lines above some of them): `GET /health` `:105` (E08 makes it `NONE` auth — the smoke's check 1), `GET /api/v1/db/ping` `:119`, `POST /api/v1/admin/db/migrate` `:123` (E12 adds `lock_timeout` → 409 `MIGRATION_IN_PROGRESS`; `dryRun` body flag), `GET /api/v1/admin/db/migrations` `:131`, `GET /api/v1/authoring/publish/status` `:195`, `GET /api/v1/authoring/publish/jobs` `:199` (job rows with `buildId`), `POST /api/v1/admin/manifest/rebuild` `:207`, `GET /api/v1/authoring/dashboard` `:212`, `GET /api/v1/admin/manifest` `:230`, `GET /api/v1/admin/decks` `:235` (deck ids + slugs); new in the wave: `POST /api/v1/admin/decks/{deckId}/rollback` (E03, body `{"buildId":"…"}`), `POST /api/v1/admin/publish/reap` (E03), `POST /api/v1/admin/db/bootstrap-roles` (E06), the internal event `{"source":"developercards.scheduler","action":"health/deep"}` (E12, direct `aws lambda invoke`, answers `{ ok, schemaVersion, required }`).
- **Live account, 2026-09-22 (what the runbooks describe as "after the wave" versus today).** `core-vpc:prod` → version 48, `worker-lambda:prod` → 4 (`lambda get-alias`); RDS `developercards`: postgres 17.9, `db.t4g.micro`, gp3 20 GB, encrypted, not public, backups 7 d (14 after E02) in window `12:55-13:25` UTC, maintenance `wed:17:19-wed:17:49`, subnet group `default-vpc-04af44dd8f5f48717`, security groups `sg-0d2541aec08b1a215` + `sg-0fbc6607e6473cbd3`, parameter group `default.postgres17`, `DeletionProtection false` (true after E02); automated snapshots are named `rds:developercards-YYYY-MM-DD-HH-MM` (latest `rds:developercards-2026-09-21-13-02`), manual snapshots 0. `route53 list-hosted-zones` → **one zone `developercards.app.` already exists** (`Z0284954BSN00C8BF94Q`, comment "HostedZone created by Route53 Registrar", NS + SOA only) and the public NS of `developercards.app` already answers with its four `awsdns` servers — the owner registered the domain through Route 53 Domains, so delegation is done; no ACM certificate in either region, no API custom domain, no A/AAAA record: none of the `*.developercards.app` hostnames resolves today, E09 creates them (E09 must adopt that zone rather than create it — noted for the supervisor, not E15's concern). `cloudwatch describe-alarms` → 0, `sns list-topics` → 0, `scheduler list-schedule-groups` → `default` only, `ssm describe-parameters` → 0, `sqs list-queues` → `recallsmith-publish-jobs` only; log groups `/aws/lambda/{core-vpc,worker-lambda,edge-public,developercards-api}` with no retention (E02 sets it; `/aws/apigateway/developercards-api` is created by E04). The runbooks document the post-wave state (E00's names) and say so in one line each; they do not describe today's account.

What E00 decided (and what the tree adds to it — recorded here, binding for E15):

1. **Scope is four new files plus `README.md`** (E00 §1.2 `:105-106`). `docs/runbooks/secrets-rotation.md` is E06's: E15 links to it and never edits it. Dated `docs/*.md` are never edited for hostnames (E00 §2.15 "history stays history"); no `paths-not-on-disk` block is added anywhere.
2. **"Outside comments" for a runbook.** The task's step-5 rule ("no worker artefact contains `terraform apply` / `aws … create|update|delete|put` outside comments") is meant for scripts and `.tf`; a deploy/rollback runbook that cannot spell `aws lambda update-alias` is useless. Resolution, mirroring E06's `secrets-rotation.md` and the `DRY_RUN`/`echo` exemption of E00 §5: a state-changing command may appear **only inside a fenced ```` ```bash ```` block whose first line is the comment `# who: supervisor`, `# who: owner` or `# who: ci`** — the comment assigns the block to a human or a workflow, never to a worker. Prose, inline code, blocks labelled `# who: anyone` (read-only commands) and blocks of any other language must not match the pattern; `eas …` appears only in `owner`/`ci` blocks; `terraform apply`/`terraform import` only in `supervisor`/`ci` blocks. The verify enforces exactly this (Acceptance 2, 5) and additionally parses every bash block with `bash -n` without running it.
3. **The DR drill's `PGHOST` override is a CLI overlay, not an env-file edit.** E00 §2.15 says "`ENV=staging` functions pointed at it by `PGHOST` override"; the E06 overlay projects only four file keys onto the worker, so editing `src_C/env/staging.env.json` would move `core-vpc-staging` and leave `worker-lambda-staging` on the real database — the smoke's publish round trip (E00 §2.12 check 4) would then hang. Resolution: the runbook reads each staging function's current environment into a shell variable (never printed), sets `PGHOST`, writes it back with `update-function-configuration`, publishes a version and moves the `staging` alias — the same overlay `deploy.sh` does, with one extra key — and restores by writing the recorded host back to `$LATEST` and moving the aliases back to the recorded versions (a plain alias move is not enough: the next `ENV=staging ./deploy.sh` reads `$LATEST`'s environment as its base).
4. **Manual deploys migrate after the alias move; CD migrates before it.** E00 §6 #17 orders `publish-version` → `db/migrate` on `core-vpc:<newVersion>` → `update-alias` for CD (`cd.yml`, E11). `src_C/deploy.sh` publishes and moves the alias in one call (`:44-51`), so the manual path is deploy → migrate, with the E12 schema gate answering `503 SERVER_NOT_READY_SCHEMA` on write routes in between. The runbook says exactly that and tells the operator to use CD when a migration ships.
5. **README structure is preserved.** `## Architecture` is inserted between `## 1. What is in this repository` and `## 2. Running it` (unnumbered, like `## Deployment`); `## Deployment` keeps its heading and place (`:71` on base, between §2 and §3); the numbered sections 1–6 keep their numbers and content; the only other README edit is the `:304` brand string. Removed lines are therefore a subset of base `:71-100` ∪ `:304` — the verify computes that set from `git show <merge-base>:README.md` and rejects any other `-` line.
6. **No file is executed.** `scripts/rollback.sh`, `scripts/smoke.sh`, `scripts/invoke-as-admin.sh`, `src_C/deploy.sh`, `infra/scripts/rds-snapshot.sh`, `frontend/deploy.sh`, `site/deploy.sh` are cited by path and by their usage line; the worker may read them (and should, to quote flags correctly) but never runs them, not even with `DRY_RUN=1` — nothing in this issue needs their output.

## Read first

1. `docs/delivery/r16-issues/E00-contracts.md` §0 (`:9-35`), §1.2 (`:71-108`), §2.1.3 (`:205-207`), §2.2 (`:213-221`), §2.3 (`:223-251`), §2.4 (`:253-279`), §2.6 (`:290-296`), §2.9 (`:334-349`), §2.10 (`:351-357`), §2.11 (`:359-379`), §2.12 (`:381-385`), §2.15 (`:407-409`), §3.3 (`:436-438`), §5 (`:470-482`), §6 #3, #7, #17, #18, #23 (`:487`, `:491`, `:502-503`, `:508`).
2. `docs/backend-architecture-review-2026-09-22.md` `:26-76` (§1 diagram + table — the source of the README "Architecture" section), `:132-136` (§2.4.2–2.4.6), `:177-241` (§3).
3. `README.md` whole file (345 lines), especially `:1-36`, `:71-100`, `:304`.
4. `frontend/tests/rootReadmePaths.test.ts:16-43`; `frontend/tests/docsPaths.test.ts:84-137`.
5. As merged on the integration branch (not on base): `infra/README.md` (all six sections), `infra/RUNBOOK.md`, `docs/runbooks/secrets-rotation.md` (its H2 headings — link to them, do not restate them), `scripts/rollback.sh` and `scripts/smoke.sh` (usage headers), `scripts/invoke-as-admin.sh:1-20`, `src_C/deploy.sh` (header + `deploy_one`), `infra/scripts/rds-snapshot.sh` (header), `.github/workflows/cd.yml` (job names and order), `ota.yml`, `terraform.yml`, `src_C/env/staging.env.json`, `mobile/src/config/hosts.ts`, `site/deploy.sh:1-10`, `src_C/Vpc/Authoring/DeckRollback.cs` (route + body + response), `src_C/Vpc/Internal/InternalEvents.cs` (the action names), `src_C/Shared/RecallSmith.Lambda.Common/Log.cs` (the JSON fields), `src_C/Shared/RecallSmith.Lambda.Common/RouteMetrics.cs` (`DeveloperCards` namespace, `EmitGauge`), `infra/modules/observability/alarms.tf` (the 13 `alarm_name`s), `dashboard.tf` (widget order), `api_logs.tf` (the access-log field names).
6. `src_C/deploy.sh:1-11`, `:38-51` (base); `frontend/deploy.sh:9-10`, `:26`; `mobile/app.json:5-7`, `:51-53`; `mobile/eas.json:52-79`, `:84`; `mobile/src/content/deckRepository.ts:26-33`; `src_C/Vpc/VpcFunction.cs:105`, `:119-131`, `:191-235`.

## Constraints

- **Scope (the ONLY files that may change):**
  1. `docs/runbooks/deploy-rollback.md` (new)
  2. `docs/runbooks/incident.md` (new)
  3. `docs/runbooks/dr-restore-drill.md` (new)
  4. `docs/runbooks/staging-promotion.md` (new)
  5. `README.md` (the `## Deployment` section, one new `## Architecture` section, the `:304` brand string; line 1 untouched)
  Nothing else: no `docs/runbooks/secrets-rotation.md` (E06 — link only), no other `docs/*.md`, no `infra/**` (not even `infra/README.md` §6), no `scripts/**`, no `src_C/**`, no `frontend/**`, no `mobile/**`, no `site/**`, no `.github/**`, no `snowflake/**`, no `LICENSE`.
- **WORKER SAFETY RULE (E00 §0, verbatim):** a worker may run read-only AWS CLI, `terraform init/validate/plan` (plan must be saved with -out and shown as JSON), but NEVER `terraform apply`, `terraform import` (state-changing), `aws ... create/update/delete/put`, `eas`, or any deploy script. The supervisor applies plans after each merge. Each infra brief's verify.sh therefore checks the PLAN (terraform show -json) against an explicit allow-list of resource addresses and actions — nothing else may appear as create/update/delete.
  For E15 this means: no AWS CLI is needed at all (every fact the runbooks state is in E00 and this brief); never run `terraform …`, `eas …`, `aws …`, `scripts/*.sh`, `src_C/deploy.sh`, `frontend/deploy.sh`, `site/deploy.sh`, `infra/scripts/*.sh` — not even with `DRY_RUN=1`. The commands you write are for the supervisor, the owner or a workflow, and each block says which (Context decision 2).
- **Frozen files (E00 §0) — zero diff:** `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`.
- **OTA rule:** nothing under `mobile/` changes; `mobile/package.json`, `mobile/package-lock.json`, `mobile/app.json` (`"version": "1.6.1"`), `mobile/eas.json` stay byte-identical; no `@sentry/*` anywhere.
- **Runbook form (E00 §2.15):** every runbook starts with one H1, then `## When to use` as the first H2 and `## Commands` as the second H2; hostnames are the ones of E00 §2.9 (`api.` / `cdn.` / `console.` / `api-staging.` / `cdn-staging.` / `console-staging.developercards.app`, apex + `www`), resource names the ones of E00 §2.1.3 and §0 (adopted names unchanged). English only (no CJK — the same rule `frontend/tests/uiLanguage.test.ts` applies to the console; the verify greps for it). ASCII quotes and dashes in commands (a curly quote inside a command line is a real bug). Placeholders inside a `bash` block are single-quoted `'<…>'` (`scripts/rollback.sh api '<version>'`, `MIGRATE_SECRET='<from /developercards/prod/migrate-secret>'`) so that every block still parses under `bash -n`; a bare `<version>` is a redirection to bash and fails the gate. Real values that are not secrets (`sg-…`, subnet group, hostnames, queue names) are written out.
- **Fenced blocks:** every fenced block in a runbook is ```` ```bash ````, ```` ```json ```` or ```` ```text ````. Every `bash` block's first line is `# who: supervisor`, `# who: owner`, `# who: ci` or `# who: anyone`; every `bash` block passes `bash -n`. State-changing commands (`terraform apply`/`import`, `aws … create-|update-|delete-|put-…`, `eas …`) only where Context decision 2 allows them. Secrets are `<…>` placeholders (`<32+ chars>`, `<from the owner's password manager>`); no example that looks like a value; nothing prints `Environment.Variables` to the terminal (captured into a shell variable, never `echo`ed) — the E00 §5 secret-leak grep runs over every added line.
- **README rules:** line 1 stays `# DeveloperCards` (E14's); the numbered sections keep their numbers and, except `## Deployment` and `:304`, their content (Context decision 5); every backticked repo path cited anywhere in `README.md` or a runbook exists on disk (the verify checks `frontend|mobile|src_C|pg-layer|snowflake|docs|.github|infra|scripts|site` prefixes — wider than `rootReadmePaths.test.ts`); at least 12 citations remain for the test's floor. No brand string `RecallSmith` in any added line except inside `RecallSmith.Lambda` (assembly/test-project paths) and the adopted resource names `recallsmith-publish-jobs`, `recallsmith-console-622994489535`, `recallsmith-tfstate-622994489535`, `snowflake-recallsmith-s3-role`.
- **Banned literals in any added line** (driver gate, case-insensitive): the six terms of B00 §0 (E00 §0 does not spell them; neither does this brief) — say "guard", "work around", "sidestep", "fallback", "spot". The two live env-var names E00 §0 calls unspellable are never written (the env→SSM map of E00 §2.6.2 is the only env-var list a runbook needs). No `@ts-ignore`, `@ts-expect-error`, `eslint-disable`, `.skip(`, `.only(`, `#pragma warning disable`, `[Fact(Skip` anywhere in the diff (the C07 suppression grep runs over the new files and the README `+` lines).
- **Never copy ExamTopics / SAA-C03 content; never quote `docs/home-review-and-launch-copy-2026-09-17.md:399-427`** (nothing in this issue needs that document).
- **Tests:** no test file changes. The verify runs `frontend/tests/docsPaths.test.ts` and `frontend/tests/rootReadmePaths.test.ts` (E00 §2.15, §3.3) against the symlinked `frontend/node_modules`; `npm install` is not needed and not allowed.
- Standing rules: no `git push`, no PR, never touch `main`, no deploy, no `expo`, no `eas`, no `npm install`, no `dotnet`, no `terraform`. Git only inside your worktree, never in `/Users/qc/src/recallsmith` or under `/Users/qc/Desktop`.

## Changes required

Common to all four runbooks: H1 `# <Title>` on line 1; H2 order `## When to use` (first), `## Commands` (second), then the sections named below; a one-line note under the H1 saying the document describes the system after Wave E (E00 names) and naming the date; `# who:` on every bash block; one sentence saying the legacy AWS hostnames stay served wherever the new hostnames are introduced; links between runbooks as relative Markdown links (`[incident](incident.md)`, `[secrets rotation](secrets-rotation.md)`, `[../../infra/README.md](../../infra/README.md)`).

1. **`docs/runbooks/deploy-rollback.md` (new)** — H1 `# Deploy and rollback`. Sections (H2, verbatim): `## When to use`, `## Commands`, `## Rollback matrix`, `## Migrations and rollback`, `## After a rollback`.
   - `## When to use`: every backend, console, landing-page, OTA and deck-content change; every alarm-driven "undo the last change" decision (link `[incident](incident.md)`); the preconditions (`infra/scripts/rds-snapshot.sh "pre-deploy-$(git rev-parse --short=7 HEAD)"` before any deploy that carries a migration; staging first — link `[staging promotion](staging-promotion.md)`).
   - `## Commands` — H3s in this order, each with the exact command(s):
     - `### Backend through CD (normal path)`: `.github/workflows/cd.yml` on push to `main`: `test` → `deploy-staging` → `smoke-staging` → `deploy-prod` (GitHub environment `production`, required reviewer = owner) → `smoke-prod` → `rollback` on failure; the order inside `deploy-prod` (`rds-snapshot.sh pre-deploy-<sha7>` → package → `update-function-code` → `publish-version` N → `db/migrate` invoked on `core-vpc:N` → `update-alias prod` → `frontend/deploy.sh` → `scripts/smoke.sh prod`) — prose plus one `# who: anyone` block with the two read-only `gh run` commands to watch a workflow (`gh run list --workflow cd.yml --limit 5`, `gh run watch '<run-id>'`).
     - `### Backend by hand (emergency path)`: `# who: supervisor` block: `export AWS_PROFILE=dev`, `ENV=prod ./src_C/deploy.sh` (packages, overlays env from `src_C/env/prod.env.json` + SSM `/developercards/prod/*`, publishes, moves `prod` on both functions, verifies `CodeSha256`), then `MIGRATE_SECRET='<from /developercards/prod/migrate-secret>' scripts/invoke-as-admin.sh core-vpc:prod POST /api/v1/admin/db/migrate` and `scripts/smoke.sh prod`; the sentence from Context decision 4 about the `503 SERVER_NOT_READY_SCHEMA` window and "use CD when a migration ships"; `ONLY=vpc` / `ONLY=worker` mentioned.
     - `### Console`: `# who: supervisor` block `CONSOLE_URL=https://console.developercards.app ./frontend/deploy.sh` (defaults after E09; bucket `recallsmith-console-622994489535`, distribution `E85FKUMZZWQWX`); CD does the same in `deploy-prod` and keeps `dist-<sha>.tgz` as a workflow artifact for 30 days.
     - `### Landing page`: `# who: owner` block `./site/deploy.sh` (bucket `developercards-site-622994489535`, `https://developercards.app`).
     - `### OTA (JavaScript-only mobile changes)`: what qualifies (no `mobile/package.json` / `app.json` / native change; same `"version"` → same runtime, `mobile/app.json:51-53` `runtimeVersion.policy = appVersion`); the tag path `git tag 'v<version>-<n>' && git push origin 'v<version>-<n>'` → `.github/workflows/ota.yml` → `eas update --channel production --message "<tag>"` (`# who: ci`); the manual path `# who: owner`: `cd mobile && eas update --channel production --message "<why>"`; the EAS `production` environment must carry `EXPO_PUBLIC_API_BASE=https://api.developercards.app` and `EXPO_PUBLIC_CONTENT_BASE_URL=https://cdn.developercards.app` (E00 §2.9.4) — the first OTA after E09 is what moves shipped clients to the new hostnames; the frozen default in `mobile/src/content/deckRepository.ts` keeps pointing at the CloudFront/execute-api names, which stay served. Binaries: `mobile/scripts/release/ios-build.sh` → `ios-submit.sh` → `asc-release.cjs` (owner; not CD; E00 §6 #18).
     - `### Rollback: API (core-vpc alias)`: `# who: anyone` block to find the previous version (`aws lambda list-versions-by-function --function-name core-vpc --query 'Versions[-5:].[Version,Description,LastModified]' --output table`; `aws lambda get-alias --function-name core-vpc --name prod`); `# who: supervisor` block `scripts/rollback.sh api '<version>'` (= `update-alias --function-version` + `CodeSha256` print) then `scripts/smoke.sh prod`. CD's `rollback` job does the same with the versions it captured (`PREV_VPC`, `PREV_WORKER`). RTO < 1 min.
     - `### Rollback: worker (worker-lambda alias)`: same shape with `scripts/rollback.sh worker '<version>'`; the ESM targets the `prod` alias after E03, so the next message runs the previous version; an in-flight job finishes on the version that took it; a `FAILED` row is re-acquirable (E03).
     - `### Rollback: console`: `scripts/rollback.sh console '<sha>'` (re-syncs the saved `dist-<sha>.tgz`) or, older than 30 days, `git checkout '<sha>' -- frontend && ./frontend/deploy.sh` (`# who: supervisor`).
     - `### Rollback: OTA`: `# who: owner` block `cd mobile && eas update:list --branch production --limit 10` → `eas update:republish --group '<group-id>'`; same-runtime rule; clients pick it up on the next launch; a binary cannot be rolled back (only a new submission).
     - `### Rollback: deck live pointer`: find the deck and its builds — `curl -fsS https://cdn.developercards.app/content/manifest.json` (`# who: anyone`) or `scripts/invoke-as-admin.sh core-vpc:prod GET /api/v1/admin/decks` + `… GET /api/v1/authoring/publish/jobs` (`aws lambda invoke` underneath, so `# who: supervisor`), then `# who: supervisor`: `printf '{"buildId":"<build-id>"}' > "$TMP/rollback.json"` + `scripts/invoke-as-admin.sh core-vpc:prod POST '/api/v1/admin/decks/<deckId>/rollback' "$TMP/rollback.json"` (E03: `decks.live_build_id` flips, manifest rebuilt in-process, response `{ deckId, slug, liveBuildId, previousBuildId, manifestRebuilt: true }`; 400 `VALIDATION_ERROR` when the build is not a `SUCCESS` row of that deck); `scripts/rollback.sh deck …` is the wrapper (quote its usage line from the file); clients see the previous build at the next manifest fetch; premium decks: same pointer, presigned URLs follow it.
     - `### Rollback: infrastructure`: revert the PR under `infra/` → `terraform.yml` plans on the PR and applies on merge (environment `infra-prod`); emergency: `# who: supervisor` `cd infra/envs/prod && terraform init && terraform plan -out=revert.tfplan && terraform show -json revert.tfplan | python3 ../../scripts/check-plan.py --summary` then `terraform apply revert.tfplan`; the state bucket `recallsmith-tfstate-622994489535` is versioned (previous state = previous object version); link `[../../infra/RUNBOOK.md](../../infra/RUNBOOK.md)`.
     - `### Rollback: hostnames`: nothing to roll back — `https://ktbq1sie2c.execute-api.ap-southeast-2.amazonaws.com`, `https://d1ditdi9jqpy6n.cloudfront.net` and `https://d12pfy1rhi3ekm.cloudfront.net` stay served (E00 §0, §6 #7); mobile falls back to the execute-api host on a network error (`mobile/src/config/hosts.ts`); the console's base is `frontend/.env.production` (re-point + redeploy); an OTA can re-point mobile through the EAS environment.
   - `## Rollback matrix`: a table `| What | Command | RTO | Caveat |` with one row each for API, worker, console, landing page, OTA, deck pointer, infrastructure, secrets (→ `[secrets rotation](secrets-rotation.md)`), database (→ `[DR restore drill](dr-restore-drill.md)`, PITR 14 d).
   - `## Migrations and rollback`: forward-only, additive (`if not exists`); an older code version runs on a newer schema (E12's gate compares `SchemaVersion.Required` ≤ current); a newer version on an older schema answers `503 SERVER_NOT_READY_SCHEMA` and the `developercards-prod-core-vpc-errors` / `SchemaBehind` signals fire; the CD order of E00 §6 #17; a bad migration's data fix is a PITR restore, never a "down" script.
   - `## After a rollback`: `scripts/smoke.sh prod`, watch the dashboard `developercards-prod` for 10 min, note the version pair in the PR/issue, open the fix-forward PR; if `infra/`, a second `terraform plan` must be empty.

2. **`docs/runbooks/incident.md` (new)** — H1 `# Incident response`. Sections (H2, verbatim): `## When to use`, `## Commands`, `## Alarm map`, `## Common causes`, `## After the incident`.
   - `## When to use`: an email from SNS topic `developercards-alerts` (alarm, RDS event, budget, scheduler on-failure destination), a user report, a failed `smoke-prod`.
   - `## Commands` — `### First five minutes`: open the dashboard `developercards-prod` (CloudWatch → Dashboards); `# who: anyone` block: `aws cloudwatch describe-alarms --state-value ALARM --query 'MetricAlarms[*].[AlarmName,StateUpdatedTimestamp]' --output table`; `aws lambda get-alias --function-name core-vpc --name prod --query FunctionVersion` (+ worker); `gh run list --workflow cd.yml --limit 3` (was there a deploy?); `curl -fsS https://api.developercards.app/health`; `# who: supervisor` block: `scripts/smoke.sh prod` and the direct `aws lambda invoke --function-name core-vpc:prod --cli-binary-format raw-in-base64-out --payload '{"source":"developercards.scheduler","action":"health/deep"}' "$TMP/deep.json" && cat "$TMP/deep.json"` (answers `ok`, `schemaVersion`, `required`). Decision line: "if it started at a deploy, roll back first, read logs second" → `[deploy and rollback](deploy-rollback.md)`. `### Reading logs`: the three JSON-indexed groups and their fields — `/aws/lambda/core-vpc` and `/aws/lambda/worker-lambda` lines are `{"ts","level","msg"}` or `Log.Event` objects with `tag` (`boot`, `unhandled`, `schema`, `rc-webhook`, `analytics-salt-missing`) plus the EMF lines (namespace `DeveloperCards`, metrics `Latency`, `Errors`, `OutboxPending`, `SchemaBehind`); `/aws/apigateway/developercards-api` lines carry `requestId`, `ip`, `requestTime`, `method`, `routeKey`, `path`, `status`, `integrationLatency`, `responseLatency`, `integrationError`, `authorizerError`, `userAgent` (E00 §2.4.4); `/aws/lambda/edge-public` is Node text. Two Logs Insights queries verbatim in a `# who: anyone` block using `aws logs start-query` + `aws logs get-query-results` (read-only): `fields @timestamp, level, msg, tag | filter level = "error" | sort @timestamp desc | limit 50` and `fields @timestamp, status, routeKey, path, integrationError, authorizerError, requestId | filter status >= 500 | sort @timestamp desc | limit 50`; plus how to pivot on one `requestId` across the access log and the function log.
   - `## Alarm map`: a table `| # | Alarm | Look at first (Logs Insights) | Look at second (dashboard widget) | Then |` with exactly thirteen rows, in this order and with these `alarm_name`s verbatim: 1 `developercards-prod-api-5xx`, 2 `developercards-prod-core-vpc-errors`, 3 `developercards-prod-worker-errors`, 4 `developercards-prod-core-vpc-throttles`, 5 `developercards-prod-worker-throttles`, 6 `developercards-prod-core-vpc-duration-p95`, 7 `developercards-prod-sqs-oldest-age`, 8 `developercards-prod-dlq-nonempty`, 9 `developercards-prod-rds-cpu`, 10 `developercards-prod-rds-free-storage`, 11 `developercards-prod-rds-connections`, 12 `developercards-prod-outbox-backlog`, 13 `developercards-prod-scheduler-errors`. Column 3 names the log group and a one-line query (e.g. 1: access log `filter status >= 500` grouped `stats count(*) by routeKey, integrationError`; 2: `/aws/lambda/core-vpc` `filter level = "error"` then `filter tag = "unhandled"` / `filter tag = "schema"`; 3: `/aws/lambda/worker-lambda` `filter level = "error"` + `filter msg like /JobId=/`; 4: access log `stats count(*) by ip, routeKey` last 15 min; 6: access log `stats pct(integrationLatency, 95) by routeKey`; 7/8: ESM state `aws lambda list-event-source-mappings --function-name worker-lambda:prod --query 'EventSourceMappings[0].State'` (`# who: anyone`) + a peek at `developercards-publish-jobs-dlq` with `aws sqs receive-message --queue-url '<dlq url>' --max-number-of-messages 1 --visibility-timeout 0` (`# who: supervisor` — a receive is a read with a side effect), then the worker log by `JobId`; 9/11: RDS Performance Insights top SQL + `stats count(*) by routeKey` on the access log; 10: `FreeStorageSpace` trend, largest tables, outbox retention (E13 deletes sent rows > 30 d); 12: `internal:outbox/publish` in the Latency-by-Route widget + `filter msg like /outbox/`; 13: the SNS mail body carries the failed payload (`event_invoke_config` on-failure destination), `aws scheduler get-schedule --group-name developercards-prod --name outbox-publish` (or `snapshot-import`, `reap-orphans`), core-vpc log around the schedule time). Column 4 uses the widget names of E00 §2.4.5 (`API Count/4xx/5xx`, `API Latency p50/p95`, `core-vpc Invocations/Errors/Throttles`, `core-vpc Duration p50/p95/max`, `worker Invocations/Errors/Duration`, `SQS age + DLQ visible`, `RDS CPU/Connections`, `RDS FreeStorageSpace/FreeableMemory`, `OutboxPending`, `Latency p95 by Route`). Column 5 names the runbook section to jump to (`deploy-rollback.md` API/worker alias, deck pointer; `#dlq-redrive`; `#rds`; `#scheduler`; `staging-promotion.md#migration-rules` when `tag = "schema"`; `secrets-rotation.md` when the webhook log shows `impl` + 401).
   - `## Common causes` — H3s `### API and core-vpc`, `### Worker, queue and DLQ` (anchor text "DLQ redrive": `# who: supervisor` `aws sqs start-message-move-task --source-arn '<dlq arn>'` after the cause is fixed; `scripts/invoke-as-admin.sh core-vpc:prod POST /api/v1/admin/publish/reap` for orphaned rows; the reaper schedule runs every 5 min anyway), `### RDS` (connections budget: `PG_MAX=1` × reserved concurrency 40 + worker 2 + staging functions on the same instance; storage: `aws rds modify-db-instance --db-instance-identifier developercards --allocated-storage '<GiB>'` `# who: supervisor`, gp3 grows online; CPU: Performance Insights), `### Scheduler and outbox` (the three schedules, `maximum_retry_attempts = 1`, on-failure → SNS; re-run by hand = `aws lambda invoke` with the internal event, `# who: supervisor`), `### Webhooks` (RevenueCat 401/503 semantics of E00 §2.7; 503 → RevenueCat retries; rotation → `secrets-rotation.md`).
   - `## After the incident`: a timeline (alarm time, first action, resolution), the version pair, "what would have caught it earlier", an issue for the fix; alarms return to OK by themselves (`ok_actions` mail).

3. **`docs/runbooks/dr-restore-drill.md` (new)** — H1 `# Disaster recovery: restore drill`. Sections (H2, verbatim): `## When to use`, `## Commands`, `## Real restore`, `## What an RDS restore does not bring back`, `## Drill log`.
   - `## When to use`: quarterly (first week of Jan/Apr/Jul/Oct), after any change to `modules/data`, and — for real — a bad migration or data loss (→ `## Real restore`). Facts: instance `developercards`, automated backups 14 d (E02) in window `12:55-13:25` UTC, PITR granularity 5 min, snapshot names `rds:developercards-YYYY-MM-DD-HH-MM`, `DeletionProtection` on (E02), `final_snapshot_identifier = "developercards-final-tf"` (E01); RPO = snapshot age (≤ 24 h for a snapshot restore, ≤ 5 min for PITR); RTO = what the drill measures.
   - `## Commands` — numbered H3 steps, every block `# who: supervisor` unless read-only:
     1. `### 1. Pick the snapshot` — `# who: anyone`: `SNAP=$(aws rds describe-db-snapshots --db-instance-identifier developercards --snapshot-type automated --query 'sort_by(DBSnapshots,&SnapshotCreateTime)[-1].DBSnapshotIdentifier' --output text)`; `date -u` recorded as T0.
     2. `### 2. Restore to developercards-drill` — `aws rds restore-db-instance-from-db-snapshot --db-instance-identifier developercards-drill --db-snapshot-identifier "$SNAP" --db-instance-class db.t4g.micro --db-subnet-group-name default-vpc-04af44dd8f5f48717 --vpc-security-group-ids sg-0d2541aec08b1a215 sg-0fbc6607e6473cbd3 --db-parameter-group-name default.postgres17 --no-publicly-accessible --no-multi-az --no-deletion-protection --tags Key=Project,Value=DeveloperCards Key=Env,Value=drill` then `aws rds wait db-instance-available --db-instance-identifier developercards-drill` (≈ 10 min; the snapshot is encrypted with the instance's KMS key, the restore inherits it); `DRILL_HOST=$(aws rds describe-db-instances --db-instance-identifier developercards-drill --query 'DBInstances[0].Endpoint.Address' --output text)`.
     3. `### 3. Record what staging points at` — for `fn in core-vpc-staging worker-lambda-staging`: `aws lambda get-alias --function-name "$fn" --name staging --query FunctionVersion --output text` into `PREV_<fn>`; `PROD_HOST=$(aws lambda get-function-configuration --function-name core-vpc-staging --query 'Environment.Variables.PGHOST' --output text)` (a hostname, not a secret).
     4. `### 4. Point staging at the drill instance` — the CLI overlay of Context decision 3, per function: `cur="$(aws lambda get-function-configuration --function-name "$fn" --query Environment.Variables --output json)"` (captured, never printed), `merged="$(jq -c --arg h "$DRILL_HOST" '.PGHOST = $h' <<<"$cur")"`, `aws lambda update-function-configuration --function-name "$fn" --environment "$(jq -cn --argjson v "$merged" '{Variables: $v}')" --query LastUpdateStatus --output text`, `aws lambda wait function-updated --function-name "$fn"`, `ver=$(aws lambda publish-version --function-name "$fn" --description "drill $SNAP" --query Version --output text)`, `aws lambda update-alias --function-name "$fn" --name staging --function-version "$ver"`. The role `developercards_app_staging` and its password live inside the snapshot (roles are instance-wide), so `/developercards/staging/pg-password` keeps working unless it was rotated after the snapshot — in that case run the app-role rotation of `[secrets rotation](secrets-rotation.md)` against the drill instance first.
     5. `### 5. Smoke the drill` — `scripts/smoke.sh staging` (E11: `/health`, `health/deep` with `schemaVersion >= required`, manifest, a publish round trip on `developercards-publish-jobs-staging`, cleanup); `scripts/invoke-as-admin.sh core-vpc-staging:staging GET /api/v1/authoring/dashboard`; `date -u` as T1; **RTO = T1 − T0**.
     6. `### 6. Restore staging` — per function: overlay `$LATEST` back with `PGHOST = $PROD_HOST` (same three commands, no publish) and `aws lambda update-alias --function-name "$fn" --name staging --function-version "$PREV_<fn>"`; `scripts/smoke.sh staging` again.
     7. `### 7. Delete the drill instance` — `aws rds delete-db-instance --db-instance-identifier developercards-drill --skip-final-snapshot --delete-automated-backups` + `aws rds wait db-instance-deleted --db-instance-identifier developercards-drill` (cost ≈ 1 h of `db.t4g.micro`).
     8. `### 8. Record` — append a row to `## Drill log`.
   - `## Real restore`: (a) bad migration / bad data → `aws rds restore-db-instance-to-point-in-time --source-db-instance-identifier developercards --target-db-instance-identifier developercards-restored --restore-time '<ISO-8601 UTC>' …` (same network flags as step 2), then the step-4 overlay on `core-vpc` / `worker-lambda` with `ENV=prod` aliases `prod` (or `aws rds modify-db-instance --new-db-instance-identifier` rename dance — say which and why: overlay first, rename later, both `# who: supervisor`); the old instance is kept until the restore is verified; pause CD (`gh workflow disable cd.yml`, `# who: supervisor`) during the swap. (b) Region loss: no cross-region snapshot copy exists in this wave (the review's §3 cross-region snapshot row is post-wave; say so plainly, in English), Terraform in `infra/` rebuilds every other resource in another region only after the modules gain region-neutral names — post-wave; Lambda versions are the code backup; S3 content is versioned since E02 (`aws s3api list-object-versions --bucket core-vpc --prefix content/manifest.json` recovers a previous manifest).
   - `## What an RDS restore does not bring back`: S3 objects (content bucket versioning, E02; previous `manifest.json` version), SSM parameters (`/developercards/*`, re-put from the password manager), Cognito users (no export exists — post-wave), Lambda environment (overlaid on the next deploy), EAS updates, the Terraform state (`recallsmith-tfstate-622994489535`, versioned).
   - `## Drill log`: table `| Date | Snapshot | T0 → T1 (RTO) | Smoke | Notes |` with one placeholder row `| (none yet) | | | | first drill is due the quarter after Wave E ships |`.

4. **`docs/runbooks/staging-promotion.md` (new)** — H1 `# Staging promotion`. Sections (H2, verbatim): `## When to use`, `## Commands`, `## Migration rules`, `## Infra promotion`, `## Mobile promotion`, `## Staging hostnames`.
   - `## When to use`: every change that reaches prod goes through staging first; migrations always (E00 §2.10.5); a PR that touches `src_C/Vpc/Db/Migrations/` is not merged until `smoke-staging` is green on it.
   - `## Commands` — `### The CD path (cd.yml)` in prose: push to `main` → `test` (`ci.yml` via `workflow_call`) → `deploy-staging` (OIDC role `developercards-gha-staging`; `ENV=staging ./src_C/deploy.sh` with `PUBLISH_ALIAS=staging` on `core-vpc-staging` / `worker-lambda-staging`; `db/migrate` invoked on the new version before the alias moves; `frontend/deploy.sh` with `CONSOLE_BUCKET=developercards-console-staging` and `CONSOLE_URL=https://console-staging.developercards.app`) → `smoke-staging` (`scripts/smoke.sh staging`, the five checks of E00 §2.12 incl. the `smoke-<sha7>` publish round trip) → `deploy-prod` (environment `production`, required reviewer = owner; `infra/scripts/rds-snapshot.sh pre-deploy-<sha7>`; same deploy against `core-vpc` / `worker-lambda`, alias `prod`; previous versions captured) → `smoke-prod` → `rollback` (`if: failure()`, `scripts/rollback.sh api $PREV_VPC && scripts/rollback.sh worker $PREV_WORKER`). One `# who: anyone` block: `gh run list --workflow cd.yml --limit 5`, `gh run view <id> --log-failed`. `### The manual path` (`# who: supervisor`, in this order): `export AWS_PROFILE=dev`; `ENV=staging ./src_C/deploy.sh`; `MIGRATE_SECRET='<from /developercards/staging/migrate-secret>' scripts/invoke-as-admin.sh core-vpc-staging:staging POST /api/v1/admin/db/migrate`; `scripts/smoke.sh staging`; `CONSOLE_BUCKET=developercards-console-staging CONSOLE_DISTRIBUTION_ID='<staging distribution id>' CONSOLE_URL=https://console-staging.developercards.app ./frontend/deploy.sh`; then prod: `infra/scripts/rds-snapshot.sh "pre-deploy-$(git rev-parse --short=7 HEAD)"`; `ENV=prod ./src_C/deploy.sh`; `MIGRATE_SECRET='<from /developercards/prod/migrate-secret>' scripts/invoke-as-admin.sh core-vpc:prod POST /api/v1/admin/db/migrate`; `scripts/smoke.sh prod`; `./frontend/deploy.sh`. Note the 503 window of Context decision 4.
   - `## Migration rules`: files `src_C/Vpc/Db/Migrations/NNN_<name>.sql`, additive, `if not exists`, one statement per concern, no `CREATE INDEX CONCURRENTLY` (the runner wraps each file in a transaction); bump `SchemaVersion.Required` (`src_C/Shared/RecallSmith.Lambda.Db/SchemaVersion.cs`) in the same PR; the gate (`503 SERVER_NOT_READY_SCHEMA` on writes, worker retries, `SchemaBehind` gauge, `developercards-prod-core-vpc-errors`); `lock_timeout = '5s'` → 409 `MIGRATION_IN_PROGRESS` (retry); `dryRun: true` body to list pending files; expand/contract in two releases when a column is removed; the pre-deploy snapshot label convention `pre-deploy-<sha7>`.
   - `## Infra promotion`: `infra/envs/staging` and `infra/envs/prod` are separate states in `recallsmith-tfstate-622994489535`; a PR under `infra/**` gets a plan comment per root from `terraform.yml` (role `developercards-gha-plan`); merge → environment `infra-prod` approval → apply; the second plan must be empty; link `[../../infra/README.md](../../infra/README.md)` §5 and `[../../infra/RUNBOOK.md](../../infra/RUNBOOK.md)`; the supervisor's local procedure (`terraform plan -out` → `check-plan.py` → `terraform apply <plan>`) in a `# who: supervisor` block.
   - `## Mobile promotion`: internal build `eas build --profile staging-internal-release --platform ios` (channel `staging` after E10, EAS environment `preview` with `EXPO_PUBLIC_API_BASE=https://api-staging.developercards.app`), `# who: owner`; promotion of JS-only changes = the tag path of `deploy-rollback.md` (`ota.yml`); binaries = the owner's `mobile/scripts/release/` scripts.
   - `## Staging hostnames`: table `| Purpose | Staging | Production |` with `https://api-staging.developercards.app` / `https://api.developercards.app`, `https://cdn-staging.developercards.app` / `https://cdn.developercards.app`, `https://console-staging.developercards.app` / `https://console.developercards.app`; functions `core-vpc-staging` / `core-vpc`, `worker-lambda-staging` / `worker-lambda`; queue `developercards-publish-jobs-staging` / `recallsmith-publish-jobs`; buckets `developercards-content-staging` / `core-vpc`, `developercards-premium-staging` / `core-vpc-premium`, `developercards-console-staging` / `recallsmith-console-622994489535`; database `developercards_staging` / `developercards_db` (same instance `developercards`); SSM `/developercards/staging/*` / `/developercards/prod/*`; Cognito clients `console-staging` + `MobileDeveloperCards-staging` / `6lkofepp2llp6v4nueg52mcm5v` + `7agirr7f56r9k5p6v6o63al1on` (same two pools).

5. **`README.md`** — three edits, nothing else:
   a. Insert `## Architecture` directly before `## 2. Running it` (`:37`). Content: one paragraph (request path in English: mobile and console → API Gateway HTTP API `developercards-api` with two Cognito JWT authorizers → Lambda `core-vpc` (.NET 8, arm64, in the VPC, alias `prod`) → RDS PostgreSQL `developercards`; publish = `deck_publishes` row + SQS `recallsmith-publish-jobs` → `worker-lambda` → immutable `builds/<buildId>/` under `s3://core-vpc/content/` + `manifest.json` → CloudFront; premium decks by presigned URL from `core-vpc-premium`; review events → analytics bucket → Snowflake marts → snapshot import); then a table `| Layer | What | Where it is defined |` with exactly these first-column rows in this order: `Clients`, `Edge`, `API`, `Compute`, `Data`, `Async publish`, `Analytics`, `Identity`, `Observability`, `Delivery`, `Infrastructure as code` — the third column cites repo paths (`mobile/`, `frontend/`, `infra/modules/edge/`, `infra/modules/api/`, `src_C/Vpc/`, `src_C/Worker/`, `infra/modules/data/`, `snowflake/`, `infra/modules/identity/`, `infra/modules/observability/`, `.github/workflows/cd.yml`, `infra/envs/prod/`) — every cited path must exist; then `### Hostnames` with the table `| Purpose | Hostname | Legacy name (still served) |`: API `https://api.developercards.app` / `https://ktbq1sie2c.execute-api.ap-southeast-2.amazonaws.com` (stages `$default` and `dev`); content CDN `https://cdn.developercards.app` / `https://d1ditdi9jqpy6n.cloudfront.net`; console `https://console.developercards.app` / `https://d12pfy1rhi3ekm.cloudfront.net`; landing page `https://developercards.app` + `https://www.developercards.app` / — ; staging `https://api-staging.developercards.app`, `https://cdn-staging.developercards.app`, `https://console-staging.developercards.app` / — ; one sentence: the legacy names are pinned by shipped binaries (`mobile/src/content/deckRepository.ts`) and are never removed; then one sentence linking `infra/README.md` (what Terraform manages, what stays out of state) and `infra/RUNBOOK.md`.
   b. Rewrite `## Deployment` (base `:71-100`; keep the heading text and position). New content: CD in one paragraph (`.github/workflows/cd.yml`: push to `main` → tests → staging → smoke → owner approval → prod → smoke → automatic alias rollback; `.github/workflows/terraform.yml` for `infra/`; `.github/workflows/ota.yml` on tag `v*`; OIDC roles, no long-lived keys — GitHub holds no AWS secret); the manual scripts by path (`src_C/deploy.sh` — packages, overlays the environment from `src_C/env/prod.env.json` + SSM, publishes, moves the alias; `frontend/deploy.sh`; `site/deploy.sh`; `scripts/rollback.sh`; `scripts/smoke.sh`; `infra/scripts/rds-snapshot.sh`); the three console bullets kept verbatim (`**SPA fallback.**`, `**Cache split.**`, `**Build-time config.**` — base `:78-87`); a closing list linking the five runbooks: `docs/runbooks/deploy-rollback.md`, `docs/runbooks/incident.md`, `docs/runbooks/dr-restore-drill.md`, `docs/runbooks/staging-promotion.md`, `docs/runbooks/secrets-rotation.md` (one line each, what it is for). No `aws …` command in the README (the commands live in the runbooks): the base lines `Deploying is two commands from …`, `aws s3 sync dist s3://recallsmith-console-622994489535 --delete …` and `CI does not deploy …` are gone (the last one is no longer true).
   c. `:304`: `the RecallSmith API and the Cognito hosted UI` → `the DeveloperCards API and the Cognito hosted UI` (one line).
   Removed lines ⊆ base `:71-100` ∪ `:304`; line 1 untouched; `## Deployment` appears exactly once; heading order `## 1.` < `## Architecture` < `## 2. Running it` < `## Deployment` < `## 3. How one request flows`.

## Acceptance

Run from the worktree root. `docs/delivery/r16-issues/E15.verify.sh` re-runs exactly these.

1. Scope files exist (exit 0): the four runbooks exist and are non-empty; prerequisites on the integration branch (absent on base): `README.md:1` is `# DeveloperCards` (E14), `docs/runbooks/secrets-rotation.md` (E06), `infra/README.md` + `infra/RUNBOOK.md` (E01), `infra/scripts/rds-snapshot.sh` (E02), `scripts/invoke-as-admin.sh` (E06), `scripts/rollback.sh` + `scripts/smoke.sh` + `.github/workflows/cd.yml` + `ota.yml` + `terraform.yml` (E11), `mobile/src/config/hosts.ts` + `site/deploy.sh` (E09), `src_C/env/staging.env.json` (E10), `src_C/Vpc/Internal/InternalEvents.cs` (E12), `src_C/Vpc/Authoring/DeckRollback.cs` (E03).
2. Literal guards (exit 0):
   - Every runbook: line 1 is `# …`; the first two H2s are `## When to use` then `## Commands`; H2 set per Changes 1–4 verbatim; no CJK character; every fenced block is `bash`/`json`/`text` and fences are balanced; every `bash` block's first line matches `# who: (supervisor|owner|ci|anyone)`; every `bash` block passes `bash -n`; every relative Markdown link `(…​.md)` resolves; every backticked repo path (`frontend|mobile|src_C|pg-layer|snowflake|docs|.github|infra|scripts|site` prefix, trailing `:lines` stripped) exists on disk.
   - `deploy-rollback.md`: `scripts/rollback.sh api`, `scripts/rollback.sh worker`, `scripts/rollback.sh console`, `eas update:republish --group`, `eas update --channel production`, `/api/v1/admin/decks/`, `/rollback`, `"buildId"`, `previousBuildId`, `manifestRebuilt`, `ENV=prod ./src_C/deploy.sh`, `PUBLISH_ALIAS=staging`, `/api/v1/admin/db/migrate`, `SERVER_NOT_READY_SCHEMA`, `scripts/smoke.sh prod`, `infra/scripts/rds-snapshot.sh`, `pre-deploy-`, `frontend/deploy.sh`, `site/deploy.sh`, `.github/workflows/cd.yml`, `.github/workflows/ota.yml`, `.github/workflows/terraform.yml`, `EXPO_PUBLIC_API_BASE=https://api.developercards.app`, `EXPO_PUBLIC_CONTENT_BASE_URL=https://cdn.developercards.app`, `runtimeVersion`, `mobile/scripts/release/ios-build.sh`, `recallsmith-tfstate-622994489535`, `check-plan.py`, `https://ktbq1sie2c.execute-api.ap-southeast-2.amazonaws.com`, `https://d1ditdi9jqpy6n.cloudfront.net`, `https://d12pfy1rhi3ekm.cloudfront.net`, `mobile/src/config/hosts.ts`, `secrets-rotation.md`, `dr-restore-drill.md`, `incident.md`, `staging-promotion.md`, `| What | Command | RTO | Caveat |`.
   - `incident.md`: the thirteen alarm names verbatim, each as the numbered row `| n | <alarm> |` of the `## Alarm map` table in the order of Changes 2 (exactly thirteen numbered table rows in the file); `developercards-alerts`, `developercards-prod` (dashboard), `/aws/lambda/core-vpc`, `/aws/lambda/worker-lambda`, `/aws/apigateway/developercards-api`, `filter level = "error"`, `filter status >= 500`, `tag = "unhandled"`, `tag = "schema"`, `requestId`, `integrationError`, `authorizerError`, `routeKey`, `OutboxPending`, `SchemaBehind`, `"action":"health/deep"`, `schemaVersion`, `list-event-source-mappings`, `start-message-move-task`, `/api/v1/admin/publish/reap`, `developercards-publish-jobs-dlq`, `Performance Insights`, `PG_MAX=1`, `outbox-publish`, `snapshot-import`, `reap-orphans`, `scripts/smoke.sh prod`, `https://api.developercards.app/health`, `deploy-rollback.md`, `secrets-rotation.md`, `staging-promotion.md`.
   - `dr-restore-drill.md`: `developercards-drill`, `restore-db-instance-from-db-snapshot`, `--snapshot-type automated`, `sort_by(DBSnapshots,&SnapshotCreateTime)[-1]`, `default-vpc-04af44dd8f5f48717`, `sg-0d2541aec08b1a215`, `sg-0fbc6607e6473cbd3`, `default.postgres17`, `wait db-instance-available`, `Endpoint.Address`, `core-vpc-staging`, `worker-lambda-staging`, `--name staging`, `.PGHOST = $h`, `update-function-configuration`, `wait function-updated`, `publish-version`, `update-alias`, `scripts/smoke.sh staging`, `/api/v1/authoring/dashboard`, `RTO`, `delete-db-instance`, `--skip-final-snapshot`, `wait db-instance-deleted`, `restore-db-instance-to-point-in-time`, `list-object-versions`, `12:55-13:25`, `secrets-rotation.md`, `| Date | Snapshot | T0 → T1 (RTO) | Smoke | Notes |`, `(none yet)`; every line containing `Environment.Variables` captures it into a shell variable (`grep -F 'Environment.Variables' | grep -Ev '=\$\(|="\$\(|^\s*#'` is empty — prose says "the function environment", never the literal).
   - `staging-promotion.md`: `deploy-staging`, `smoke-staging`, `deploy-prod`, `smoke-prod`, `developercards-gha-staging`, `developercards-gha-plan`, `infra-prod`, `ENV=staging ./src_C/deploy.sh`, `ENV=prod ./src_C/deploy.sh`, `core-vpc-staging:staging`, `/api/v1/admin/db/migrate`, `scripts/smoke.sh staging`, `scripts/smoke.sh prod`, `pre-deploy-`, `developercards-console-staging`, `https://console-staging.developercards.app`, `https://api-staging.developercards.app`, `https://cdn-staging.developercards.app`, `SchemaVersion.Required`, `SERVER_NOT_READY_SCHEMA`, `MIGRATION_IN_PROGRESS`, `dryRun`, `if not exists`, `staging-internal-release`, `developercards_staging`, `developercards-publish-jobs-staging`, `MobileDeveloperCards-staging`, `console-staging`, `infra/envs/staging`, `../../infra/README.md`, `| Purpose | Staging | Production |`.
   - `README.md`: line 1 `# DeveloperCards`; `## Architecture` exactly once, `### Hostnames` exactly once, `## Deployment` exactly once; heading order of Changes 5; the eleven `Layer` rows in order; `| Purpose | Hostname | Legacy name (still served) |`; the eight hostnames/legacy names; `infra/README.md`, `infra/RUNBOOK.md`, `.github/workflows/cd.yml`, `.github/workflows/terraform.yml`, `.github/workflows/ota.yml`, `src_C/deploy.sh`, `src_C/env/prod.env.json`, `frontend/deploy.sh`, `site/deploy.sh`, `scripts/rollback.sh`, `scripts/smoke.sh`, `infra/scripts/rds-snapshot.sh`, the five `docs/runbooks/*.md` links, `mobile/src/content/deckRepository.ts`; the three console bullets (`**SPA fallback.**`, `**Cache split.**`, `**Build-time config.**`) still present; absent: `aws s3 sync dist s3://recallsmith-console-622994489535`, `CI does not deploy`, `Deploying is two commands`, `the RecallSmith API`; no added README line matches `aws +[a-z0-9-]+ +[a-z-]+` (commands live in the runbooks); at least 12 `rootReadmePaths`-style citations.
   - Brand: no added line (README `+` lines, the four runbooks) contains `RecallSmith` outside `RecallSmith.Lambda`; no added line contains `recallsmith` outside `recallsmith-publish-jobs`, `recallsmith-console-622994489535`, `recallsmith-tfstate-622994489535`, `snowflake-recallsmith-s3-role`, `ChuanQiao1128/recallsmith`.
   - Suppression grep (`C07.verify.sh:151` verbatim) over the four runbooks and the README `+` lines is empty; the E00 §5 secret-leak grep over the same set is empty; the two unspellable names are absent (the verify checks that no added line contains an `=` assignment of any `PG*`/`*_SECRET`/`RC_WEBHOOK_AUTH_*`/`ANALYTICS_USER_SALT` name to anything other than a `<…>` placeholder or `$(`).
3. Docs gates (exit 0): `python3` fence/heading parser over the four runbooks (balanced fences, languages ∈ {bash, json, text}, `# who:` on every bash block, H2 order); `bash -n` on every extracted bash block; `python3 -c 'import json'` on every `json` block; no root (`infra`, `src_C`, `frontend`, `mobile`, `site`, `.github`, `scripts`, `snowflake`) shows any diff against the merge base (there is nothing to gate — a diff there fails the scope check instead).
4. Targeted tests (exit 0): `( cd frontend && npx vitest run tests/docsPaths.test.ts tests/rootReadmePaths.test.ts --reporter=dot )`.
5. Scope + frozen + OTA + apply guard (exit 0): `git diff --name-only <merge-base>` ∪ the pathspec-scoped untracked scan of `docs README.md infra scripts src_C frontend/src frontend/tests mobile/src mobile/tests site .github snowflake` contains nothing outside the five scope files (+ `docs/delivery/r16-issues/`); `docs/runbooks/secrets-rotation.md`, every other `docs/*.md`, the three frozen mobile files, `mobile/package.json`, `mobile/package-lock.json`, `mobile/app.json`, `mobile/eas.json`, `LICENSE` are zero-diff; `grep -Fq '"version": "1.6.1"' mobile/app.json`; no `@sentry` under `mobile/src`; README removed lines ⊆ base `:71-100` ∪ `:304`; apply guard: with `APPLY_RE='terraform +(apply|import)|aws +[a-z0-9-]+ +(create|update|delete|put)-'`, no match in runbook prose (outside fences), in any `json`/`text` block, in any `# who: anyone` block (these three are checked block by block in step 3), in any added README line, or in `E15.verify.sh` outside its comments; `eas ` appears only in `owner`/`ci` blocks and `terraform +(apply|import)` only in `supervisor`/`ci` blocks (step 3).

## Verify

```bash
BASE=delivery/r16-e-prod bash docs/delivery/r16-issues/E15.verify.sh
```
Steps 1–3 are file/literal/parse checks (seconds; `bash -n` and `python3` only, nothing is executed). Step 4 runs two vitest files against the symlinked `frontend/node_modules` (< 10 s). Step 5 is git only. No AWS CLI, no Terraform, no Docker, no network; total < 1 min. The driver additionally runs its diff-scoped banned-term grep and suppression scan; no root gate fires because no root changes (a change under `frontend/`, `src_C/`, `infra/` or `mobile/` is a scope failure here).

**The supervisor applies** nothing for this issue (no plan, no deploy). After the merge the supervisor reads the four runbooks once against the live account (every hostname resolves after E09's second apply; every script path exists) and files a follow-up issue for anything the runbooks describe that the account does not yet do — the first DR drill is scheduled for the quarter after the wave ships and its row goes into `## Drill log`.

## Do NOT

- Do NOT edit `docs/runbooks/secrets-rotation.md` (E06 owns it; link to its H2s), `infra/README.md` §6 or any other file under `infra/`, `scripts/`, `.github/`, `src_C/`, `frontend/`, `mobile/`, `site/`, `snowflake/`; do NOT edit any dated `docs/*.md` for hostnames (history stays history); do NOT add a `paths-not-on-disk` block anywhere.
- Do NOT touch `README.md:1` (E14's title), renumber sections 1–6, or remove lines outside base `:71-100` and `:304`.
- Do NOT put a state-changing command outside a `# who: supervisor|owner|ci` bash block, a `# who: anyone` label on a block that writes anything, or an `eas` command in a non-owner/non-ci block; do NOT write a command with a real or realistic secret value, and do NOT write a command that prints `Environment.Variables` to the terminal.
- Do NOT describe the two unspellable env-var names, the account's stray keys (`ENI_BUMP`, `VERSION_BUMP`, `ADMIN_DEBUG`), or today's pre-wave state as current (the runbooks describe the post-wave system; one line per runbook says so).
- Do NOT invent flags for `scripts/rollback.sh`, `scripts/smoke.sh`, `scripts/invoke-as-admin.sh`, `src_C/deploy.sh`, `infra/scripts/rds-snapshot.sh`, `frontend/deploy.sh`, `site/deploy.sh`: read each file's usage header on the integration branch and quote it; where E11's `scripts/rollback.sh deck` argument shape differs from this brief, the file wins and the brief's endpoint call (`POST /api/v1/admin/decks/{deckId}/rollback`) is documented beside it.
- Do NOT run `aws …`, `terraform …`, `eas …`, `gh …`, `npm install`, `dotnet …`, any `.sh` in the repo (not even with `DRY_RUN=1`), or git in `/Users/qc/src/recallsmith`.
- Do NOT write Chinese in the runbooks or the README (the review's Chinese row titles are paraphrased in English), and do NOT quote `docs/home-review-and-launch-copy-2026-09-17.md`.
