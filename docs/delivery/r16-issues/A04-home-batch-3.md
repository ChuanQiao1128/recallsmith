# A04 — Home batch 3: first-run CTA → Draw (F3), hero pack = primary decision (F12), render the goal line (F16)

slug: `home-batch-3` · deps: A03 (and A02, already on the integration branch) · timeout: 60 min

> Line numbers below are as of base `c9ddc8d` on `delivery/r16-a-home`. A02 (HomeScreen.tsx) and A03 (homeSelectors.ts) land before your worktree is cut, so lines drift by a few dozen. Every anchor also quotes the identifier or literal to `grep -n` for — locate by that, not by the number.

## Context

Home's primary button and the tappable hero pack are two independent decision trees: a brand-new user with 3 starter pulls (`STARTER_PULL_GRANT = 3`, `mobile/src/features/gacha/rewards/rewardWallet.ts:37`) and an installable-but-not-installed deck gets `first_run` → `'Open library'`/`nav: 'library'` (`mobile/src/features/gacha/selectors/homeSelectors.ts:246-248` in `inferStatusKind`, `:306-313` in `mapStatusToCta`, and the override `:658-665` in `buildHomeVM`) while the header/hero already say "A reward draw is ready" and the badge says "3 pulls ready" (`buildDrawVM`, `:218-223`). The pack's `handleFeaturedPackPress` (`mobile/src/screens/HomeScreen.tsx:631-658`) routes by `walletHasPulls` (`:628-630`) so pulls outrank due study there, and its `accessibilityLabel={\`Open ${featuredDeck.title} pack\`}` (`:675`) names neither destination. The spec'd minimum-goal line (`buildGoalVM`, `homeSelectors.ts:410-416`, attached as `vm.goal` at `:705`; gacha-v7.md §3.1.1 item 3) is built but never rendered, and `HomeHero.tsx` is an unimported component (grep `HomeHero` → only `contracts.ts` types and the file itself). Read first: `homeSelectors.ts` `buildHomeVM` (`:606-745`), `HomeScreen.tsx` `handlePrimaryCta` (`:339-376`), `handleDeckPress` (`:377-408`), the hero IIFE from `const realRows` (`:541`) through the `home-featured-pack` Pressable (`:672-702`) and the action group (`:730-762`), the hidden probes (`:868-899`), and both test files in scope end to end. Spec rows: `docs/home-review-and-launch-copy-2026-09-17.md` §1.3 rows F3 (`:50`), F12 (`:59`), F16 (`:63`) and the Home v5 sketch §1.4 (`:67-98`).

## Constraints

- Scope (only these paths may change; nothing else, no new files outside them):
  `mobile/src/features/gacha/selectors/homeSelectors.ts`, `mobile/src/screens/HomeScreen.tsx`, `mobile/src/features/gacha/components/HomeHero.tsx` (delete), `mobile/tests/integration/home-cta-target.test.tsx`, `mobile/tests/integration/home.screen.test.tsx`.
- Frozen (gacha-v7.md §2.1, `mobile/gacha-v7.md:83-88`) — must show zero lines in `git diff --numstat`: `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`. Also do not touch `mobile/src/features/gacha/contracts.ts` (out of scope; `HomeHeroVM.helper` stays as a type).
- No new dependencies. No `@ts-ignore` / `@ts-expect-error` / `eslint-disable`. Do not change tsconfig/vitest config.
- testIDs to keep, byte-identical, in `HomeScreen.tsx`: `screen-home-root`, `screen-home-primary-cta`, `home-primary-cta` (exactly ONE Pressable — `home.screen.test.tsx:176-181` asserts `toHaveLength(1)`; do NOT put it on the pack), `home-draw-status-badge` (keeps rendering `homeState.vm.draw.label` unchanged — pinned by `home-primary-cta.test.tsx:283/337`, `home-economy-floor.spec.tsx:221`, `home.screen.test.tsx:183`), `home-pack-visual`, `home-featured-pack`, `home-error-retry`, and the hidden probes `home-study-due-link`, `home-first-draw-link`, `home-collapse-decks-toggle`, `home-collapse-week-support-toggle`, `home-deck-row-*`.
- New testID (exactly this spelling): `home-goal-line`.
- CTA literals that must NOT change: `'Open library'` for `deckSummaries = []` (`home-cta-target.test.tsx:264-289`, `summary-home.test.ts:62`, `homeSelectors.spec.ts:278`) and for every existing non-first-run branch (`homeSelectors.spec.ts:217/230/255`, `p2-smoke.ts:201/226`); `DRAW_CTA_LABEL = 'Open reward draw'` is the only label the new first-run→draw branch may use. `vm.drawStatusLabel` stays (`tests/unit/summary-home.test.ts:121`, `tests/p2-smoke.ts:253`). Do not edit `home-primary-cta.test.tsx`, `home-economy-floor.spec.tsx`, `homeSelectors.spec.ts` — they are A03's and must stay green untouched.
- Never hardcode "earn 1 pull" anywhere (rewardPulls varies per route, `summaryMapper.ts:209`). Do not add a second status line under the CTA.
- Existing test literals that may be edited: none. New cases are additive; the only existing test you may extend is `home.screen.test.tsx` 'renders root shell with one primary CTA surface' (add the goal-line assertion).
- A02 contract that stays in force (it is on your base): `MOCKED_HOME_DECKS` is gone, the literal `Coming soon` / `coming soon` is banned anywhere in `HomeScreen.tsx` (gacha-v7 §3.2.3, `mobile/gacha-v7.md:213`), and the only `featuredDeck.realRow === null` case is `EMPTY_FEATURED` (zero decks), whose pack `accessibilityLabel` is exactly `'Connect to load packs'` and whose press is `void refreshHome()` with no navigation and no `Alert` — pinned by A02's `it('renders the hero and pack shelf with zero decks')` in `home.screen.test.tsx`, which must stay green untouched.

## Changes required

1. **`homeSelectors.ts` — F3 selector override.** In `buildHomeVM`, the `else if (selectedDeck && !selectedDeck.canStudy && statusKind === 'first_run')` branch (`:658-665`, grep `statusKind === 'first_run'`): before it, resolve the selected deck's hint with the existing `actionHintForDeck({ deck: selectedDeck, updateInfo: updates[selectedDeck.slug] ?? null, premium, signedIn: hasSignedInUser })` (or move the `buildDeckRows(...)` call above the override and read `rows.find(r => r.isSelected)?.actionHint`). When `draw.state !== 'locked'` AND that hint is exactly `'install'` (free deck, installable from `updates` — NOT `'paywall'`, `'trial-start'`, `'open'`, `'none'`), set `cta = { kind: 'first_run', label: DRAW_CTA_LABEL, nav: 'draw', testID: 'home-primary-cta', disabled: false }`; otherwise keep the current `'Open library'`/`'library'` assignment. Leave the `if (!selectedDeck && statusKind !== 'error')` branch (`:650-657`) byte-identical. Do not touch `mapStatusToCta`'s `case 'first_run'`. Optionally (F3, allowed): in `buildHeroCopy`'s `!selectedDeck.canStudy` block (`:443-450`) change `subtitle` to `'Your first pack installs when you open it.'` — leave `title`/`eyebrow` alone.
2. **`homeSelectors.ts` — F16 deprecation.** Add a JSDoc `/** @deprecated \`helper\` is never rendered on Home (HomeHero.tsx removed in A04); keep for HomeHeroVM shape only. */` above `function buildHeroCopy(` (`:418`). Keep `vm.drawStatusLabel` (`:693-698`) and `goal: buildGoalVM(selectedDeck)` (`:705`) exactly as they are.
3. **`HomeScreen.tsx` — F12 + F3 pack handler.** Replace `handleFeaturedPackPress` (`:631-658`, grep `const handleFeaturedPackPress`) and delete `const drawState`/`const walletHasPulls` only if no longer needed (you need `drawState` for 3d). New dispatch, in this order:
   a. `!featuredDeck.realRow` (`EMPTY_FEATURED` after A02: zero decks) → `void refreshHome(); return;` — no navigation, no `Alert`, no `disabled` on the pack `Pressable`, and the `accessibilityLabel` stays `'Connect to load packs'` exactly as A02 left it. Never write `Coming soon` (banned; A02's zero-decks test asserts this label and that nothing navigates).
   b. `firstDrawCoach` (the existing screen-level const from `route.params?.firstDrawCoach`, `:110`) → `navigation.navigate('Draw', { slug: featuredDeck.slug, rewardPending: true })` — same call as the `home-first-draw-link` probe (`:887-899`).
   c. `hint === 'paywall' || hint === 'update' || hint === 'trial-start'`, or `hint === 'install'` with an EMPTY wallet (`drawState === 'locked'`) → `void handleDeckPress(featuredDeck.realRow)` (unchanged resolver path).
   d. `hint === 'install'` with pulls (`drawState !== 'locked'`) → `navigation.navigate('Draw', { slug: featuredDeck.slug, rewardPending: drawState !== 'wallet-full' })` — DrawScreen's `resolveOrInstall` (`DrawScreen.tsx:362-371`) installs the deck itself; do NOT call `executeDeckAction`/`resolveDeckAction` from Home for this.
   e. otherwise → `void handlePrimaryCta()`.
   Delete the old "installed + has pulls → straight to Draw" and "Installed + no pulls → SessionCard" branches and their comment bullets (`:626-627`, `:647-657`) so pulls never outrank due study (matches `home-cta-target.test.tsx:235-257`).
   Set the pack's `accessibilityLabel` (`:675`) to the text the user sees for that branch: a → `'Connect to load packs'` (unchanged from A02); b → `'Open reward draw'`; c → `` `${featuredDeck.status} ${featuredDeck.title}` `` (e.g. "Install C# Interview", "Locked C# Interview"); d and e → `homeState.vm.cta.label`. Keep `testID="home-featured-pack"`, `accessibilityRole="button"`, `hitSlop`, and the `AnimatedView`/image children unchanged. `handlePrimaryCta` needs no change: its `case 'draw'` (`:362-367`) already navigates `Draw` with `{ slug, rewardPending: true }`.
4. **`HomeScreen.tsx` — F16 goal line.** Inside the action group, between `<TodayPressureCard …/>` and `<View testID="screen-home-primary-cta">` (`:733-736`), render one `<Text testID="home-goal-line" style={styles.goalLine} numberOfLines={1}>{\`${homeState.vm.goal.minimum} · ${homeState.vm.goal.fullClear}\`}</Text>`. Add `goalLine` to `styles` (13pt, `colors.inkSecondary`, centered, small `marginBottom`). Do not reorder TodayPressureCard/CTA/badge; do not touch `home-draw-status-badge`.
5. **Delete `mobile/src/features/gacha/components/HomeHero.tsx`** (`git rm`). It has no importers (`grep -rn "HomeHero" mobile/src mobile/App.tsx` → only `contracts.ts` type names and the file itself). Do not delete or edit `HomeHeroVM` in `contracts.ts`.
6. **`mobile/tests/integration/home-cta-target.test.tsx` — two F3 cases.** Add `let updatesFixture: Record<string, any> = {};` next to `deckSummariesFixture` (`:5-7`), return `updates: updatesFixture` from the `loadHomeDeckSummaries` mock (`:91`, currently `updates: {}`), and reset `updatesFixture = {}` in `beforeEach` (`:160-166`). Then:
   - `it('sends a brand-new user with starter pulls straight to Draw')`: `deckSummariesFixture = [{ slug: 'csharp', title: 'C# Interview', locale: 'en-US', version: '1', deckType: 1, totalCards: 40, localCards: 0, studyCards: 0, canStudy: false, dueToday: 0, plannedToday: 0, newToday: 0, masteredApprox: 0, percent: 0 }]`, `updatesFixture = { csharp: { slug: 'csharp', installedVersion: null, remoteVersion: '1', hasUpdate: true, remoteUrl: 'https://example.test/csharp.json', remoteSha256: null } }` (shape: `UpdateInfo`, `mobile/src/content/deckRepository.ts:362-371`), `walletFixture = { availablePulls: 3, reservePulls: 0 }`. Render as the other cases do, find `home-primary-cta`, assert its label (same `find` pattern as `:253-256`) is `'Open reward draw'`, press it, and assert `navigateMock` was called with `('Draw', { slug: 'csharp', rewardPending: true })`, NOT with `'Library'`, and that the mocked `executeDeckAction` was not called (import it via `vi.mocked(...)` from the mocked module or check `resolveDeckAction`).
   - `it('keeps the library CTA for an installable deck when the wallet is empty')`: same fixtures with `walletFixture = { availablePulls: 0, reservePulls: 0 }` → label `'Open library'`, press → `navigateMock` called with `'Library'` and not with `'Draw'`.
7. **`mobile/tests/integration/home.screen.test.tsx` — pack cases + goal line.** Using the default fixture (csharp `dueToday: 1, newToday: 1`, wallet `{ availablePulls: 2, reservePulls: 0 }` = due work + pulls):
   - `it('routes the hero pack to study, not Draw, when work is due and pulls are available')`: find the `Pressable` with `testID === 'home-featured-pack'`, assert `props.accessibilityLabel === 'Start today’s challenge'` (note the curly apostrophe, same as `home-cta-target.test.tsx:257`), press, `await Promise.resolve()`, assert `navigateMock` called with `('SessionCard', { slug: 'csharp' })` and `not.toHaveBeenCalledWith('Draw', expect.anything())`.
   - `it('routes the hero pack to Draw when the first-draw coach is on')`: render with `params: { firstDrawCoach: true }`, press `home-featured-pack`, assert `('Draw', { slug: 'csharp', rewardPending: true })` and `accessibilityLabel === 'Open reward draw'`.
   - In the existing 'renders root shell with one primary CTA surface' case add `const goal = tree.root.findByProps({ testID: 'home-goal-line' }); expect(String(goal.props.children)).toContain('Full clear: 2 cards');` (due 1 + new 1).

## Acceptance

Each bullet is one command, run from the worktree root; `docs/delivery/r16-issues/A04.verify.sh` runs exactly these.

- `cd mobile && npx vitest run tests/integration/home-cta-target.test.tsx tests/integration/home.screen.test.tsx tests/integration/home-primary-cta.test.tsx tests/integration/home-economy-floor.spec.tsx tests/unit/homeSelectors.spec.ts tests/unit/summary-home.test.ts --reporter=dot` → all pass, and `home-cta-target.test.tsx` has ≥ 6 `it(` cases, `home.screen.test.tsx` ≥ 7.
- `cd mobile && npm run test:typecheck` → exit 0.
- `test ! -e mobile/src/features/gacha/components/HomeHero.tsx && grep -q 'testID="home-goal-line"' mobile/src/screens/HomeScreen.tsx && grep -Eq 'vm\.goal\.(minimum|fullClear)' mobile/src/screens/HomeScreen.tsx && grep -q '@deprecated' mobile/src/features/gacha/selectors/homeSelectors.ts && awk '/export function buildHomeVM/,0' mobile/src/features/gacha/selectors/homeSelectors.ts | grep -q DRAW_CTA_LABEL && grep -q 'rewardPending: true' mobile/tests/integration/home-cta-target.test.tsx && grep -q 'home-featured-pack' mobile/tests/integration/home.screen.test.tsx` → exit 0 (the `buildHomeVM` override must reference `DRAW_CTA_LABEL`; on base it only assigns `'Open library'`).
- Negative greps, all must exit 0: `! grep -q 'installed + has pulls' mobile/src/screens/HomeScreen.tsx`, `! grep -q 'Installed + no pulls' mobile/src/screens/HomeScreen.tsx`, `! grep -q 'Open ${featuredDeck.title} pack' mobile/src/screens/HomeScreen.tsx`, `! grep -qi 'earn 1 pull' mobile/src/screens/HomeScreen.tsx mobile/src/features/gacha/selectors/homeSelectors.ts`, `[ "$(grep -c 'testID={homeState.vm.cta.testID}' mobile/src/screens/HomeScreen.tsx)" = 1 ]`, `! grep -q 'testID="home-primary-cta"' mobile/src/screens/HomeScreen.tsx`, `! grep -qi 'coming soon' mobile/src/screens/HomeScreen.tsx`, `grep -q 'Connect to load packs' mobile/src/screens/HomeScreen.tsx`, and every kept testID / `drawStatusLabel` / `'Open library'` literal listed in Constraints still greps.
- `git diff --numstat "$(git merge-base HEAD delivery/r16-a-home)" -- mobile/src/content/deckRepository.ts mobile/src/sync/progressSync.ts mobile/src/review/model.ts mobile/src/features/gacha/contracts.ts mobile/tests/integration/home-primary-cta.test.tsx mobile/tests/integration/home-economy-floor.spec.tsx mobile/tests/unit/homeSelectors.spec.ts` → empty, and every other changed path is inside the scope list (or `docs/delivery/r16-issues/`).

## DO NOT

- Do not push, do not open a PR, never touch `main`, no deploy commands, no `npm ci`.
- Do not disable, skip, delete, or weaken any test; do not edit `home-primary-cta.test.tsx`, `home-economy-floor.spec.tsx`, `homeSelectors.spec.ts`, `summary-home.test.ts`, `p2-smoke.ts`.
- Do not add `testID="home-primary-cta"` to the pack or anywhere else; do not remove the hidden probes.
- Do not delete `vm.drawStatusLabel`, `hero.helper`, `HomeHeroVM`, or the `'Open library'` fallback for `deckSummaries = []`.
- Do not install the deck from Home (no `executeDeckAction` in the F3 path) — DrawScreen owns install-on-open.
- Do not add a loop/"earn a pull" line under the CTA, do not change the draw badge labels, do not touch `TodayPressureCard.tsx`, `HomeDeckRow.tsx`, `WelcomeScreen.tsx`, `LibraryScreen.tsx`, or `DrawScreen.tsx`.
- Do not refactor the selector tiles, animations, or styles beyond the one new `goalLine` style.
- Do not re-introduce `Coming soon` (any case), a `disabled` pack, or an `Alert` for the empty-featured case; do not change the `'Connect to load packs'` label or the `refreshHome()` press A02 set for it.
