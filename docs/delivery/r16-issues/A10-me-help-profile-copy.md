# A10 — me-help-profile-copy

R8 Me / Help / Profile: replace the design-spec placeholder copy on the Me tab, the Help FAQ and the Profile screen with real content (real streak + collection numbers, Settings / Help / Privacy / Support rows, byline, version) and 8 real user-language FAQs.

## Context

The Me tab still renders the design-spec prose verbatim to every user — `mobile/src/screens/MoreScreen.tsx:14` ("Treat this tab as your support rail"), a "Support areas: 3" headline stat (`:20`), a "Developer tools — Debug menu" section (`:30-36`) — while Help renders the 3-entry mock at `mobile/src/mock/faq.ts:1-5` through `mobile/src/screens/HelpFAQScreen.tsx:5,23` under a hero body that calls itself a "support companion" (`:14`), and Profile shows "Learner #local" plus a "Next best return point" section written for the designer, not the user (`mobile/src/screens/ProfileScreen.tsx:81-94`). Spec: `docs/home-review-and-launch-copy-2026-09-17.md:454` (§3.2 row "Me / Help / Profile 占位文案": streak / collection stats, Settings, Help, Privacy, contact, "Made by one developer in Auckland", version; FAQ = the user version of §2.9) and `:368-397` (§2.9 answers; use **version A** of the AI answer at `:372`, and respect the red lines at `:399-425`); `docs/release-1.6.0-plan-2026-09-19.md:243` (R8) and `docs/delivery-wave-1.6-plan-2026-09-19.md:75` (A10: `src/mock/faq.ts` → `src/content/faq.ts`, assert no "support rail" / "Developer tools" literal).

Read first, in this order: `MoreScreen.tsx:1-53` · `HelpFAQScreen.tsx:1-32` · `ProfileScreen.tsx:1-105` · `mobile/src/mock/faq.ts` · `mobile/src/components/AppInfoScreen.tsx:20-35` (props; `footer` at `:34` is the only slot that renders arbitrary nodes, `:109`) and `:93-107` (only three buttons exist — Profile/Settings/Help/Privacy/Support need five pressables, so rows go in `footer`) · `mobile/src/features/gacha/streaks/streakTracker.ts:6-13, 70` (`StreakSnapshot`, `loadStreakSnapshot`) · `mobile/src/features/gacha/draw/drawStateStore.ts:58-64, 218, 273` (`DrawStateRecord.owned`, `loadDrawState`, `listDrawStateSlugs`; both swallow storage errors and resolve to empty) · `mobile/src/screens/SettingsScreen.tsx:48-51` (the Support / Privacy Notion URLs to reuse) and `:349` (Debug already lives behind `__DEV__` there) · `mobile/app.json:6` (`expo.version`, the version string to show) · the five suites that already render these screens with a 5-export `react-native` mock and **no** drawStateStore / expo mocks: `mobile/tests/integration/me-real-data.spec.tsx:24-56, 133, 171, 193-223`, `me-final.screen.test.tsx:77-128`, `phase-c-complete.screen.test.tsx:55-65, 102-112`, `phase-c-shells.screen.test.tsx:55-65, 99-109`, `support-pool-polish.screen.test.tsx:71-77`, `support-family-final-polish.screen.test.tsx:65-70`.

## Constraints

- **No new dependencies.** Version comes from `import appJson from '../../app.json'` (`resolveJsonModule` is on via `expo/tsconfig.base`; verified to typecheck and to load under Vite). Do **not** import `expo-constants`, `expo-application` or `../config/remoteConfig` into any scope screen: those modules do not parse under vitest (Flow sources) and five existing suites render `MoreScreen` without mocking them.
- **`react-native` imports in the three screens are limited to** `View, Text, ScrollView, Pressable, StyleSheet` plus `Linking` — the existing suites mock `react-native` with exactly those five exports (`me-real-data.spec.tsx:24-38`), and `Linking` may only be dereferenced **inside a press handler** (never at module or render time). `ProfileScreen` must not use `Linking` at all: `me-real-data.spec.tsx:183-188` presses every Pressable on it.
- **Scope (only these paths may change):**
  - `mobile/src/screens/MoreScreen.tsx`
  - `mobile/src/mock/faq.ts` (delete)
  - `mobile/src/content/faq.ts` (new)
  - `mobile/src/screens/HelpFAQScreen.tsx`
  - `mobile/src/screens/ProfileScreen.tsx`
  - `mobile/tests/integration/more.screen.test.tsx` (new)
  - Plus exactly these one-line assertion flips in existing tests, because the issue rules that no test may assert the old placeholder strings: `mobile/tests/integration/me-final.screen.test.tsx:89, 106, 108`, `mobile/tests/integration/support-pool-polish.screen.test.tsx:77`, `mobile/tests/integration/support-family-final-polish.screen.test.tsx:70` (see change 6). Nothing else in those files. Line numbers are as of base `92cebbd`: A07 (queued before you) removes two import lines above these in `me-final.screen.test.tsx` (its lines 33 and 36), so the three assertions sit two lines higher — locate them by their literal (`toContain('Developer tools')`, `toContain('Study identity')`, `toContain('Next best return point')`), not by line.
  - Scope size: 9 paths exceeds the brief template's ≤ 8-file guide by one; accepted because the three one-line flips are forced by the wave plan's verify rule (no positive assertion of the old placeholder strings may survive), not by design choice. Non-test LOC stays under 400.
- **Frozen files (gacha-v7.md §2.1) — zero lines changed:** `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`. Also out of scope: `AppInfoScreen.tsx`, `SettingsScreen.tsx`, `EditProfileScreen.tsx`, `AboutScreen.tsx`, `App.tsx`, `navigation/types.ts`.
- **Existing test literals that must keep passing untouched:** Pressables whose single `Text` child is exactly `'Profile'`, `'Settings'`, `'Help'` on More — **exactly one each**, because the suites use `tree.root.find(...)` which throws on 2+ matches (`me-final.screen.test.tsx:54-60, 95`; `phase-c-*:61-63`; `me-real-data.spec.tsx:208-213`) — navigating to `'Profile'`, `'Settings'` (not `'SettingsMain'`), `'HelpFAQ'`; More title `'Your profile and support'` (`me-final:85`); More blob free of `'Achievements'` and `'Dev tools'` (`me-real-data:199-201`). Profile: title `'Your study profile'`, section titles `'Current setup'` and `'Momentum this week'` (`me-final:104-107`), the literal `` `${snapshot.totalQualifiedSessions} sessions have counted toward a streak` `` (`me-real-data:133`), the Audience row rendering the raw preference string as its own Text node (`me-real-data:171`), `'Edit profile'` → `navigate('EditProfile')` (`phase-c-*`), no `'Achievements'` / `'Milestone hall'` text. Help: title `'Help and answers'`, stat label `'Top questions'` (`me-final:125-126`), and an FAQ entry whose question is exactly `'Why is Draw locked?'` (`phase-c-complete:111`, `phase-c-shells:108`) — it is a real user question (wallet at 0 ⇒ `buildDrawState` returns `locked`, `mobile/src/features/gacha/draw/drawState.ts:79-87`), so keep the question and write a real answer.
- **testIDs to add and keep:** `more-row-profile`, `more-row-settings`, `more-row-help`, `more-row-privacy`, `more-row-support`, `more-byline`, `more-version`.
- **Exported literals (tests import them):** `MORE_LINKS = { support, privacy }` and `MORE_BYLINE = 'Made by one developer in Auckland'` from `MoreScreen.tsx`; `FAQ_LIST` from `mobile/src/content/faq.ts`.
- **Fact red lines** (`home-review… :399-425`): no "thousands of cards", no Android/Google Play promise, no "no AI anywhere", no "verified against official docs", no fixed drop percentages, no FSRS/SM-2/Anki, no "buy pulls". Pull economy facts to state: +1 pull per fully cleared review (`rewardResolver.ts:12-14`), 3 starter pulls (`rewardWallet.ts:15`), 1-pull daily floor only when due = 0, unstudied owned = 0 and wallet = 0 (`economyFloor.ts:19, 69-80`), pity = after 10 Commons the next pull is Rare+ while the pack still has an unowned Rare+ (`pity.ts:32, 46`), draws are a uniform pick from unowned cards (no duplicates), light-only UI (`app.json:9 userInterfaceStyle: light`), iOS only.

## Changes required

1. **`mobile/src/content/faq.ts` (new) — the real FAQ.** Export `type FaqEntry = { q: string; a: string }` and `export const FAQ_LIST: readonly FaqEntry[]` with exactly these 8 entries, in this order (wording may be tightened but every fact and the quoted literals must survive):
   1. `q: 'How do I earn pulls?'` — a: "Fully clear today's review and you earn 1 pull. New accounts start with 3 starter pulls, and if you have no cards left to study and no pulls, a 1-pull daily floor keeps you going. Pulls are never sold."
   2. `q: 'Why is Draw locked?'` — a: "Draw locks when you have no pulls to spend. Clear today's review to earn one, or wait for the daily floor pull if you have nothing left to study."
   3. `q: 'What is pity?'` — a: "After 10 Commons in a row, your next pull is Rare or better, as long as the pack still has a Rare or Legendary you don't own. Every other draw is a uniform random pick from the cards you're still missing, so there are no duplicates."
   4. `q: 'Why is a card locked in the Library?'` — a: "You study the cards you've drawn. A locked card is one you haven't pulled yet; anything you already studied stays open."
   5. `q: 'Does it work offline?'` — a: "Yes. Installed decks and your progress live on this phone. Signing in adds cloud backup."
   6. `q: 'Is there an Android version?'` — a: must contain the literal `iOS only`: "iOS only right now. There's no Android date yet, and I'd rather not promise one until I can keep it."
   7. `q: 'Is there a dark mode?'` — a: "Not yet. The app is light-only today."
   8. `q: 'Are the cards AI-generated?'` — version A, must contain the literal `AI assistance`: "I draft with AI assistance, then rewrite and check every card myself before it ships. I'm one person, so mistakes happen. If you find one, tell me through Support and I'll patch it."
   Then `git rm mobile/src/mock/faq.ts`; nothing else imports it (verified: only `HelpFAQScreen.tsx:5`).

2. **`mobile/src/screens/HelpFAQScreen.tsx`** — change the import at `:5` to `import { FAQ_LIST } from '../content/faq';`. Replace the body at `:14` with user-language copy that contains neither "support companion" nor "learner", e.g. "Short answers to what people ask most: pulls, pity, locked cards, offline, Android and dark mode. Missing something? Open Support from the Me tab." Keep `eyebrow="FAQ"`, `title="Help and answers"`, the `'Top questions'` stat (`:17-19`), the `'FAQ'` section mapping `q → title, a → subtitle` (`:20-25`) and the `'Back to me'` → `navigate('More')` button.

3. **`mobile/src/screens/MoreScreen.tsx` — rewrite.**
   - Imports: `React, { useEffect, useState }`; `{ Linking, Pressable, StyleSheet, Text, View } from 'react-native'`; `AppInfoScreen`; `loadStreakSnapshot, type StreakSnapshot` from `../features/gacha/streaks/streakTracker`; `listDrawStateSlugs, loadDrawState` from `../features/gacha/draw/drawStateStore`; `appJson from '../../app.json'`; theme `colors`/`spacing`/`typography` as needed.
   - `export const MORE_LINKS = { support: <SUPPORT_URL string from SettingsScreen.tsx:49>, privacy: <PRIVACY_URL string from SettingsScreen.tsx:51> } as const;` (copy the two URLs verbatim; add a one-line comment that `SettingsScreen.tsx` keeps its own private copies and both change together) and `export const MORE_BYLINE = 'Made by one developer in Auckland';`.
   - State: `snapshot: StreakSnapshot | null` and `collected: number | null`, loaded in one `useEffect` with the same cancelled-flag shape as `ProfileScreen.tsx:23-37`: `loadStreakSnapshot()` and, in parallel, `listDrawStateSlugs()` → `Promise.all(slugs.map(loadDrawState))` → sum of `owned.length`. Wrap the collection read in `try/catch` → `0`. While pending render `'—'` (same rule as `ProfileScreen.tsx:39-44`), never a fake number.
   - Render `AppInfoScreen` with `eyebrow="Me"`, `title="Your profile and support"`, a user-language body with no "rail", "learner", "surface", "QA" or "spec" wording (a Help row subtitle may say "FAQ") (e.g. "Your streak and collection at a glance, plus settings, help, privacy and a way to reach me."), `cosmic`, **no** `chips`, **no** `sections`, **no** `primaryLabel`/`secondaryLabel`/`tertiaryLabel` (rows replace the buttons; a second Pressable reading `'Profile'`/`'Settings'`/`'Help'` breaks the `find()` suites), `stats={[{ label: 'Day streak', value: dayStreak }, { label: 'Cards collected', value: cardsCollected }]}`, and everything else in `footer`.
   - `footer` content, in order: a card with five row `Pressable`s, each `accessibilityRole="button"`, with `testID` and a first `Text` child whose children is **exactly** the label string: `more-row-profile` "Profile" → `navigation.navigate('Profile')`; `more-row-settings` "Settings" → `navigation.navigate('Settings')`; `more-row-help` "Help" → `navigation.navigate('HelpFAQ')`; `more-row-privacy` "Privacy" → `() => { void Linking.openURL(MORE_LINKS.privacy).catch(() => undefined); }`; `more-row-support` "Support" → same with `MORE_LINKS.support`. Each row may carry a second short subtitle `Text` (e.g. "Streak, week and audience" / "Reminders, account, premium" / "Pulls, pity, offline, Android" / "Privacy policy" / "Report a wrong card or ask a question"). Then `<Text testID="more-byline">{MORE_BYLINE}</Text>` and `<Text testID="more-version">{`Version ${appJson.expo.version}`}</Text>`.
   - Delete the "Developer tools" section, the "Support areas" stat, the "What you can manage" section and every comment at `:17-19` and `:41-45` (the facts they record are now in the git history; the Settings comment can shrink to one line next to the row if you want to keep it). Debug stays where it already is: `SettingsScreen.tsx:349` behind `__DEV__`.

4. **`mobile/src/screens/ProfileScreen.tsx`** — remove the placeholders, keep the real data:
   - `:50` body → user-language, e.g. "Your streak, your week and the preferences that shape what you study." (no "learner identity").
   - `:51` chips: drop `'Identity'` (keep `['Momentum', 'Progress']` or remove the prop).
   - `:64-65` "Momentum this week": delete the `body:` sentence ("A compact read on whether this learner…"); keep both items **verbatim** (`:66-79`).
   - Delete the whole `'Study identity'` section (`:81-87`, "Learner #local", "optional account linking later") and the whole `'Next best return point'` section (`:88-94`).
   - Keep `'Current setup'` / Audience (`:56-62`), the two stats (`:52-55`), `'Edit profile'` → `EditProfile` (`:96-97`) and the header comment (`:10-18`).

5. **`mobile/tests/integration/more.screen.test.tsx` (new)** — same harness as `me-real-data.spec.tsx:24-75` (react-test-renderer, `IS_REACT_ACT_ENVIRONMENT`, `collectText`/`textBlob`), with the `react-native` mock extended by `Linking: { openURL: openUrlMock }` (`openUrlMock = vi.fn(async () => undefined)`), mocks for `../../src/features/gacha/streaks/streakTracker` (`loadStreakSnapshot` → mutable fixture), `../../src/features/gacha/draw/drawStateStore` (`listDrawStateSlugs` / `loadDrawState` → mutable fixture map `{ csharp: ['c-001','c-002','c-003'], 'aws-saa-c03': ['a-001','a-002'] }`), `../../src/features/gacha/audience/audiencePrefs` (`getAudiencePreference` → `'both'`), `react-native-safe-area-context`, `expo-linear-gradient`. Import `appJson from '../../app.json'`, `{ MoreScreen, MORE_LINKS, MORE_BYLINE }`, `{ ProfileScreen }`, `{ HelpFAQScreen }`, `{ FAQ_LIST } from '../../src/content/faq'`. Render inside `await act(async …)` and flush two microtasks. At least these 8 `it(` blocks:
   1. **stats from the trackers**: streak 7, fixture map above → the exact Text nodes contain `'7'` and `'5'`; blob contains `'Day streak'` and `'Cards collected'`.
   2. **fresh install**: streak 0, no slugs → exact Text nodes contain `'0'`; blob does not contain `'—'` after flush.
   3. **rows navigate**: `findByProps({ testID: 'more-row-profile' })` press → `navigate('Profile')`; `more-row-settings` → `'Settings'` and not `'SettingsMain'`; `more-row-help` → `'HelpFAQ'`.
   4. **links open the policy pages**: `more-row-privacy` → `openUrlMock` called with `MORE_LINKS.privacy`; `more-row-support` → `MORE_LINKS.support`; both `toMatch(/^https:\/\//)`.
   5. **byline and version**: blob contains `MORE_BYLINE`, `'Made by one developer in Auckland'` and `` `Version ${appJson.expo.version}` ``.
   6. **no design-spec or dev leftovers on More**: blob `not.toContain` each of `'support rail'`, `'Developer tools'`, `'Debug menu'`, `'Dev tools'`, `'Support areas'`, `'QA shortcuts'`, `'QA lane'`, `'What you can manage'` (not the bare `'QA'` — it is a substring of "FAQ"); and for each of `'Profile'`, `'Settings'`, `'Help'` exactly **one** Pressable has a Text child equal to it (`findAll(...).length === 1`).
   7. **Profile has no placeholders**: blob `not.toContain` `'Learner #local'`, `'Study identity'`, `'Next best return point'`, `'dead-end'`, `'account linking'`; still contains `'Current setup'`, `'Momentum this week'`; `'Edit profile'` pressable → `navigate('EditProfile')`.
   8. **Help renders the real FAQ**: `FAQ_LIST.length` is `>= 6` and `<= 8`; every `q` appears in the blob; blob contains `'AI assistance'`, `'iOS only'`, `'Why is Draw locked?'`; `not.toContain` `'support companion'`, `'route pressure'`, `'Fresh Start'`.

6. **One-line flips in existing suites** (no other edits in these files):
   - `me-final.screen.test.tsx:89` `expect(moreBlob).toContain('Developer tools');` → `expect(moreBlob).not.toContain('Developer tools');`; `:106` `toContain('Study identity')` → `not.toContain('Learner #local')`; `:108` `toContain('Next best return point')` → `not.toContain('Next best return point')`.
   - `support-pool-polish.screen.test.tsx:77` `expect(moreBlob).toContain('support rail');` → `expect(moreBlob).not.toContain('support rail');`.
   - `support-family-final-polish.screen.test.tsx:70` `expect(faqBlob).toContain('support companion');` → `expect(faqBlob).not.toContain('support companion');`.
   Note for the merge: A07 (mock settings island removal) also edits `me-final.screen.test.tsx` and `support-pool-polish.screen.test.tsx` (they import `SettingsMainScreen` etc.). Keep your hunks to the single lines above so the merge is trivial; A07 removes two import lines above these, so find the three assertions by their literal, not by line; if those files are already gone in your base, skip the flip and say so.

Estimated size: ~200 LOC of source (MoreScreen ≈ 120, content/faq.ts ≈ 50, HelpFAQ ≈ 5, Profile ≈ −25) plus ~220 LOC of tests.

## Acceptance

Run from the worktree root. `docs/delivery/r16-issues/A10.verify.sh` runs exactly these.

1. `cd mobile && npx vitest run tests/integration/more.screen.test.tsx tests/integration/me-real-data.spec.tsx tests/integration/me-final.screen.test.tsx tests/integration/phase-c-complete.screen.test.tsx tests/integration/phase-c-shells.screen.test.tsx tests/integration/support-pool-polish.screen.test.tsx tests/integration/support-family-final-polish.screen.test.tsx --reporter=dot` exits 0, `tests/integration/more.screen.test.tsx` exists and contains at least 8 `it(` blocks (files among the six pre-existing ones that a parallel merge has deleted are skipped by the script; the new file is mandatory).
2. `cd mobile && npm run test:typecheck` exits 0.
3. Literal guards (all must hold): `test -f mobile/src/content/faq.ts` · `! test -e mobile/src/mock/faq.ts` · `! grep -rn "mock/faq" mobile/src mobile/tests` · `grep -q "export const FAQ_LIST" mobile/src/content/faq.ts` · the count of lines in `mobile/src/content/faq.ts` matching `q:` followed by a quoted string (one per entry; the type alias's `q: string` does not count) is between 6 and 8 · `faq.ts` contains `Why is Draw locked?`, `AI assistance`, `iOS only` and none of `route pressure`, `thousands`, `FSRS`, `SM-2`, `Google Play`, `no AI` · `HelpFAQScreen.tsx` imports `'../content/faq'` · `MoreScreen.tsx` contains `Made by one developer in Auckland`, `export const MORE_LINKS`, `app.json`, and each of `more-row-profile`, `more-row-settings`, `more-row-help`, `more-row-privacy`, `more-row-support`, `more-byline`, `more-version` · `MoreScreen.tsx` imports none of `expo-constants`, `expo-application`, `config/remoteConfig` · `ProfileScreen.tsx` does not reference `Linking` · none of `MoreScreen.tsx` / `ProfileScreen.tsx` / `HelpFAQScreen.tsx` contains `support rail`, `Developer tools`, `Debug menu`, `Dev tools`, `Support areas`, `Learner #local`, `Study identity`, `Next best return point`, `support companion`.
4. No test in `mobile/tests` positively asserts an old placeholder: `grep -rEn "\)\.toContain\('(support rail|Developer tools|support companion|Learner #local|Study identity|Next best return point)'\)" mobile/tests` returns nothing (negative `.not.toContain(...)` forms are fine and expected).
5. Scope guard: `git diff --name-only <merge-base with delivery/r16-a-home>` plus untracked files (excluding `docs/delivery/r16-issues/`) lists only the paths in the Scope list (the six scope paths and the three existing suites named in change 6), and `git diff --numstat <merge-base> -- mobile/src/content/deckRepository.ts mobile/src/sync/progressSync.ts mobile/src/review/model.ts mobile/src/components/AppInfoScreen.tsx mobile/src/screens/SettingsScreen.tsx mobile/src/screens/EditProfileScreen.tsx mobile/App.tsx` is empty.

## DO NOT

- Do not touch the frozen files (`mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`), nor `AppInfoScreen.tsx`, `SettingsScreen.tsx`, `EditProfileScreen.tsx` (its own "Learner #local" at `:19` is a follow-up, not this issue), `AboutScreen.tsx`, `App.tsx`, `navigation/types.ts`, `app.json`.
- Do not add routes, a "Report a card" mailto, share/rating features, a dark mode, or an Android promise — separate issues (R7/B15) or explicitly out of the 1.6 scope.
- Do not import `expo-constants` / `expo-application` / `remoteConfig` into the screens, and do not add `vi.mock`s to the five pre-existing suites to make such an import work.
- Do not keep the "Developer tools" / "Debug menu" section on More in any form (not even behind `__DEV__`); do not re-add `Achievements` / `Milestone hall` links to Profile.
- Do not rename the FAQ question `'Why is Draw locked?'`, the More title `'Your profile and support'`, the Profile title `'Your study profile'` or its `'Current setup'` / `'Momentum this week'` sections, or the Help title `'Help and answers'` / `'Top questions'` stat — the untouched suites pin them.
- Standing rules: no `git push`, no PR, never target or touch `main`, no deploy command, no disabling/skipping/gutting tests, no `@ts-ignore`/`@ts-expect-error`/`eslint-disable`, no loosening of `tsconfig`/eslint, no `npm ci`.
