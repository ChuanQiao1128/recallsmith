# B14 — Docs + licences: rubric rewrite, v10 design doc, v9 superseded, LICENSE-ASSETS + SFX LICENSES template (`docs-licenses`)

Rewrite `mobile/docs/qa/animation-quality-rubric.md` around the seven shipped phases with the DEVICE timing table and its ceilings, write the dated Chinese design doc `mobile/docs/design/v10-ceremony-seam-of-light.md` that records what B01–B13 actually built, put a "superseded" banner on the two v9 docs, and add the two licence files the DoD asks for: `LICENSE-ASSETS` (art + audio excluded from MIT) and the per-file `mobile/assets/sfx/LICENSES.md` template the owner fills when the placeholder samples are replaced.

## Context

The rubric still scores a ceremony that no longer exists: its phase chains are `orbit -> charge -> stabilize` and `warmup -> focus -> reveal` (`mobile/docs/qa/animation-quality-rubric.md:7-8`), its timing gate is "10-pull `2100ms ~ 2600ms` / 1-pull `1100ms ~ 1400ms`" with `charge@600~850ms`, `stabilize@1300~1600ms` (`:33-36`), and it carries only the original six A-P0 items (`:49-55`). The design audit calls this out directly — "它的阶段名（orbit/charge/stabilize）已经和代码对不上，要重写" (`docs/release-1.6.0-plan-2026-09-19.md:51`) — and the code-change table assigns the rewrite plus the v10 doc to this issue: "See rubric_update; v9 spec superseded by docs/design/v10-ceremony-seam-of-light.md (state table, DEVICE timing table with settleMs/rimSettleMs vocabulary and device ceilings, RM path, fallback path, accessibility path, asset table, licences, Skia/worklets…" (`:175`). The DoD wants the rubric "with the evidence table" and the v10 doc "written (Chinese, dated, …" (`:190`), the probe numbers "in all 6 recordings × 3 widths" (`:186`), and "all samples sourced with per-file source + licence in assets/sfx/LICENSES.md (Sonniss recorded as royalty-free, not CC0), LICENSE-ASSETS committed excluding mobile/assets art and audio from MIT" (`:188`). The wave row is `docs/delivery-wave-1.6-plan-2026-09-19.md:96` (verify: "非空 + 链接存在").

What the rest of Wave B decided, which these docs must record rather than re-derive (every number below is from `docs/delivery/r16-issues/B00-contracts.md`, the binding contract): the phase union is unchanged and maps 1:1 onto the storyboard (§3.1); every transition is a `setTimeout` (§3.2); the DEVICE table is §2.2 with **multi approach = 900, not the storyboard's 920** (§9 #3 — the storyboard's "5020" table time becomes 5000); the Reduce Motion cadence is mount → `flash-reveal` at 0 → `settle` at 180 → table/`goResult` at 420 (§3.3); the renderer is `skia` only when `motionAvailable && skiaAvailable && flags.ceremony.seamOfLight && !flags.ceremony.forceFallback && !dev.forceFallback` (§3.5) with `ceremony: { seamOfLight: true, forceFallback: false }` as the flag defaults (§3.4); the 17-name SFX vocabulary is aliased onto the six committed WAVs through `SFX_ALIASES` because no new files are committed (§2.6, §9 #9) — the owner swaps real files in at wave end and `LICENSES.md` is where each swap is recorded; the asset list is §6; the cleanup rules (one Canvas per stage, no `shadowRadius ≥ 16` in the ceremony tree) are §7. The plan's `rubric_update` section is truncated in the file (`:175` ends in "…"), so the six new A-P0 names are fixed by this brief (Changes 1.4) — the one the plan names, `A-P0-EARLY-TELL` (`:297`), is among them.

Three facts about the repo's guards, verified on this tree:

1. `frontend/tests/docsPaths.test.ts` reads **only the top-level `docs/*.md` files** — `const DOCS = \`${REPO_ROOT}docs\`` (`:85`) and `readdirSync(DOCS).filter(name => name.endsWith('.md'))` (`:110-111`), non-recursive. Nothing under `mobile/docs/` or `docs/delivery/` is scanned, so this issue cannot affect that test; the spec's "confirm" is confirmed. The verify still reproduces its two rules (fully-qualified backticked paths must exist; relative links must resolve) over the five files this issue writes, because that is the whole of "链接存在".
2. `frontend/tests/rootReadmePaths.test.ts:45-49` asserts the root `LICENSE` exists and has no template placeholders (`YOUR NAME`, `[year]`, `[fullname]`, `<name>`). `LICENSE` (MIT, "Copyright (c) 2026 Leo Qiao", `LICENSE:1-3`) is NOT edited by this issue; `LICENSE-ASSETS` is a separate file the README's "MIT — see LICENSE" line (`README.md:345`) does not need to change for. Do not put those placeholder strings in `LICENSE-ASSETS` either.
3. `mobile/assets/sfx/README.md` still documents `expo-av` (`:11`, `:27`, `:63-64`) and the six placeholder files "generated by `mobile/scripts/gen_sfx.py`" (`:40`). It is **not** in this issue's scope (the spec lists five files); `LICENSES.md` sits next to it and states the current truth (expo-audio, 17 names, aliases) so the README's staleness is contained, and the openQuestions of this brief flag the README for the owner.

**Budget.** The spec gives this issue 20 minutes (`waveB_issues.json` B14 `timeout: 20`; the driver should treat 40 as the realistic cap for ~500 lines of dated Chinese prose). Read only the B00 sections named in Read first 1 (not the whole file), copy the two timing tables verbatim from Changes 1.3, and aim for the lower end of every size target — the verify enforces floors (rubric ≥ 90, v10 ≥ 150, `LICENSE-ASSETS` ≥ 25, `LICENSES.md` ≥ 40 lines), not the targets.

**Citing the contract.** The Wave B briefs (`B00-contracts.md`, `B01`–`B15`) are committed to `delivery/r16-b-ceremony` by the driver before worktrees are cut; if `docs/delivery/r16-issues/B00-contracts.md` is nevertheless absent from your worktree, still write the two dated header lines exactly as Changes 1.1 / 2 prescribe (they cite the path WITHOUT backticks, so verify step 6's citation regex — which only matches backticked paths — never looks for it on disk) and elsewhere refer to it as the bare name `B00-contracts.md` (a backticked bare name does not start with `docs/`, so it is not a citation). Never write `` `docs/delivery/r16-issues/B00-contracts.md` `` in backticks.

This issue lands after B09 (deps). On the branch you start from, `mobile/src/components/ceremony/*`, `mobile/src/features/gacha/draw/{ceremonyTimings,spillSchedule,skipPolicy,ceremonyPrefs}.ts`, `mobile/assets/ui/*` and the generated card backs (B12) exist; B11 (deletes `CeremonyLottie.tsx`, `HolographicLayer.tsx`, `mobile/assets/lottie/`, `mobile/scripts/gen_lottie.py`) and B13 (`mobile/src/screens/dev/CeremonyTuning.tsx`) may or may not have merged yet — write the docs for the **post-B11/B13 state** and follow the path rule in Constraints so the verify's existence check cannot flip either way.

## Read first

1. `docs/delivery/r16-issues/B00-contracts.md` — ONLY these sections, in this order (skip the rest; the numbers you need are already copied into Changes 1.3): §2.2 (`TEST_BASE`, `DEVICE`, `TO_TABLE_CAP_MS`, `REDUCED_MOTION_*`, `TELL_FRACTION_OF_HOLD`, `FAST_FORWARD_*`, `SPILL_*`), §2.6 (audio names, `SFX_ALIASES`, gains), §3 (phase machine, RM path, flag, renderer, controls — the source of v10 §1, §3, §4, §6), §4.3 (testIDs — v10 §11), §6 (assets — v10 §9), §7 (cleanup rules — rubric `A-P0-CANVAS-BUDGET`), §9 #3 / #8 / #9 (cited by number in the docs). §2.3–2.5, §2.7–2.13, §2.16, §5 are NOT required reading: every identifier from them that the docs must carry is listed verbatim in Changes 2 and Acceptance 3.
2. `docs/release-1.6.0-plan-2026-09-19.md:56-82` (§2, the 16 reveal-grammar rules — the rubric dimensions cite them), `:89-117` (§3.1 storyboard S0–S6 / M0–M6), `:119-133` (§3.2 tell, §3.3 agency/skip, §3.4 RM, §3.5 performance budget), `:135-148` (§3.6 asset list), `:177-191` (DoD).
3. `mobile/docs/qa/animation-quality-rubric.md` (71 lines, the file you rewrite — keep its section shape: Scope / Pass Gate / 评分维度 / A-P0 / 数据采集 / 商业级定义, add the evidence table).
4. `mobile/docs/design/v9-draw-final-spec-and-qa.md:1-5` (H1 + "Last updated" header — the banner goes after line 1), `:112-125` (the old duration windows the v10 doc replaces); `mobile/docs/design/v9-copy-delta.md:1-3` (H1), `:92-106` (§7 old ceremony copy: `Legendary inbound` / `Rare inbound` / `Card revealed` — superseded by B10's `Pack inbound` / `Pack open`).
5. `mobile/src/features/gacha/draw/ceremonyTimings.ts` (B02, as merged) — copy the numbers from the file, then check them against B00 §2.2; if they differ, B00 wins and you say so in the doc.
6. `mobile/src/components/ceremonyAudio.ts` (B04, as merged) — `SFX_ALIASES` and `CEREMONY_GAIN` are the source for `LICENSES.md`'s "today" column and the v10 audio section.
7. `mobile/assets/sfx/` (six WAVs + `README.md`), `mobile/scripts/gen_sfx.py` (the generator of the six placeholders), `mobile/assets/packs/`, `mobile/assets/ui/` (B12) — the inventory `LICENSE-ASSETS` lists.
8. `LICENSE:1-3`, `README.md:343-345`, `frontend/tests/docsPaths.test.ts:80-120`, `frontend/tests/rootReadmePaths.test.ts:45-49`.
9. House style for dated docs: `mobile/docs/qa/1.5.0-preflight-findings.md:1-12` (Chinese, dated first line, evidence-first), `mobile/docs/design/gacha-gate-decision-log.md:1-8`.
10. Format exemplar for this brief: `docs/delivery/r16-issues/A08-draw-state-adoption.md`.

## Constraints

- **Scope (the ONLY files that may change; C = new):**
  - E (rewrite) `mobile/docs/qa/animation-quality-rubric.md`
  - C `mobile/docs/design/v10-ceremony-seam-of-light.md`
  - E `mobile/docs/design/v9-draw-final-spec-and-qa.md` — superseded banner only (2 added lines, 0 deleted); the design doc names this file (`release-1.6.0-plan:175`) and B00 §1 lists it
  - E `mobile/docs/design/v9-copy-delta.md` — superseded banner only (2 added lines, 0 deleted); the spec names this file
  - C `LICENSE-ASSETS` (repo root, no extension)
  - C `mobile/assets/sfx/LICENSES.md`
- **Frozen files (gacha-v7.md §2.1) — zero diff:** `mobile/src/content/deckRepository.ts`, `mobile/src/sync/progressSync.ts`, `mobile/src/review/model.ts`. Also untouched: `LICENSE`, `README.md`, every file under `docs/` except `docs/delivery/r16-issues/`, `mobile/assets/sfx/README.md`, `mobile/assets/packs/README.md`, every `.ts`/`.tsx`/`.py`/`.json` file. This issue writes prose only.
- **No dependencies, no code, no tests.** The driver still runs `cd mobile && npm run test:typecheck && npx vitest run` — it is unaffected by prose.
- **Language:** rubric and v10 doc in Chinese with English identifiers, testIDs, file names and numbers (the house style of `1.5.0-preflight-findings.md`); `LICENSE-ASSETS` and `LICENSES.md` in English (licence texts are read by people outside the project). Every doc carries the date `2026-09-20` in its first three lines.
- **Numbers come from B00, not from the storyboard.** Multi approach is **900** (B00 §9 #3); never write `920` or `5020` as a ceremony timing anywhere in the five files (the verify greps the digits `920` wherever they are not part of a longer number — `920ms`, `920)`, `920 |` all trip it — and `5020`). Every other DEVICE/TEST_BASE value is B00 §2.2 verbatim (table in Changes 1.3).
- **Path rule (the verify reproduces `docsPaths.test.ts`'s citation regex):** a backticked path that starts with `mobile/`, `docs/`, `frontend/`, `src_C/`, `pg-layer/`, `snowflake/` or `.github/` (with an optional `:N` / `:N-M` suffix) must exist on disk in your worktree. Files that B11 deletes (`CeremonyLottie.tsx`, `HolographicLayer.tsx`, `gen_lottie.py`, the `assets/lottie` directory) and SFX files that are not committed yet (`crinkle.wav`, `air.wav`, … — everything except the six placeholders) are referred to by **bare name only**, never as a fully-qualified backticked path. Relative markdown links (`[..](./x.md)`, `[..](../qa/y.md#anchor)`) must resolve from the linking file's directory; absolute `http(s)://` links are not checked.
- **Rubric contract literals** (verify greps them; write them exactly): the phase chain line `swipe -> approach -> hold -> tear-flip -> flash-reveal -> settle -> cards-on-table`; the twelve A-P0 identifiers of Changes 1.4; the table cells of Changes 1.3; `TO_TABLE_CAP_MS`; `p95`, `22 ms`, `50 ms`; the evidence-table header of Changes 1.6.
- **v10 contract literals:** the fourteen H2 headings of Changes 2 verbatim, and the identifiers listed in Acceptance 3.
- **Banned words in all five files** (the driver's term gate covers only source trees, but these docs must not teach the words back into the code): `humanizer`, `bypass`, `undetect`, `detector`, `evade`, `Gemini said`. Write "skips past", "escape hatch", "probe", "guard". (`release-1.6.0-plan:189` uses "bypasses" — paraphrase it, do not quote it.)
- **No placeholder strings** in `LICENSE-ASSETS` / `LICENSES.md`: `YOUR NAME`, `[year]`, `[fullname]`, `<name>`. Unknown cells are `TBD`.

## Changes required

1. **`mobile/docs/qa/animation-quality-rubric.md` — full rewrite** (floor 90 lines, target 100–160). Keep the six section titles' shape, replace every number and phase name. Required content, in order:
   1. **H1 + date + scope.** `# Draw Animation Quality Rubric (Commercial Grade) — v10 Seam of Light`; second line `2026-09-20 · supersedes the v9 rubric (orbit/charge/stabilize) · contract: docs/delivery/r16-issues/B00-contracts.md`; scope: `mobile/src/screens/DrawCeremonyScreen.tsx` + `mobile/src/components/ceremony/`, one chain for single and multi, written exactly as `swipe -> approach -> hold -> tear-flip -> flash-reveal -> settle -> cards-on-table`, with the storyboard mapping S0–S6 / M0–M6 (B00 §3.1) and the rule "每次阶段切换都是 `setTimeout`，没有任何原生回调参与导航" (B00 §3.2).
   2. **Pass Gate:** 总分 ≥ 90；全部 A-P0 = 0；`tests/integration/draw-ceremony.screen.test.tsx`、`tests/integration/draw-result.screen.test.tsx`、`tests/unit/ceremonyTimings.test.ts` 全绿；六个（isMulti × 稀有度）组合的 `toTableMs` ≤ `TO_TABLE_CAP_MS`（single 3300 / multi 5500）；探针 p95 < 22 ms 且无单帧 > 50 ms（tear/spill 期间）；首次 LEG 翻牌卡顿 < 50 ms。
   3. **评分维度（100 分）** A–F rewritten, each citing the grammar rule it scores (`release-1.6.0-plan:62-77`): **A Anticipation 15** — approach+hold 内 ≥ 2 参数同时变化（rays + packScale + halo），稀有度在 swipe/approach 全通道（含文案 `Pack inbound`）保密，tell 在 hold 前 60% 到达，silence beat 可见；**B Reveal 20** — 一次撕封口动画对所有稀有度相同；multi 的 spill 张数 = 抽到张数（`buildSpillSchedule`，不再补到 6 张），featured 卡最后落到中心槽（`centreSlot`）；`cards-on-table` 靠计时器必达，每次点击只翻一张，非 table 阶段 `TapCard` `disabled`；**C Settle 15** — settle ≥ settleMs（DEVICE 520 / 600），CTA `Show result` 只在 settle 出现，稀有度词只在有牌面朝上后进 footer；**D Timing 20** — the table of 1.3: 全部六组在上限内给 20，任一组超上限扣到 10，两组及以上为 0，anticipation 带宽（single COM 600–1200，其余 1200–2000）另计；**E Reduced Motion 15** — 0 → `flash-reveal`（标题 `Pack open`）→ 180 settle（`Show result`）→ 420 table/`goResult`，总 ≤ 450 ms，contract flash `opacity` 恒 0，无 scale/rotate/shake，恰好一次 `light` 触觉，rim 颜色作为静态 tell ≥ 400 ms；**F Frame Continuity 15** — p95 < 22 ms，max ≤ 50 ms，首次 LEG 翻牌 < 50 ms，`CeremonyTuning` 探针 + Instruments/gfxinfo，360/390/430 三宽度。
   4. **A-P0 Hard Fail — twelve identifiers, each on its own bullet, exactly these names.** Retained (rewritten text): `A-P0-NO-ANTICIPATION`, `A-P0-ORDER-BROKEN`（multi 的 featured 卡不是最后落到中心槽，或 single 无 settle 锁定态）, `A-P0-TIMING-OUT`（任一组 `toTableMs` > 上限，或 TEST_BASE 逐字改动）, `A-P0-REDUCED-MOTION-MISSING`, `A-P0-FRAME-JANK`, `A-P0-COPY-MISMATCH`. New (six): `A-P0-EARLY-TELL`（hold 之前任何通道——halo/rays/文案/footer/音频——泄露稀有度；`release-1.6.0-plan:297`）, `A-P0-NATIVE-CALLBACK`（任何阶段切换或导航依赖原生回调；B00 §3.2）, `A-P0-RM-FLASH`（Reduce Motion 下 flash 矩形挂载或 contract View `style[1].opacity` ≠ 0，或出现 scale/rotateY/shake）, `A-P0-SKIP-ESCAPES`（`skipPolicy` 之外的任何快进路径；`compress` 之外的决策；首次仪式在 settle 前出现可见跳过控件；`goResult` 在 settle 前被非 `Leave ceremony` 控件调用）, `A-P0-CANVAS-BUDGET`（舞台 Skia `Canvas` > 1 或聚焦卡 canvas > 1；`BackdropBlur`/`Blur`/`DisplacementMap`/每帧 `maskFilter`；仪式树内 `shadowRadius ≥ 16`；B00 §7）, `A-P0-A11Y-PACK`（卡包不是 `accessibilityRole="button"`、无 `activate` 动作、`Leave ceremony` 在任一阶段不可达、table 卡片无标签）.
   5. **数据采集要求:** 自动化（三份测试 + `tests/setup/ceremony.ts` 的 `motionAvailable=false` 说明测试永远走 TEST_BASE + fallback）；半自动：`CeremonyTuning`（`More → Settings → DebugMenu → Ceremony tuning`，`__DEV__`）的探针读数、DebugMenu `Force fallback renderer` / `Force repeat ceremony` / `Seed wallet 30/5` / `Only Legendary left`；真机：6 段录屏（single COM / single LEG / 10-pull LEG × iPhone + Android）× 3 宽度，Instruments (Core Animation) + `adb shell dumpsys gfxinfo`。
   6. **证据表（真机 QA 填写）** — a markdown table whose header row is exactly `| Device | Width | Pull | toTableMs | Cap | p95 ms | max ms | LEG flip stall ms | RM | VoiceOver | Fallback | Recording |` followed by the separator and **six rows** — Device `iPhone` / `Android` × Pull `single COM` / `single LEG` / `10-pull LEG`, Width `360 / 390 / 430`, Cap `3300` or `5500`, and **every measured cell (`toTableMs`, `p95 ms`, `max ms`, `LEG flip stall ms`, `RM`, `VoiceOver`, `Fallback`, `Recording`) written as `TBD`** (the verify counts rows containing a `| TBD |` cell) — plus one sentence saying the table is filled at wave end (`docs/delivery-wave-1.6-plan-2026-09-19.md:99`).
   7. **商业级定义** updated: 用户在 3.3 s（single）/ 5.5 s（multi）内到达桌面；任何时点能回答三件事（哪个阶段 / 揭示什么 / 下一步去哪）；RM 下信息完整；稀有度靠质感（rim/箔/光）而不是文字提前透露。

   The DEVICE/TEST_BASE table (**1.3**, copy verbatim into the rubric §D and the v10 doc §2 — the verify greps the `DEVICE` and `TEST_BASE` rows and the totals):

   ```
   | table | single approach | single hold COM/RAR/LEG | single tearFlip | single flashReveal | single settleMs | multi approach | multi hold COM/RAR/LEG | multi tearFlip | multi flashReveal | multi settleMs | tableTailMs |
   |---|---|---|---|---|---|---|---|---|---|---|---|
   | DEVICE | 600 | 360 / 620 / 880 | 600 | 320 | 520 | 900 | 600 / 860 / 1100 | 1800 | 400 | 600 | 200 |
   | TEST_BASE | 300 | 140 / 180 / 220 | 360 | 220 | 200 | 620 | 220 / 260 / 300 | 940 | 280 | 300 | 500 |
   ```
   and the totals table:
   ```
   | pull | anticipation (approach+hold) | settle 开始 | cards-on-table (toTableMs) | TO_TABLE_CAP_MS |
   |---|---|---|---|---|
   | DEVICE single COM | 960 | 1880 | 2600 | 3300 |
   | DEVICE single RAR | 1220 | 2140 | 2860 | 3300 |
   | DEVICE single LEG | 1480 | 2400 | 3120 | 3300 |
   | DEVICE multi COM | 1500 | 3700 | 4500 | 5500 |
   | DEVICE multi RAR | 1760 | 3960 | 4760 | 5500 |
   | DEVICE multi LEG | 2000 | 4200 | 5000 | 5500 |
   | TEST_BASE single RAR | 480 | 1060 | 1760 | — |
   | TEST_BASE multi LEG | 620 + 300 | 2140 | 2940 | — |
   ```
   (The last anticipation cell is written as a sum on purpose: the verify bans the bare literal for the storyboard's stale multi approach value everywhere in the five files, and the TEST_BASE sum happens to collide with it.) Sub-beat constants (B00 §2.2, list them under the table): `beatMs` 120 / 180 / 300, `flipMs` 380 / 480 / 640, `rimSettleMs` 800 / 1000 / 1200, `liftMs` 80, `landMs` 200, `tapQueueMs` 90, `REDUCED_MOTION_FLASH_MS` 180, `REDUCED_MOTION_SETTLE_MS` 240, `SWIPE_TRIGGER_DISTANCE` 72, `TELL_FRACTION_OF_HOLD` 0.6, `FAST_FORWARD_FROM_HOLD_FRACTION` 0.6, `FAST_FORWARD_TEAR_FACTOR` 1.6, `SPILL_STAGGER_MS` 60, `LEG_HIT_PAUSE_MS` 32, `LEG_DIM` 0.3.

2. **`mobile/docs/design/v10-ceremony-seam-of-light.md` (new, Chinese, floor 150 lines, target 180–250; every section may be short as long as it carries its listed identifiers).** First line `# v10 仪式设计：Seam of Light（1.6.0）`, second line `2026-09-20。取代 v9-draw-final-spec-and-qa.md 与 v9-copy-delta.md §7；实现契约见 docs/delivery/r16-issues/B00-contracts.md，本文只记录已建成的状态与真机验收口径。` Exactly these fourteen H2 headings, in this order, verbatim (the verify greps each):
   1. `## 0. 结论与范围` — what shipped (single + minimal multi + table + RM + fallback), what is OTA-able vs binary, deps pinned (`@shopify/react-native-skia` exactly `2.2.12`, `react-native-reanimated` ~4.1.1, `react-native-worklets` 0.5.1, `expo-audio`; `lottie-react-native` and `expo-av` removed), and the one discriminant `motionAvailable` (`mobile/src/components/ceremony/reanimatedGuard.ts`).
   2. `## 1. 状态机（7 阶段）` — a table: phase | storyboard | enters at | title (`CEREMONY_COPY_V9` — approach `Pack inbound` for all rarities, flash-reveal `Pack open`, settle `Cards in place`) | what the timeline drives (`useCeremonyTimeline` targets, B00 §2.10) | sub-beats (tell, silence beat, LEG hit-pause, spill). State the timer rule of B00 §3.2 and which controls may call `goResult` (settle CTA, table CTA, `Leave ceremony`, RM/tapFlow-off tail).
   3. `## 2. 时长表：DEVICE / TEST_BASE 与上限` — the two tables of Changes 1.3 verbatim, the anticipation band, the sub-beat constants, and one paragraph on B00 §9 #3 (why multi approach is 900) written WITHOUT the storyboard's original values (verify step 7 bans the standalone numbers 920 and 5020 in all five files). Use this wording or an equivalent that names no rejected number: `§3.1 的 multi approach 比 §2 的预期带宽（Rare+/multi 1200–2000 ms）多出 20 ms，B00 §9 #3 把它收到 900：LEG multi anticipation 恰好 2000，铺桌 5000，单元测试不用为它开特例。` Do not write phrases like "不是 storyboard 的 9xx" with the number spelled out.
   4. `## 3. 减弱动态路径（Reduce Motion）` — the 0/180/420 cadence, no swipe phase, no flash (`draw-ceremony-reveal-flash` `style[1].opacity` 0), opacity-only 180 ms crossfades, static rim tell, one `light` haptic, `tail('soft-chime')`, mid-ceremony `reduceMotionChanged` restarts the effect.
   5. `## 4. Fallback 渲染器与 feature flag` — `renderer = 'skia'` condition verbatim (B00 §3.5); `FeatureFlags.ceremony = { seamOfLight: true, forceFallback: false }` and what each flips (kill switch vs diagnostic, B00 §3.4); `FallbackStage` (`draw-ceremony-fallback-stage`) vs `StageCanvas` (`draw-ceremony-stage-canvas`); timings and tapFlow follow `motionAvailable` alone; what happens with every PNG/SFX missing.
   6. `## 5. 无障碍路径（VoiceOver）` — pack `accessibilityRole="button"`, label `Reward pack`, hint `Swipe right or double-tap to open`, `activate` action; phase copy `numberOfLines={1}` live; `Leave ceremony` always mounted at opacity 0 and never `accessibilityElementsHidden`; `Speed up` ×; table cards `Card n of N, face down` / `… revealed`; `SpillSampler` `Dealing cards, N percent`.
   7. `## 6. 玩家主动权：skip policy 与 tap-to-flip` — `skipPolicy` rules (B00 §2.4: `'none' | 'compress'`, never leaves the ceremony), `ceremoniesCompleted` fail-closed 0 (`ceremonyPrefs`), `compressTimings`, table CTA copy (`Show result` / `Skip · n/N` / `Continue`), `revealedUids` + `ceremonyEcho.tableReached` handed to DrawResult, `Not flipped` chips.
   8. `## 7. 稀有度预告：tell / beat / spill` — one channel (colour temperature: `TELL_COLORS`), withheld through swipe/approach in every channel incl. copy, tell over the first 60 % of hold, beat = last `beatMs`, LEG dim 0.3 + 18 Hz shiver + 32 ms hit-pause, `buildSpillSchedule` (start 50 % of tear, travel 1/6, stagger 60, featured last to `centreSlot`), face-down rims as the multi second tell.
   9. `## 8. 音频与触觉` — three layers, the 17 names grouped bed/hit/tail, `SFX_ALIASES` (which name plays which committed file today), `CEREMONY_GAIN` (bed .30/.35/.40, table .25, duck .15), prewarm on DrawScreen mount, haptic limiter ≤ 3 / 1000 ms, `success()` once per ceremony and LEG only, RM = one `light`.
   10. `## 9. 素材表与许可` — the B00 §6 table (path, size, generator script, `packArt.ts` export, consumers) using fully-qualified paths only for files that exist (`mobile/assets/ui/frame-com.png` … `mobile/assets/packs/aws-back.png`, `mobile/scripts/gen_card_frames.py` …), the six committed WAVs, and the licence pointers: `LICENSE-ASSETS` (root) and `mobile/assets/sfx/LICENSES.md`.
   11. `## 10. 原生依赖钉版（Skia / Reanimated / worklets）` — why Skia stays exactly 2.2.12 (worklets ≥ 0.7 needed by newer Skia, Expo 54 ships 0.5.1), no `babel.config.js`, the `_WORKLET` dev probe in `mobile/App.tsx`, `RoundedRect` not `RoundRect`, the `useClock`/`useDerivedValue` crash that killed the old HolographicLayer (bare name) and the grep guard (no `useClock` under `mobile/src/components`).
   12. `## 11. 文件地图与 testID 契约` — the B00 §1 file map condensed to the ceremony tree (created / rewritten / deleted, deleted ones by bare name), and the B00 §4.3 testID list.
   13. `## 12. 开发工具（CeremonyTuning / DebugMenu）` — B13's screen (`mobile/src/screens/dev/CeremonyTuning.tsx` — cite it as a bare name `CeremonyTuning.tsx` if the file is not on your tree yet), stepper rows write `setCeremonyTimingOverride`, cap readout, rAF probe (JS thread only — UI-thread cadence needs Instruments/gfxinfo), DebugMenu seeds.
   14. `## 13. 证据表（真机 QA 填写）` — the same header row and six `TBD`-celled rows as Changes 1.6, and the sign-off line (owner watches single COM / single LEG / 10-pull LEG side-by-side with Pocket/Hearthstone references, `release-1.6.0-plan:191`).

3. **Superseded banners.** Insert, directly after line 1 (the H1) of each v9 file, exactly two lines — an empty line and one blockquote — so `git diff --numstat` reads `2 0` for each file and line 1 is untouched:
   - `mobile/docs/design/v9-draw-final-spec-and-qa.md`:
     ```
     > **Superseded — 2026-09-20.** 1.6.0 的 Seam of Light 仪式取代了本契约（阶段时长窗口、编排门、性能门、测试矩阵）。现行规范见 [v10-ceremony-seam-of-light.md](./v10-ceremony-seam-of-light.md)，评分见 [animation-quality-rubric.md](../qa/animation-quality-rubric.md)。以下内容仅作历史记录，不再是需求。
     ```
   - `mobile/docs/design/v9-copy-delta.md`:
     ```
     > **Superseded — 2026-09-20.** §7 的仪式阶段文案已被 1.6.0 改写：approach 三档统一为 `Pack inbound`，flash-reveal 为 `Pack open`（`Legendary inbound` / `Rare inbound` / `Card revealed` 不再存在）。现行规范见 [v10-ceremony-seam-of-light.md](./v10-ceremony-seam-of-light.md)。其余章节沿用。
     ```

4. **`LICENSE-ASSETS` (repo root, English, target 40–70 lines).** Plain text/markdown-compatible. Required content: (a) a first line `RecallSmith asset licence — 2026-09-20`; (b) a paragraph stating that the MIT licence in `LICENSE` covers the source code and documentation only, and that everything under `mobile/assets/` (app icon/splash, pack covers, card backs, rarity frames, foil LUT, particle sheet, glow sprite, sound effects) is **excluded from the MIT grant** — "All rights reserved, Leo Qiao, 2026" for project-made art, third-party audio under the terms recorded per file in `mobile/assets/sfx/LICENSES.md`; (c) an inventory table `| Path | What | Origin | Licence |` with one row per directory/group: `mobile/assets/icon.png` + `adaptive-icon.png` + `splash-icon.png` + `favicon.png` (project-made), `mobile/assets/packs/` (covers: project-made via `mobile/scripts/gen_packs.py` or commissioned — mark the AWS cover row `TBD` until the owner confirms), `mobile/assets/packs/*-back.png` (`mobile/scripts/gen_card_back.py`), `mobile/assets/ui/` (`gen_card_frames.py`, `gen_particles.py`), `mobile/assets/sfx/` (six synthetic placeholders from `mobile/scripts/gen_sfx.py`; replacements per `LICENSES.md`); (d) a "Third-party names" paragraph: deck titles that name a vendor exam or language (AWS SAA-C03, C#) are used descriptively, the app is not affiliated with or endorsed by those vendors and is not an exam simulator (the M5 trademark line, `release-1.6.0-plan:228`); (e) a "How to add an asset" list: put the file in place, add a row here (art) or in `LICENSES.md` (audio), never commit CC-BY-NC / CC-BY-SA / unknown-licence audio, record Sonniss files as "Sonniss royalty-free (GDC bundle licence)", not CC0. No placeholder tokens (`[year]`, `YOUR NAME`, `[fullname]`, `<name>`).

5. **`mobile/assets/sfx/LICENSES.md` (new, English, target 50–90 lines).** (a) H1 `# Ceremony SFX — per-file sources and licences`, date line `2026-09-20`; (b) rules: accepted licences `CC0`, `CC-BY 4.0` (attribution text mandatory, copied into the table and into the app's credits screen when one exists), `Sonniss` GDC bundles (record as `Sonniss royalty-free`, **not CC0**); rejected: `CC-BY-NC`, `CC-BY-SA`, "free for personal use", unknown; format 44.1 kHz 16-bit mono WAV, ≤ 120 KB each, beds loop-clean; (c) the alias note: today only six files are committed (`whoosh.wav`, `rip.wav`, `card-drop.wav`, `card-flip.wav`, `shimmer.wav`, `legendary.wav`, synthetic placeholders from `mobile/scripts/gen_sfx.py`) and `SFX_ALIASES` in `mobile/src/components/ceremonyAudio.ts` maps every other name onto one of them; to swap a real file in, drop it under this directory with the intended name, then change that name's entry in `SFX_ALIASES` to itself, restart Metro with `-c`; (d) the table with header `| File | Layer | Plays at | Today (aliased to) | Source URL | Author | Licence | Attribution text | Processing |` and **exactly 17 rows**, one per name, intended file names with `.wav`: bed `crinkle.wav`, `air.wav`, `shimmer-pad.wav`, `choir-swell.wav`; hit `whoosh.wav`, `rip.wav`, `card-slide.wav`, `stack-thud.wav`, `seam-burst.wav`, `card-flip.wav`, `card-drop.wav`, `chime.wav`, `stinger.wav`, `shimmer.wav`, `legendary.wav`; tail `sparkle-tail.wav`, `soft-chime.wav`. "Today" column from B00 §2.6 (`crinkle→shimmer`, `air→shimmer`, `shimmer-pad→shimmer`, `choir-swell→legendary`, `card-slide→card-drop`, `stack-thud→card-drop`, `seam-burst→rip`, `chime→shimmer`, `stinger→legendary`, `sparkle-tail→shimmer`, `soft-chime→shimmer`; the six committed names map to themselves). Source/Author/Licence/Attribution cells are `TBD` for every row; the six committed rows' Licence cell reads `synthetic placeholder (project-made, see LICENSE-ASSETS)`. Refer to uncommitted files by bare name only (`crinkle.wav`), never `mobile/assets/sfx/crinkle.wav`. **Row format (verify step 5 counts rows matching `^\| *`?[a-z-]+\.wav`? *\|`):** the first cell is the bare file name in backticks, e.g. `` `crinkle.wav` ``, no bold, no path, no annotation inside the cell; `LICENSES.md` contains exactly ONE table whose first-column cells end in `.wav` — list the six committed files in (c) as an inline sentence or a bullet list, never as a second table.

Estimated size: rubric ≈ 110 lines, v10 ≈ 200, banners 2 + 2, `LICENSE-ASSETS` ≈ 40, `LICENSES.md` ≈ 55 (≈ 410 lines of prose in total; the tables in Changes 1.3 and the 17-row template are copy-paste).

## Acceptance

Run from the worktree root. `docs/delivery/r16-issues/B14.verify.sh` runs exactly these.

1. The five files exist and are non-trivial: `mobile/docs/qa/animation-quality-rubric.md` ≥ 90 lines, `mobile/docs/design/v10-ceremony-seam-of-light.md` ≥ 150 lines, `LICENSE-ASSETS` ≥ 25 lines, `mobile/assets/sfx/LICENSES.md` ≥ 40 lines; each of the four contains `2026-09-20` within its first three lines.
2. Rubric guards: contains `swipe -> approach -> hold -> tear-flip -> flash-reveal -> settle -> cards-on-table`, `TO_TABLE_CAP_MS`, `p95`, `22 ms`, `50 ms`, `| DEVICE | 600 | 360 / 620 / 880 | 600 | 320 | 520 | 900 | 600 / 860 / 1100 | 1800 | 400 | 600 | 200 |`, `| TEST_BASE | 300 | 140 / 180 / 220 | 360 | 220 | 200 | 620 | 220 / 260 / 300 | 940 | 280 | 300 | 500 |`, `| 3120 | 3300 |`, `| 5000 | 5500 |`, the twelve A-P0 identifiers (`A-P0-NO-ANTICIPATION`, `A-P0-ORDER-BROKEN`, `A-P0-TIMING-OUT`, `A-P0-REDUCED-MOTION-MISSING`, `A-P0-FRAME-JANK`, `A-P0-COPY-MISMATCH`, `A-P0-EARLY-TELL`, `A-P0-NATIVE-CALLBACK`, `A-P0-RM-FLASH`, `A-P0-SKIP-ESCAPES`, `A-P0-CANVAS-BUDGET`, `A-P0-A11Y-PACK`), the evidence header `| Device | Width | Pull | toTableMs | Cap | p95 ms | max ms | LEG flip stall ms | RM | VoiceOver | Fallback | Recording |`, `CeremonyTuning`, `Pack inbound`, `Pack open`; contains none of `orbit -> charge -> stabilize`, `warmup -> focus -> reveal`, `charge@`, `stabilize@`, `2100ms ~ 2600ms`, `1100ms ~ 1400ms`, `Legendary inbound`, `Rare inbound`.
3. v10 guards: the fourteen H2 headings verbatim, in order; contains `Seam of Light`, `motionAvailable`, `skiaAvailable`, `seamOfLight`, `forceFallback`, `settleMs`, `rimSettleMs`, `tableTailMs`, `beatMs`, `flipMs`, `TO_TABLE_CAP_MS`, `REDUCED_MOTION_FLASH_MS`, `REDUCED_MOTION_SETTLE_MS`, `SFX_ALIASES`, `CEREMONY_GAIN`, `skipPolicy`, `buildSpillSchedule`, `centreSlot`, `2.2.12`, `react-native-worklets`, `react-native-reanimated`, `expo-audio`, `RoundedRect`, `Pack inbound`, `Pack open`, `Reward pack`, `Leave ceremony`, `Speed up`, `draw-ceremony-stage-canvas`, `draw-ceremony-fallback-stage`, `draw-ceremony-reveal-flash`, `CeremonyTuning`, `LICENSE-ASSETS`, `LICENSES.md`, `B00-contracts.md`, both DEVICE/TEST_BASE rows and `| 3120 | 3300 |` / `| 5000 | 5500 |`, the evidence header; the H1 is `# v10 仪式设计：Seam of Light（1.6.0）`.
4. Banners: both v9 files contain `> **Superseded — 2026-09-20.**` and `v10-ceremony-seam-of-light.md`; `git diff --numstat <merge-base> -- <file>` is exactly `2 0` for each; line 1 of each is unchanged (`# v9 Draw Final Spec + Quantitative QA Gates (Full Contract)` / `# v9 Copy Delta (vs v8)`).
5. Licence files: `LICENSE-ASSETS` contains `MIT`, `LICENSE`, `mobile/assets/`, `mobile/assets/sfx/LICENSES.md`, `All rights reserved`, `Sonniss`, `not affiliated`; `LICENSES.md` contains all 17 `*.wav` names, `SFX_ALIASES`, `mobile/scripts/gen_sfx.py`, `Sonniss`, `royalty-free`, `CC0`, `CC-BY`, `CC-BY-NC`, exactly 17 table rows whose first cell ends in `.wav`; neither contains `YOUR NAME`, `[year]`, `[fullname]`, `<name>`; root `LICENSE` and `README.md` have zero diff.
6. Links + paths (the spec's "链接存在"): for each of the five files, every relative markdown link resolves from that file's directory (anchor stripped), and every backticked fully-qualified path (regex of `frontend/tests/docsPaths.test.ts:95-98`, `:N`/`:N-M` suffix stripped) exists on disk; no top-level `docs/*.md` file changed (`git diff --name-only <merge-base> -- ':(glob)docs/*.md'` — the `:(glob)` magic keeps `*` from matching `/`, so briefs under `docs/delivery/r16-issues/` are not caught; `docsPaths.test.ts` scans only `docs/*.md`, `:85`, `:110`); if `frontend/node_modules/.bin/vitest` exists the verify also runs `cd frontend && npx vitest run tests/docsPaths.test.ts tests/rootReadmePaths.test.ts --reporter=dot` (no install, skipped with a note otherwise).
7. Hygiene (purely negative): none of the five files contains the number `920` (as a standalone number, in any unit or table cell) or `5020`, `humanizer`, `bypass`, `undetect`, `detector`, `evade`, `Gemini said`; scope guard (changed/untracked ⊆ scope ∪ `docs/delivery/r16-issues/`); frozen files zero diff; `"vite": "7.2.4"` still in `mobile/package.json`; no `@sentry` under `mobile/src`.

## Do NOT

- Do NOT edit any `.ts`/`.tsx`/`.py`/`.json` file, `LICENSE`, `README.md`, `mobile/assets/sfx/README.md`, `mobile/assets/packs/README.md`, any top-level `docs/*.md`, or any test. Do NOT add or remove asset files.
- Do NOT change any line of the two v9 docs other than inserting the two banner lines after line 1; do NOT delete, rename or "clean up" the v9 docs (`v9-reference-aligned-spec.md`, `v9-screen-diff.md`, `v9-test-impact.md`, `v9.1-mechanism-spec.md` are not touched at all).
- Do NOT write `920` or `5020` as a ceremony timing; do NOT copy numbers from `release-1.6.0-plan:89-117` where they differ from B00 §2.2.
- Do NOT cite a deleted (B11) or not-yet-committed (SFX) file as a fully-qualified backticked path; do NOT write `docs/delivery/r16-issues/…` inside backticks (the briefs may be absent from a worktree — cite them without backticks or as bare names); do NOT link to files outside the repo with relative links.
- Do NOT add a second `.wav`-keyed table to `LICENSES.md` and do NOT bold or path-qualify the first cell of the 17 rows.
- Do NOT invent licence facts: unknown source/author/licence cells are `TBD`; do NOT record any Sonniss file as CC0; do NOT declare the AWS pack cover's origin without the owner's confirmation.
- Do NOT use the banned words (`humanizer`, `bypass`, `undetect`, `detector`, `evade`, `Gemini said`) even when quoting the plan.
- Standing rules: no `git push`, no PR, never touch `main`, no EAS/expo/npm-install command, no test gutting, no loosening of `tsconfig`.
